# 2026-03-30

## DevOps

Hôm nay tìm hiểu về **CI/CD pipeline với GitHub Actions**. Đã thiết lập workflow tự động build và test khi push code lên branch `main`. Sử dụng `on: [push]` và `jobs` để chạy các step theo thứ tự. Rất tiện vì không cần server riêng, tích hợp luôn vào repo.

Plan: sẽ cấu hình thêm step deploy lên VPS khi test thành công.


## DevOps

Hôm nay tìm hiểu về cách dùng Docker Compose để quản lý multi-container application. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file YAML, rất tiện lợi cho việc phát triển và testing.

- Sử dụng `docker-compose up` để khởi động toàn bộ services được định nghĩa trong file `docker-compose.yml`.
- Lệnh `docker-compose down` giúp dừng và xóa các container, network, volume đã tạo.

Plan: Sẽ áp dụng Docker Compose vào dự án web app để đơn giản hóa việc deploy và quản lý database, backend, frontend.


## DevOps  

Hôm nay tìm hiểu về Docker Compose để quản lý multi-container application. Compose cho phép định nghĩa services, networks, volumes trong một file YAML, giúp việc deploy và scale app dễ dàng hơn. Sẽ áp dụng trong dự án tiếp theo để tối ưu hóa CI/CD pipeline.


## DevOps

Hôm nay tìm hiểu về **Docker multi-stage builds** để tối ưu hóa Docker image. Việc sử dụng multi-stage giúp giảm kích thước image đáng kể bằng cách chỉ giữ lại file cần thiết ở giai đoạn cuối. Điều này rất hữu ích khi deploy lên cloud, giúp tiết kiệm tài nguyên và tăng tốc độ pull image. Plan: sẽ áp dụng cho dự án Spring Boot đang làm.


## DevOps  

Hôm nay tìm hiểu về cách setup CI/CD pipeline cho một dự án Node.js nhỏ. Sử dụng GitHub Actions với các job build, test và deploy tự động. Cấu hình đơn giản chỉ cần 1 file `.github/workflows/main.yml` và một vài secrets cho server.  

Nhận thấy việc tự động hóa giúp tiết kiệm thời gian deploy và giảm lỗi manual. Plan sắp tới là áp dụng cho dự án cá nhân để làm quen với workflow này.


## DevOps

Hôm nay tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container applications. Docker Compose cho phép định nghĩa và chạy đồng thời nhiều containers thông qua file YAML, giúp đơn giản hóa việc triển khai và quản lý môi trường phát triển. Sẽ áp dụng vào dự án web app để test trước khi production.

## Automation

Học được cách tạo script tự động backup database bằng Python và cron job. Script sẽ export dữ liệu từ MySQL ra file .sql và gửi email thông báo khi hoàn tất. Kế hoạch là sẽ triển khai cho hệ thống nội bộ để đảm bảo dữ liệu an toàn.

## Travel Tech

Khám phá ứng dụng Maps.me - bản đồ offline hữu ích khi đi du lịch, đặc biệt ở những nơi không có internet. Ứng dụng này còn cho phép đánh dấu điểm tham quan và ghi chú cá nhân. Sẽ thử dùng trong chuyến đi Sa Pa sắp tới.

## Programming

Hôm nay tìm hiểu về design pattern Singleton trong Python. Pattern này đảm bảo chỉ có duy nhất một instance của class được tạo ra, hữu ích cho việc quản lý connection hay cấu hình. Sẽ áp dụng vào dự án hiện tại để tối ưu hóa resource.

## Trekking - Hiking - Camping

Địa điểm: Núi Bà Đen, Tây Ninh. Đây là ngọn núi cao nhất miền Nam, có hệ thống cáp treo hiện đại nhưng vẫn giữ được vẻ hoang sơ của đường mòn trekking. Thời điểm lý tưởng để trekking là sáng sớm để ngắm bình minh từ đỉnh núi. Kế hoạch là sẽ tổ chức chuyến trekking cuối tuần này cùng nhóm bạn.


## Programming

Hôm nay mình tìm hiểu về cách sử dụng **async/await** trong JavaScript để xử lý các tác vụ bất đồng bộ một cách hiệu quả hơn. Mình nhận ra rằng việc dùng async/await giúp code trở nên dễ đọc và dễ bảo trì hơn so với callback hay Promise chains truyền thống. Tuy nhiên, cần chú ý đến việc xử lý lỗi bằng try-catch để tránh trường hợp bỏ sót exception.

Plan: Áp dụng async/await vào dự án hiện tại để tối ưu hóa phần xử lý API.


## DevOps

Hôm nay tìm hiểu về Docker Compose, cách dùng file `docker-compose.yml` để định nghĩa và chạy multi-container applications. Compose giúp đơn giản hóa việc quản lý services, networks và volumes trong 1 lệnh duy nhất. Sẽ áp dụng cho dự án NodeJS + MongoDB đang làm.

## Automation

Học cách tạo script tự động backup database MySQL bằng crontab trên Linux. Script chạy hàng đêm, nén file backup và upload lên S3. Giúp giảm thiểu rủi ro mất dữ liệu và tiết kiệm thời gian thủ công.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** - công cụ giúp định nghĩa và chạy multi-container applications. Đặc biệt hữu ích khi muốn deploy cả một stack (web app + database + cache) chỉ với một file `docker-compose.yml`. Cảm thấy đây là bước tiến lớn so với việc chạy từng container riêng lẻ.

- Học được cách viết service, network, volume trong file compose.
- Test thử trên local thành công với ứng dụng Node.js + MongoDB.
- Plan: Áp dụng cho dự án cá nhân để rút ngắn thời gian setup môi trường.


## DevOps

Hôm nay tìm hiểu về CI/CD pipeline với GitHub Actions. Tự động build và test khi push code, deploy lên staging server nếu pass. Giúp giảm thiểu lỗi và tăng tốc độ release.

Plan: Áp dụng cho dự án cá nhân để làm quen trước khi triển khai cho team.
