# 2026-03-24

## DevOps
Hôm nay tìm hiểu về CI/CD pipeline trong GitLab. Mình thử tạo một pipeline đơn giản với các stages: build, test, deploy. Cảm thấy GitLab CI khá tiện lợi vì cấu hình chỉ bằng file `.gitlab-ci.yml`. Plan: sẽ áp dụng vào dự án hiện tại để tự động hóa quy trình deploy.


## DevOps

Hôm nay tìm hiểu về **AWS Lambda Layers**, giúp tái sử dụng code và thư viện giữa các function. Thay vì upload lại dependencies cho mỗi function, chỉ cần tạo layer chứa common libraries rồi attach vào. Giúp giảm kích thước function và dễ maintain hơn. Plan sẽ áp dụng cho các project microservices để standardize logging và error handling.


## DevOps

Hôm nay mình tìm hiểu về cách cấu hình CI/CD pipeline sử dụng GitHub Actions cho một dự án Node.js. Tự động build và test mỗi khi có commit mới, deploy lên staging khi merge vào branch main. Cảm thấy việc tự động hóa này giúp tiết kiệm thời gian và giảm thiểu lỗi do thao tác thủ công. Plan sẽ áp dụng cho dự án hiện tại để cải thiện quy trình làm việc.


## DevOps

Hôm nay tìm hiểu về Docker Compose để quản lý multi-container applications. Compose giúp định nghĩa services, networks và volumes trong một file YAML, giúp việc triển khai và scale ứng dụng dễ dàng hơn. Điều này rất hữu ích khi làm việc với microservices.

Plan: Áp dụng Docker Compose cho dự án cá nhân để rút ngắn thời gian setup môi trường phát triển.


## DevOps

Hôm nay tìm hiểu về cách setup CI/CD pipeline cho dự án Node.js sử dụng GitHub Actions. Tạo workflow `.yml` để tự động chạy test, build và deploy lên Heroku khi push code vào branch `main`. Cảm thấy GitHub Actions khá dễ dùng và tích hợp tốt với GitHub.

## Automation

Học được cách sử dụng Python's `schedule` library để tự động hóa các task định kỳ. Ví dụ: chạy script backup dữ liệu mỗi ngày lúc 2AM. Rất hữu ích cho các tác vụ lặp lại mà không cần can thiệp thủ công.

## Travel Tech

Khám phá ứng dụng Maps.me để offline navigation khi trekking ở Đà Lạt. App này cho phép tải map về trước, rất tiện lợi khi ở vùng sóng yếu. Kế hoạch cuối tuần sẽ thử nghiệm thực tế ở Langbiang.

## Programming

Hôm nay đọc về SOLID principles trong OOP. Đặc biệt là Dependency Inversion giúp code dễ maintain hơn. Sẽ áp dụng vào refactor một module cũ trong dự án sắp tới.

## Trekking - Hiking - Camping

Tìm hiểu về cung đường trekking Fansipan bằng cách đi bộ từ Sapa town. Độ khó trung bình - cao, cần chuẩn bị đồ ấm và nước uống đầy đủ. Plan tháng sau sẽ thử thách bản thân với cung này.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** để quản lý multi-container applications. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file `docker-compose.yml`, rất tiện lợi cho việc phát triển và testing.

- Có thể định nghĩa services, networks, và volumes trong một file.
- Lệnh `docker-compose up` giúp khởi động toàn bộ ứng dụng chỉ với một command.

Plan: Sẽ áp dụng Docker Compose cho dự án cá nhân để đơn giản hóa việc setup môi trường development.


## DevOps

Hôm nay tìm hiểu về Docker multi-stage builds, giúp tối ưu Docker image bằng cách tách biệt môi trường build và runtime. Sử dụng nhiều FROM trong cùng một Dockerfile, chỉ giữ lại những file cần thiết ở stage cuối cùng. Giảm được đáng kể dung lượng image và tăng tốc độ deploy. Sẽ áp dụng cho project sắp tới để cải thiện CI/CD pipeline.
