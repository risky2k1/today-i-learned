# 2026-03-25

## Programming  

Hôm nay tìm hiểu về cách sử dụng `git bisect` để tìm lỗi trong lịch sử commit. Công cụ này rất hữu ích khi cần xác định commit nào đã gây ra bug, giúp tiết kiệm thời gian debug đáng kể.  

- `git bisect` sử dụng binary search để thu hẹp phạm vi tìm kiếm.  
- Kết hợp với test tự động để xác định commit lỗi nhanh chóng.  

Plan: Áp dụng vào dự án hiện tại để tìm nguyên nhân lỗi performance gần đây.


## DevOps

Hôm nay tìm hiểu về Docker Compose. Compose giúp định nghĩa và chạy multi-container applications bằng YAML file. Thay vì chạy từng container riêng lẻ, chỉ cần một lệnh `docker-compose up` là xong. Rất tiện cho local development và testing.

Cá nhân thấy đây là công cụ "must-have" khi làm việc với microservices, giúp tiết kiệm thời gian setup môi trường. Plan: sẽ áp dụng cho dự án nhỏ đang làm để tối ưu quy trình dev.


## DevOps  

Hôm nay tìm hiểu về **GitOps** - một cách tiếp cận để quản lý hạ tầng và ứng dụng thông qua Git.  
- GitOps sử dụng Git làm nguồn sự thật duy nhất, mọi thay đổi đều được thực hiện qua pull request.  
- Các công cụ phổ biến như ArgoCD, Flux giúp tự động đồng bộ trạng thái mong muốn từ Git xuống môi trường chạy.  

Cá nhân thấy GitOps giúp tăng tính minh bạch và dễ audit, plan sẽ thử áp dụng cho dự án nhỏ trước khi mở rộng.


## DevOps

Hôm nay tìm hiểu về **Docker multi-stage build**, giúp giảm size image bằng cách chỉ giữ lại các file cần thiết từ các stage build. Ví dụ build Go app trong stage 1, copy binary ra stage 2 và chạy trên Alpine Linux. Tiết kiệm được 60-70% dung lượng so với build truyền thống. Sẽ áp dụng cho dự án microservice sắp tới để tối ưu CI/CD pipeline.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** để quản lý multi-container applications. Compose cho phép định nghĩa services, networks, và volumes trong một file YAML, giúp đơn giản hóa việc chạy và scale ứng dụng. Đặc biệt hữu ích khi cần tái tạo môi trường dev/production một cách nhất quán.

Plan: Sẽ áp dụng Compose vào dự án NodeJS + MongoDB để thay thế cho việc chạy thủ công.


## DevOps

Hôm nay tìm hiểu về Docker Compose - công cụ giúp định nghĩa và chạy multi-container applications. Chỉ cần viết 1 file `docker-compose.yml` là có thể khởi động toàn bộ stack (web, db, cache) bằng 1 lệnh `docker-compose up`. Rất tiện cho việc phát triển local và CI/CD pipeline. Dự định sẽ áp dụng cho dự án sắp tới để đơn giản hóa việc setup môi trường cho team.


## DevOps

Hôm nay tìm hiểu về `kubectl rollout restart deployment <tên-deploy>`, cách restart deployment mà không cần downtime. Giúp cập nhật container mà không ảnh hưởng đến traffic. Sẽ áp dụng cho CI/CD pipeline của team.


## DevOps

Hôm nay tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container applications. Compose cho phép định nghĩa và chạy nhiều containers cùng lúc thông qua file YAML, giúp đơn giản hóa việc triển khai các dịch vụ liên quan như web app + database. Rất tiện lợi cho việc phát triển và testing.

- Dùng `docker-compose up` để khởi động toàn bộ services.
- Có thể scale services bằng `docker-compose scale`.

Plan: Sẽ áp dụng Compose trong dự án cá nhân để thử nghiệm microservices.


## DevOps  

Hôm nay tìm hiểu về cách setup CI/CD pipeline cho project Node.js bằng GitHub Actions. Tìm hiểu được cách tự động test, build và deploy lên Heroku chỉ với vài file YAML. Giúp tiết kiệm thời gian deploy và giảm lỗi manual.  

Plan: Sẽ apply vào dự án cá nhân để tối ưu quy trình release.
