# 2026-03-28

## Programming

Hôm nay tìm hiểu về `asyncio` trong Python, đặc biệt là cách sử dụng `asyncio.gather()` để chạy đồng thời nhiều coroutine. Thấy rằng việc này giúp tăng hiệu năng đáng kể khi xử lý các tác vụ I/O-bound như gọi API hay đọc file. Cần lưu ý: không nên trộn code sync và async một cách bừa bãi để tránh deadlock.

Plan: Áp dụng asyncio vào script crawler dữ liệu để giảm thời gian chạy.


## DevOps

Hôm nay tìm hiểu về **CI/CD pipeline** với GitHub Actions. Tự động build và deploy ứng dụng khi có thay đổi trên nhánh `main`. Cấu hình YAML đơn giản, chỉ cần xác định trigger, jobs và steps. Rất tiện lợi cho dự án nhỏ và vừa.

Cảm thấy GitHub Actions dễ tiếp cận hơn so với Jenkins, nhưng vẫn cần cân nhắc chi phí nếu dự án lớn. Plan sẽ áp dụng cho dự án cá nhân sắp tới.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trên GitHub Actions để tự động deploy ứng dụng lên AWS EC2. Điểm thú vị là có thể dùng SSH key được mã hóa trong secrets để kết nối an toàn với server. Cũng nhớ thiết lập biến môi trường cho database URL để tránh hardcode. Sẽ thử nghiệm thêm trigger theo tag để deploy production riêng biệt.

## Automation

Học được cách dùng Python với Selenium để tự động điền form và lưu dữ liệu từ trang web. Script có thể chạy headless và ghi kết quả vào CSV. Hữu ích cho các báo cáo định kỳ. Cần lưu ý về tốc độ load trang và handle exception.

## Travel Tech

Khám phá ứng dụng Maps.me có thể dùng offline khi trekking ở vùng sâu vùng xa. Tải map về trước và đánh dấu waypoints rất tiện lợi. Pin dự phòng cũng rất quan trọng khi đi lâu ngày. Lần sau sẽ test thêm với Gaia GPS xem có khác biệt gì không.

## Programming

Biết cách dùng Context API trong React để truyền state giữa các component mà không cần prop drilling. Đơn giản hơn Redux cho các ứng dụng nhỏ. Tuy nhiên cần cẩn thận với re-render không cần thiết. Sẽ refactor lại component hiện tại để thử nghiệm.

## Trekking - Hiking - Camping

Địa điểm: Núi Bà Đen, Tây Ninh. Đường lên đỉnh khá dốc nhưng có cabin treo và cáp treo hỗ trợ. View từ trên cao đẹp, đặc biệt là lúc hoàng hôn. Nên mang theo nước uống và đồ ăn nhẹ vì ít quán ven đường. Lần sau muốn thử cắm trại qua đêm để ngắm sao.


## DevOps

Hôm nay tìm hiểu về **GitOps** - mô hình vận hành cơ sở hạ tầng và ứng dụng thông qua Git. Thay vì chỉ dùng Git để lưu code, GitOps dùng Git làm nguồn chân lý duy nhất để quản lý toàn bộ trạng thái của hệ thống. Các thay đổi sẽ được tự động triển khai qua CI/CD pipeline, giúp tăng tính lặp lại và khả năng quan sát.

Tiếp theo sẽ thử áp dụng GitOps vào project nhỏ để làm quen với workflow này.


## DevOps  

Hôm nay tìm hiểu về Docker Compose để quản lý multi-container applications. Compose cho phép định nghĩa và chạy nhiều services trong cùng 1 file YAML, giúp dev và deploy dễ dàng hơn. Đặc biệt hữu ích khi cần test local môi trường giống production.

Kế hoạch: sẽ áp dụng Compose cho dự án web app với backend, database và reverse proxy.


## DevOps
Hôm nay tìm hiểu về **Docker multi-stage build** để giảm kích thước image. Chỉ cần định nghĩa nhiều `FROM` trong Dockerfile, mỗi stage có thể build riêng rồi copy artifact sang stage cuối. Giảm được vài chục MB so với cách build truyền thống. Sẽ áp dụng cho dự án Python sắp tới để tối ưu CI/CD pipeline.

## Automation
Tìm hiểu **GitHub Actions self-hosted runner** để chạy CI trên server riêng. Hữu ích khi cần truy cập mạng nội bộ hoặc tài nguyên cục bộ. Setup khá đơn giản, chỉ cần đăng ký runner và cài agent. Plan: triển khai cho dự án cần build Docker image với base image private.

## Travel Tech
Khám phá **Ba Bể Lake** - hồ nước ngọt lớn nhất Việt Nam ở Bắc Kạn. Có thể đi thuyền tham quan, trekking quanh hồ và thăm bản Pác Ngòi. Mùa thu là thời điểm đẹp nhất vì thời tiết mát mẻ. Plan: cuối tháng 10 sẽ lên kế hoạch 3 ngày 2 đêm khám phá khu vực này.

## Programming
Học cách **debug Python multi-threading** bằng `threading` module và `logging`. Vấn đề phổ biến là race condition, nên dùng `Lock` hoặc `Queue` để đồng bộ. Cũng nên set `logging.basicConfig(level=logging.DEBUG)` để trace thread execution. Sẽ áp dụng cho script xử lý batch data đang gặp lỗi ngẫu nhiên.

## Trekking - Hiking - Camping
Đi **Fansipan** qua cung đường **Trạm Tôn** - khó nhất trong 3 đường lên đỉnh. Cần chuẩn bị thể lực tốt, giày trek chuyên dụng và đồ giữ ấm vì nhiệt độ có thể xuống 10°C. Đoạn từ 2.200m trở lên nhiều sương mù, cần đi theo nhóm có porter dẫn đường. Plan: cuối tháng 11 sẽ booking tour 2 ngày 1 đêm với guide local.


## DevOps

Hôm nay tìm hiểu về Docker multi-stage build để tối ưu Docker image. Kỹ thuật này giúp giảm kích thước image đáng kể bằng cách chỉ giữ lại những file cần thiết ở giai đoạn cuối. Áp dụng cho dự án web app sắp tới để giảm thời gian deploy.

## Automation

TIL về cách dùng GitHub Actions để tự động deploy khi push lên main branch. Cấu hình workflow YAML khá đơn giản, giúp tiết kiệm thời gian deploy thủ công. Sẽ thử áp dụng cho repo cá nhân để quen tay hơn.

## Travel Tech

Biết thêm ứng dụng Maps.me có thể download bản đồ offline, rất hữu ích khi trekking ở vùng núi không có sóng. Giao diện trực quan, có cả đường mòn hiking. Sẽ test thử ở chuyến đi Fansipan sắp tới.

## Programming

Học được cách dùng Context API trong React để quản lý state toàn app thay vì Redux cho các dự án nhỏ. Code ngắn gọn, dễ hiểu hơn. Tuy nhiên vẫn cần cân nhắc khi app lớn để tránh re-render không cần thiết.

## Trekking - Hiking - Camping

Khám phá ra khu cắm trại Pù Luông (Thanh Hóa) - cảnh đẹp, yên tĩnh, thích hợp cho cuối tuần. Có nhiều bungalow và camping site giá rẻ. Dự định đi vào tháng sau, sẽ chuẩn bị thêm đồ giữ ấm vì ban đêm lạnh.


## DevOps
Hôm nay tìm hiểu về Docker Compose để quản lý multi-container application. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file YAML, rất tiện lợi cho việc phát triển và testing. Mình đã thử setup 1 project đơn giản với nginx và mysql, mọi thứ hoạt động mượt mà.

## Automation
Học được cách dùng GitHub Actions để tự động deploy code lên server khi có commit mới. Việc này giúp tiết kiệm thời gian và giảm thiểu sai sót do thao tác thủ công. Mình sẽ áp dụng cho các project cá nhân để tối ưu workflow.

## Travel Tech
Khám phá ứng dụng Maps.me để offline navigation khi trekking ở các vùng núi cao. Ứng dụng này cho phép tải bản đồ về điện thoại và sử dụng GPS mà không cần internet, rất hữu ích cho các chuyến đi xa. Mình sẽ thử nghiệm trong chuyến trekking Fansipan sắp tới.

## Programming
Tìm hiểu về async/await trong JavaScript để xử lý các tác vụ bất đồng bộ hiệu quả hơn. Việc sử dụng async/await giúp code dễ đọc và dễ maintain hơn so với callback hoặc promise chain. Mình sẽ refactor lại 1 số function trong project để áp dụng.

## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)
Đọc về kinh nghiệm trekking ở Pù Luông (Thanh Hóa), một địnhh điểm đẹp với nhiều thửa ruộng bậc thang và bản làng dân tộc Thái. Thời điểm lý tưởng để trekking là từ tháng 9-11 khi lúa chín vàng hoặc tháng 2-4 khi lúa mới cấy xanh mướt. Mình muốn lên kế hoạch cho 1 chuyến đi cuối năm nay.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** - công cụ giúp định nghĩa và chạy multi-container applications. Thay vì phải chạy từng container riêng lẻ, chỉ cần một file `docker-compose.yml` là có thể khởi động cả hệ thống. Rất tiện cho việc phát triển local dev environment.

- Docker Compose giúp đồng bộ version và config giữa các service.
- Có thể scale service dễ dàng với `docker-compose up --scale`.

Plan: Sẽ áp dụng Docker Compose vào dự án nhỏ để rút ngắn thời gian setup cho team.
