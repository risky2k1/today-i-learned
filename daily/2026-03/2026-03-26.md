# 2026-03-26

## DevOps

Hôm nay tìm hiểu về **Docker Compose** - cách định nghĩa và chạy multi-container applications. Compose sử dụng YAML file để cấu hình services, networks và volumes, giúp đơn giản hóa việc quản lý các containers liên quan. Ví dụ: `docker-compose up -d` sẽ start toàn bộ stack chỉ với một lệnh.

Điểm hay là có thể version control file compose, giúp team dễ chia sẻ môi trường dev giống nhau. Plan: sẽ áp dụng cho dự án NodeJS + MongoDB đang làm.


## DevOps

Hôm nay tìm hiểu về **AWS Lambda với Node.js** để xây dựng microservices. Lambda cho phép chạy code mà không cần quản lý server, giúp giảm chi phí và tăng tính linh hoạt. Tuy nhiên, cần chú ý đến cold start time khi mới khởi động function.

Plan: Thử deploy 1 API nhỏ dùng Lambda + API Gateway để test performance và cost.


## Programming

Hôm nay tìm hiểu về **CSS custom properties** (variables) và cách dùng chúng để xây dựng theme system. Bằng cách khai báo `--primary-color: #3498db;` trong `:root`, ta có thể thay đổi màu sắc toàn app chỉ bằng cách đổi giá trị CSS variable, không cần touch JS.

- Dùng `var(--primary-color)` trong các thuộc tính như `color`, `background-color`.
- Hỗ trợ override ở cấp độ selector, rất tiện cho dark/light mode.
- Tương thích tốt trên các trình duyệt hiện đại.

Plan: Áp dụng cho dự án web dashboard để dễ maintain theme và tăng trải nghiệm người dùng.


## Programming

Hôm nay tìm hiểu về **WebAssembly (WASM)** và cách nó giúp cải thiện performance cho web app. WASM cho phép code viết bằng C/C++/Rust được biên dịch và chạy trực tiếp trên browser, gần như native speed.

- Lý tưởng cho các ứng dụng cần xử lý nặng như game, video editing, hoặc data visualization.
- Có thể kết hợp với JS để tận dụng cả 2 thế mạnh.

Plan: sẽ thử build 1 demo nhỏ với Rust + WASM để xem hiệu năng thực tế so với JS thuần.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** - công cụ giúp định nghĩa và chạy multi-container applications. Docker Compose sử dụng file YAML để cấu hình services, networks và volumes. Một lệnh `docker-compose up` có thể khởi động toàn bộ hệ thống một cách nhất quán. Rất tiện lợi cho việc phát triển và testing local.

Plan: Sẽ áp dụng Docker Compose cho dự án web app sắp tới để đơn giản hóa môi trường development.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline với GitHub Actions để tự động deploy ứng dụng lên AWS Elastic Beanstalk. Điểm thú vị là có thể dùng secrets để lưu key AWS, giúp bảo mật thông tin nhạy cảm.

- Tạo workflow file `.github/workflows/deploy.yml`.
- Cấu hình deploy stage với `aws-actions/configure-aws-credentials`.
- Chạy `eb deploy` để tự động cập nhật ứng dụng.

Tiếp theo sẽ thử nghiệm với Docker để build image trước khi deploy.


## DevOps  

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline cho dự án Node.js bằng GitHub Actions. Đơn giản là chỉ cần tạo file `.github/workflows/main.yml` với các step build, test và deploy. Điểm hay là có thể chạy tự động mỗi khi push code, giúp tiết kiệm thời gian deploy thủ công.  

- Build project với `npm ci && npm run build`  
- Chạy test với `npm test`  
- Deploy lên VPS qua SSH nếu test pass  

Plan: Tích hợp thêm step check code quality bằng ESLint để đảm bảo code sạch trước khi deploy.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** - công cụ giúp định nghĩa và chạy multi-container application. Sử dụng file `docker-compose.yml` để mô tả services, network, volumes. Rất tiện khi muốn deploy local development environment cho cả team.

## Programming

Học về **Python type hints** và cách dùng `mypy` để check type statically. Giúp code dễ đọc hơn và catch lỗi sớm trong quá trình development. Sẽ áp dụng vào project mới để cải thiện maintainability.

## Travel Tech

Khám phá ứng dụng **Maps.me** - bản đồ offline với đầy đủ thông tin hiking trails ở Việt Nam. Rất hữu ích khi trekking ở những nơi không có internet. Sẽ thử dùng cho chuyến Ba Vì sắp tới.
