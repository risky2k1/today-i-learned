# 2026-08-08

## Tự động hóa

- Mình vừa tìm hiểu cách sử dụng GitHub Actions để triển khai ứng dụng Node.js lên AWS Lambda thông qua serverless framework.  
- Quy trình này giúp giảm thời gian cấu hình server xuống gần như bằng không, đồng thời tích hợp kiểm tra lint và unit test tự động mỗi khi push code.  
- Kế hoạch sắp tới là mở rộng pipeline này cho các dịch vụ microservices khác trong dự án.


## DevOps

Hôm nay mình học cách dùng Health Check trong Docker để tự động khởi động lại container khi service lỗi. Bằng cách thêm `HEALTHCHECK` vào Dockerfile, Docker sẽ đợi service sẵn sàng trước khi route traffic.

- Dùng CMD hoặc CURL để gọi endpoint `/health` hoặc kiểm tra process đang chạy
- Kết hợp với `--restart unless-stopped` để container tự recovery khi lỗi

Mình sẽ áp dụng công nghệ này vào dự án Node.js đang phát triển để tăng độ ổn định.


## Travel Tech

- Hôm nay mình đã thử nghiệm việc sử dụng Google Maps offline bằng cách tải bản đồ của Đà Lạt trước khi đi, và đã tiết kiệm được khoảng 30 % chi phí data khi di chuyển.  
- Nhận xét: Việc chuẩn bị bản đồ offline thực sự giúp giảm thiểu việc lạc đề khi sinal yếu hoặc không có internet.

Sẽ thử nghiệm thêm với bản đồ của Sapa vào cuối tuần tới.


## Travel Tech

- Hôm nay mình tìm hiểu về cách sử dụng API của Vietnam Railways để lấy lịch trình tàu real-time và tích hợp vào ứng dụng di động.  
- API cung cấp dữ liệu JSON với trường `status`, `departure_time` và `platform`, cho phép xây dựng tính năng thông báo khi tàu trễ.  
- Mình đã viết một script Python nhỏ dùng `requests` và `schedule` để poll mỗi 5 phút và push thông báo qua Telegram Bot.  

Cá nhân thấy rất tiện lợi, định áp dụng vào dự án du lịch nội륙 tới năm sau để giúp khách du lịch tự động cập nhật thông tin chuyển động.


## Trekking - Hiking - Camping

- Hôm nay tôi khám phá được một đường mòn mới trên Fansipan, độ cao 2.500 m, với không khí trong lành và cảnh quan hùng vĩ.  
- Đây là một địa điểm ít người biết, rất lý tưởng cho những buổi trekking ngắn vào cuối tuần.  

Plan: sẽ lên kế hoạch cho một ngày trekking vào cuối tuần tới, chuẩn bị túi rюк và camera.


## DevOps

Hôm nay mình tìm hiểu về **Docker volumes** – cơ chế lưu trữ dữ liệu bền vững cho container. Khác với việc dùng bind mounts (gán thẳng thư mục host), volumes được quản lý bởi Docker nên dễ sao chép, di chuyển giữa các máy và an toàn hơn với quyền truy cập.

- Tạo volume: `docker volume create mydata`
- Gán vào container: `docker run -v mydata:/app/data ...`
- Liệt kê & kiểm tra: `docker volume ls`, `docker volume inspect mydata`

👉 Áp dụng ngay trong dự án Laravel đang xây dựng: mình sẽ dùng volumes để lưu trữ database và file upload, tránh tình trạng mất dữ liệu khi rebuild container.
