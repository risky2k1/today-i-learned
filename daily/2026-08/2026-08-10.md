# 2026-08-10

## Automation  
- Hôm nay mình tự động tạo pipeline **GitHub Actions** để chạy test sau mỗi Pull Request, không còn phải gõ lệnh thủ công.  
- Thêm một step để backup database qua cron mỗi đêm, giúp tôi yên tâm hơn về dữ liệu.  
- Kế hoạch: tích hợp workflow này vào dự án mới và chia sẻ với team để tối ưu quy trình CI/CD.


## DevOps  
Hôm nay mình học cách dùng `docker-compose` để quản lý các dịch vụ đa container một cách hiệu quả. Thay vì chạy từng lệnh `docker run` phức tạp, chỉ cần một file `docker-compose.yml` có thể định nghĩa toàn bộ môi trường (database, backend, frontend) và khởi chạy bằng lệnh `docker-compose up`.  

- **Lợi ích chính**: Dễ dàng tái tạo môi trường phát triển trên bất kỳ máy nào chỉ với một lệnh.  
- **Công thức**: `docker-compose up -d` để chạy nền, `docker-compose down` để dừng và xóa.  

Nhận xét cá nhân: Cảm thấy đây là bước tiến lớn để tránh tình trạng "chạy được ở đây nhưng sao ở kia chạy không được". Plan: Áp dụng vào project cá nhân mình đang build để tối ưu quy trình CI/CD.
