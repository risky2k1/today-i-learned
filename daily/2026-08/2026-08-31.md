# 2026-08-31



## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)
*   Hồi sáng tìm hiểu về hành trình trekking Ta Nung (Đà Lạt), nhận ra việc download trước bản đồ offline và mang theo power bank là cực kỳ cần thiết vì signal di động ở các passes remote thường rất yếu.
*   Những cung đường ở Việt Nam rất đẹp nhưng cũng đầy thách thức về địa hình, cần có sự chuẩn bị kỹ về cả đồ dùng cá nhân và công cụ hỗ trợ định vị.

*   *Kế hoạch áp dụng:* Sắp tới đây mình sẽ đi một chặng camping ngắn, sẽ cài sẵn app bản đồ offline và kiểm tra kỹ lượng pin trước khi khởi hành để đảm bảo an toàn tuyệt đối.


## Programming

TIL về cách sử dụng `console.table()` trong JavaScript khi debug - nó hiển thị data dạng bảng, dễ nhìn hơn console.log rất nhiều, đặc biệt khi làm việc với array of objects. Cái này mình dùng hoài mà giờ mới note lại. Plan tuần này: thay thế console.log bằng console.table trong các project để debug nhanh hơn.


User Safety: safe


User Safety: safe


## DevOps

Hôm nay mình biết được `docker compose down -v` không chỉ stop containers mà còn **xóa luôn cả named volumes và networks** — khác với `docker compose down` thông thường chỉ dừng và remove containers. Cú pháp đầy đủ có thể là `docker compose down --rmi all` để xóa thêm cả images. Lần sau khi test local mà muốn reset sạch database thì sẽ dùng luôn combo này thay vì rm thủ công từng thứ.


## Travel Tech

Mình vừa khám phá công cụ AI để lên lịch trình du lịch tại Việt Nam, giúp tối ưu hóa việc kết hợp các trải nghiệm văn hóa và ẩm thực theo sở thích. Công cụ này tích hợp dữ liệu thời tiết, giá vé và khuyến mãi từ nhiều nền tảng, giúp tôi sắp xếp chuyến đi nhanh chóng hơn rất nhiều.  
- Tích hợp Google Maps với bản đồ du lịch nội địa để tìm đường ngắn nhất giữa các điểm tham quan.  
- Sử dụng chatbot để đặt phòng khách sạn và ăn uống ngay từ khi có kế hoạch.  

Mình sẽ thử áp dụng công cụ này cho chuyến đi đến Sapa vào tháng tới, để không bỏ lỡ bất kỳ trải nghiệm nào.


## Trekking - Hiking - Camping
- Hôm nay đã thử thách mình lên đèo Sapa, cảm nhận không khí mát mẻ và những terraced field xanh mướt.  
- Đã học cách đặt lều nhanh gọn bằng cách dùng các dây cáp và mũi nhọn, giảm thời gian setup từ 15 phút xuống còn 5 phút.  

*Plan:* Bring a compact camera on the next trek to capture the sunrise over the rice terraces.




## Programming

Hôm nay tôi học được rằng tính năng `match-case` (pattern matching) trong Python 3.10+ giúp viết các điều kiện phức tạp trở nên cực kỳ sạch sẽ, dễ đọc và gần giống với cấu trúc `switch-case` của các ngôn ngữ khác, nhưng còn mạnh mẽ hơn nữa khi hỗ trợ匹配 các cấu trúc dữ liệu phức tạp.

* Giúp code logic trở nên ngắn gọn, trực quan hơn nhiều so với chuỗi `if-elif` truyền thống.
* Hỗ trợ pattern matching cực kỳ mạnh mẽ cho các dictionary, list, và các object phức tạp.

**Kế hoạch áp dụng:** Tôi sẽ refactor lại đoạn logic xử lý trạng thái phức tạp trong dự án hiện tại bằng `match-case` để code dễ bảo trì và thân thiện với đồng nghiệp hơn.


## Automation

Hôm nay mình tìm hiểu và setting xong framework **pre-commit** cho repo cá nhân — tự động check format (ruff, prettier) và lint trước mỗi commit luôn, ko còn phải nhắc bản thân thủ công nữa.

- Cài chỉ bằng `pip install pre-commit` rồi tạo file `.pre-commit-config.yaml` ở root repo.
- Các hook chạy nhanh trong vài giây, nếu fail thì commit sẽ bị chặn lại luôn.

Mình định áp dụng luôn cho toàn bộ project làm việc nhóm để giảm thiểu review loop vô nghĩa về formatting — code style nhất quán từ ngay commit đầu tiên 😄
