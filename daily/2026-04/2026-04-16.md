# 2026-04-16

## DevOps  
Hôm nay tìm hiểu về **AWS CodePipeline** và **CodeDeploy** để tự động hóa CI/CD cho dự án. Thấy rằng việc kết hợp 2 service này giúp giảm thiểu sai sót khi deploy và tăng tốc quá trình phát hành.  

- CodePipeline quản lý workflow từ source code đến production.  
- CodeDeploy xử lý việc deploy lên EC2 hoặc Lambda một cách an toàn.  

Plan: Sẽ setup demo pipeline cho dự án nhỏ để test trước khi áp dụng vào production.


## DevOps

Hôm nay tìm hiểu về `kubectl exec` với option `-it` để truy cập trực tiếp vào container đang chạy trong Kubernetes. Cách này rất hữu ích khi cần debug hoặc kiểm tra log trực tiếp mà không cần vào pod. Kết hợp với `kubectl logs -f` để theo dõi log real-time.

Plan: Lần sau sẽ thử kết hợp với `kubectl cp` để copy file từ container ra ngoài để debug.


## Programming  

Hôm nay tìm hiểu về cách sử dụng `asyncio.run()` trong Python để chạy một coroutine đơn giản. Nếu cần chạy nhiều task song song, có thể dùng `asyncio.gather()` để gom chúng lại và chờ tất cả hoàn thành. Điều này giúp code async trở nên gọn gàng hơn so với việc tự quản lý event loop. Plan: sẽ áp dụng vào script crawler để tăng tốc độ thu thập dữ liệu.


## DevOps  
Hôm nay tìm hiểu về **AWS Lambda Layers** để tách riêng dependencies chung cho nhiều Lambda function, giúp giảm kích thước deployment package và dễ quản lý hơn. Mình đã thử tạo một layer cho thư viện `requests` và attach vào function Python, thấy deploy nhanh hơn hẳn. Plan sắp tới sẽ áp dụng cho các project có nhiều function dùng chung SDK AWS.

---

## Travel Tech  
Khám phá ứng dụng **Google Maps Offline** khi đi trekking ở Fansipan: tải map trước, đánh dấu waypoints và tracking GPS offline vẫn chính xác. Rất hữu ích ở vùng sóng yếu, giúp mình tự tin hơn khi không có internet. Plan là sẽ test thêm Gaia GPS cho những chuyến dài ngày.

---

## Programming  
Học được cách dùng **Python `pathlib`** thay vì `os.path` để xử lý file path cross-platform dễ đọc hơn: `Path.home() / 'data' / 'file.txt'`. Code ngắn gọn, không cần lo lắng về dấu `\` hay `/`. Plan: refactor dần các script cũ để dùng pathlib.

---

## Trekking - Hiking - Camping  
Điểm đến: **Đỉnh Bà Nà (Đà Nẵng)** - cung trekking khá dốc nhưng view đẹp, có thể kết hợp cáp treo về. Nên mang theo áo khoác vì nhiệt độ chênh lệch lớn giữa sáng và trưa. Plan: cuối tháng sẽ thử cắm trại qua đêm ở đây để săn mây.
