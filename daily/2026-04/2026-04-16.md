# 2026-04-16

## DevOps  
Hôm nay tìm hiểu về **AWS CodePipeline** và **CodeDeploy** để tự động hóa CI/CD cho dự án. Thấy rằng việc kết hợp 2 service này giúp giảm thiểu sai sót khi deploy và tăng tốc quá trình phát hành.  

- CodePipeline quản lý workflow từ source code đến production.  
- CodeDeploy xử lý việc deploy lên EC2 hoặc Lambda một cách an toàn.  

Plan: Sẽ setup demo pipeline cho dự án nhỏ để test trước khi áp dụng vào production.


## DevOps

Hôm nay tìm hiểu về `kubectl exec` với option `-it` để truy cập trực tiếp vào container đang chạy trong Kubernetes. Cách này rất hữu ích khi cần debug hoặc kiểm tra log trực tiếp mà không cần vào pod. Kết hợp với `kubectl logs -f` để theo dõi log real-time.

Plan: Lần sau sẽ thử kết hợp với `kubectl cp` để copy file từ container ra ngoài để debug.


## Programming  

Hôm nay tìm hiểu về cách sử dụng `asyncio.run()` trong Python để chạy một coroutine đơn giản. Nếu cần chạy nhiều task song song, có thể dùng `asyncio.gather()` để gom chúng lại và chờ tất cả hoàn thành. Điều này giúp code async trở nên gọn gàng hơn so với việc tự quản lý event loop. Plan: sẽ áp dụng vào script crawler để tăng tốc độ thu thập dữ liệu.
