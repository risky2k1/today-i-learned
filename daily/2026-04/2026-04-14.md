# 2026-04-14

## DevOps

Hôm nay tìm hiểu về Docker Compose - công cụ giúp định nghĩa và chạy multi-container Docker applications. Compose sử dụng file YAML để cấu hình services, networks và volumes, giúp triển khai môi trường development nhanh chóng và nhất quán giữa các máy.

- `docker-compose up` để start services, `docker-compose down` để stop và remove.
- Rất hữu ích cho việc setup local development với full-stack app (ví dụ: web app + database + cache).

Plan: Áp dụng Compose để standardize môi trường dev cho team, giảm thiểu "works on my machine" issues.


## DevOps

Hôm nay tìm hiểu về cách dùng Docker Compose để quản lý multi-container application. Compose giúp định nghĩa services, networks và volumes trong 1 file YAML, rất tiện cho việc deploy nhanh và đồng bộ môi trường dev/production.

Kế hoạch: sẽ thử setup 1 stack gồm web app + database bằng Compose để test tính năng scaling.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trên GitHub Actions để tự động deploy React app lên Vercel. Đặc biệt, cần set biến môi trường `VERCEL_TOKEN` trong secrets để tránh hardcode. Thấy GitHub Actions khá linh hoạt, có thể kết hợp với Docker để build multi-stage, giảm kích thước image. Plan sẽ áp dụng cho dự án cá nhân để rút ngắn thời gian deploy.


## Programming

Hôm nay tìm hiểu về **WebAssembly (WASM)** và cách nó giúp JavaScript chạy nhanh hơn. WASM cho phép code viết bằng các ngôn ngữ compiled (C/C++, Rust) chạy trực tiếp trong browser, giảm tải cho JS engine. Đặc biệt hữu ích cho các ứng dụng nặng về tính toán như games, image processing.

```javascript
// Ví dụ nhỏ: import một module WASM từ JS
import init, { add } from './calculator.js';

async function run() {
  await init();
  console.log(add(2, 3)); // 5
}
```

Sẽ thử build một demo nhỏ với Rust → WASM để cảm nhận performance khác biệt so với pure JS.


## DevOps  
Hôm nay tìm hiểu về **GitOps** - một phương pháp quản lý hạ tầng và triển khai ứng dụng thông qua Git. Khác với CI/CD truyền thống, GitOps đặt Git repository làm single source of truth, mọi thay đổi đều được kiểm soát qua pull request và tự động đồng bộ với môi trường production.  

- GitOps giúp tăng tính minh bạch, dễ audit và rollback.
- Công cụ phổ biến: ArgoCD, Flux CD.
- Kế hoạch: Áp dụng thử nghiệm trên dự án nhỏ trước khi mở rộng.  

> GitOps có vẻ hợp với team đang dùng Kubernetes, sẽ thử nghiệm trong sprint tới.
