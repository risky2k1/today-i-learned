# 2026-04-01

## DevOps

Hôm nay tìm hiểu về cách setup CI/CD pipeline cho một ứng dụng Node.js sử dụng GitHub Actions. Cấu hình bao gồm các bước build, test và deploy tự động khi push code vào branch main. Điểm thú vị là có thể tích hợp Docker để đảm bảo môi trường build và production giống nhau.

Sẽ thử áp dụng cho dự án cá nhân để làm quen với workflow này và tối ưu thời gian deploy.


## DevOps

Hôm nay tìm hiểu về **Docker Compose với multiple environments**. Có thể dùng `.env` file và `docker-compose.override.yml` để tách biệt config dev/prod. Điều này giúp tránh hardcode biến môi trường vào file chính.

- `.env` chứa các biến dùng chung.
- `docker-compose.override.yml` ghi đè cho dev.
- Dùng `docker-compose -f docker-compose.yml -f docker-compose.prod.yml up` để chạy production.

Cảm thấy cách này sạch và dễ maintain hơn việc clone file rồi sửa tay. Sắp tới sẽ áp dụng cho dự án cá nhân để test CI/CD.


## Programming  

Hôm nay tìm hiểu về `git rebase -i` để chỉnh sửa commit history trước khi push. Có thể dùng `pick`, `squash`, `fixup` để gộp hoặc sửa message commit một cách linh hoạt.  

- `squash` giữ message của commit bị gộp, `fixup` thì bỏ qua.  
- Nên rebase trước khi push để tránh conflict với remote.  

Plan: Sẽ áp dụng khi cần clean history cho PR, tránh các commit "fix typo" lộn xộn.


## DevOps

Hôm nay tìm hiểu về cách cấu hình Docker Compose với multiple stage build để tối ưu Docker image. Bằng cách tách riêng build stage và runtime stage, có thể giảm đáng kể kích thước image. Cũng học được cách dùng `.dockerignore` để loại bỏ các file không cần thiết khỏi context. Plan: sẽ áp dụng cho project sắp tới để giảm thời gian deploy.


## DevOps

Hôm nay tìm hiểu về `kubectl debug`, một tính năng mới giúp debug pod mà không cần thay đổi deployment. Có thể tạo debug container trong pod đang chạy để kiểm tra trạng thái ứng dụng. Rất hữu ích cho troubleshooting production mà không gây downtime. Plan: sẽ áp dụng cho team khi có incident cần debug nhanh.


## DevOps

Hôm nay tìm hiểu về cách cấu hình Docker Compose để chạy multi-stage build cho một ứng dụng Node.js. Kết hợp với .dockerignore để giảm kích thước image và tối ưu tốc độ build. Đây là cách hiệu quả để deploy CI/CD pipeline nhanh hơn. Plan: sẽ áp dụng cho dự án sắp tới để giảm thời gian deploy.


## DevOps

Hôm nay tìm hiểu về **GitOps** - một phương pháp quản lý hạ tầng và triển khai ứng dụng thông qua Git làm single source of truth. Thay vì dùng CI/CD truyền thống, GitOps tự động đồng bộ trạng thái mong muốn từ Git repository xuống môi trường runtime. Lợi ích lớn nhất là khả năng quan sát, audit và rollback dễ dàng. Plan: sẽ thử nghiệm trên dự án nhỏ với ArgoCD để đánh giá hiệu quả.


## DevOps

Hôm nay mình tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container applications. Compose giúp định nghĩa và chạy đồng thời nhiều services trong một file YAML, rất tiện cho việc phát triển và testing local. Mình sẽ thử áp dụng cho dự án web app của team để đơn giản hóa quá trình setup môi trường cho các thành viên mới.

## Programming

Mình vừa biết về khái niệm "lazy loading" trong JavaScript, giúp cải thiện performance bằng cách chỉ load resources khi cần thiết. Đặc biệt hữu ích cho các trang web có nhiều images hoặc components. Sẽ thử implement cho trang sản phẩm của công ty để giảm thời gian load ban đầu.

## Travel Tech

Ở Việt Nam, ứng dụng đặt vé xe khách như Vexere hay Vexere đang rất phổ biến, giúp người dùng dễ dàng so sánh giá và lựa chọn chuyến đi phù hợp. Mình thấy giao diện của Vexere khá trực quan, có thể đề xuất cho bạn bè khi cần di chuyển giữa các tỉnh.


## DevOps

Hôm nay mình tìm hiểu về việc sử dụng Docker Compose để quản lý multi-container applications. Docker Compose giúp định nghĩa và chạy nhiều containers cùng lúc thông qua một file YAML, rất tiện lợi cho việc phát triển và testing. Mình đã thử setup một ứng dụng Node.js với database PostgreSQL và thấy việc cấu hình khá đơn giản. Plan tiếp theo là sẽ tìm hiểu cách tích hợp Docker Compose với CI/CD pipeline để tự động hóa việc deploy.

## Automation

Mình vừa học được cách sử dụng GitHub Actions để tự động chạy test mỗi khi push code lên repository. GitHub Actions cho phép tạo workflows dựa trên YAML file, giúp tự động hóa các tác vụ như test, build, deploy. Mình đã setup một workflow đơn giản để chạy test cho project Node.js của mình. Việc này giúp phát hiện lỗi sớm và đảm bảo code quality. Plan tiếp theo là sẽ tìm hiểu cách tích hợp GitHub Actions với Docker để tự động build và push image lên registry.

## Travel Tech

Hôm nay mình tìm hiểu về ứng dụng GrabBike ở Việt Nam. GrabBike là một dịch vụ gọi xe máy công nghệ, giúp người dùng dễ dàng di chuyển trong thành phố. Ứng dụng này sử dụng GPS để xác định vị trí và kết nối người dùng với các tài xế gần nhất. Mình thấy đây là một giải pháp tiện lợi và an toàn cho việc đi lại ở các thành phố lớn. Plan tiếp theo là sẽ thử trải nghiệm GrabBike ở Hà Nội để đánh giá chất lượng dịch vụ.

## Programming

Mình vừa học được về khái niệm SOLID principles trong lập trình hướng đối tượng. SOLID là viết tắt của 5 nguyên tắc: Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, và Dependency Inversion. Các nguyên tắc này giúp thiết kế code dễ maintain và mở rộng hơn. Mình đã áp dụng Single Responsibility Principle vào project hiện tại và thấy code trở nên rõ ràng hơn. Plan tiếp theo là sẽ tìm hiểu thêm về Open/Closed Principle và áp dụng vào các module khác.

## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)

Hôm nay mình tìm hiểu về trekking ở Fansipan, đỉnh núi cao nhất Việt Nam. Fansipan có độ cao 3.143m và là một trong những điểm trekking hấp dẫn nhất ở Việt Nam. Đường trek khá khó khăn với nhiều đoạn dốc và đá trơn, nhưng cảnh quan thiên nhiên rất đẹp và đáng giá. Mình đã chuẩn bị sẵn đồ dùng cần thiết như giày trekking, balo, và lều ngủ. Plan tiếp theo là sẽ lên kế hoạch chi tiết cho chuyến trek Fansipan vào mùa thu này.


## DevOps  
Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline với GitHub Actions cho một dự án Node.js. Sử dụng `on: [push]` để tự động trigger và `jobs` để định nghĩa các bước build, test, deploy. Cũng học thêm cách caching `node_modules` để tăng tốc độ pipeline.  

Cảm thấy GitHub Actions khá trực quan và dễ setup so với Jenkins. Plan sẽ apply vào dự án hiện tại để rút ngắn thời gian release.
