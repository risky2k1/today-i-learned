# 2026-04-11

## DevOps

Hôm nay tìm hiểu về việc sử dụng Docker Compose để quản lý multi-container applications. Compose giúp định nghĩa services, networks, và volumes trong một file YAML duy nhất, rất tiện cho việc triển khai local development hoặc staging environments. Đặc biệt hữu ích khi muốn chạy đồng thời database, backend, và frontend trong cùng một project.

Plan: Áp dụng Compose cho project cá nhân để rút ngắn thời gian setup môi trường cho team.


## DevOps

Hôm nay tìm hiểu về cách cấu hình Docker Compose để quản lý multi-stage builds cho CI/CD pipeline. Việc tách biệt các stage build giúp giảm kích thước image cuối cùng và tăng tốc độ deploy. Mình sẽ thử áp dụng cho project hiện tại để tối ưu hơn.


## Programming

Hôm nay tìm hiểu về `git worktree` - một tính năng cho phép tạo thêm các nhánh làm việc độc lập mà không ảnh hưởng đến repository chính. Rất hữu ích khi cần debug song song nhiều phiên bản hoặc chuẩn bị release mà vẫn tiếp tục phát triển tính năng mới.

- Lệnh tạo: `git worktree add ../branch-name branch-name`
- Có thể xóa bằng: `git worktree remove ../branch-name`

Cá nhân thấy đây là một cách làm thông minh thay vì phải clone nhiều repo, giúp tiết kiệm disk và quản lý code gọn gàng hơn. Plan sẽ áp dụng cho dự án sắp tới có nhiều phiên bản song song.


## DevOps

Hôm nay tìm hiểu về cách cấu hình `kubectl` để quản lý multiple clusters. Có thể dùng `kubectl config use-context` để chuyển đổi giữa các clusters một cách nhanh chóng, hoặc dùng `kubectx` để tiện hơn. Ngoài ra, có thể thiết lập alias trong `.bashrc` hoặc `.zshrc` để gõ lệnh ngắn gọn hơn. Sẽ thử áp dụng cho dự án kế tiếp để giảm thời gian chuyển đổi môi trường.


## Programming  

Hôm nay tìm hiểu về `asyncio` trong Python để tối ưu hóa việc gọi API song song. Thay vì dùng `requests` với vòng lặp thông thường, chuyển sang `httpx.AsyncClient` giúp giảm thời gian chạy từ ~12s xuống còn ~2s cho 100 requests.  

- Cần chú ý `await` đúng chỗ và giới hạn concurrency bằng `Semaphore` để tránh quá tải server.  
- Plan: áp dụng cho script crawler dữ liệu hàng ngày.


## Programming

Hôm nay mình tìm hiểu về **Tailwind CSS** và cách sử dụng utility-first class để xây dựng UI nhanh hơn. Tailwind cho phép customize mọi thứ thông qua config, giúp kiểm soát design system tốt hơn so với các framework truyền thống.

- Các utility class như `flex`, `justify-center`, `bg-blue-500` rất trực quan và dễ nhớ.
- Không cần viết CSS riêng cho các component đơn giản, tiết kiệm thời gian đáng kể.

Mình sẽ thử áp dụng Tailwind cho project cá nhân tiếp theo để làm quen sâu hơn với cách tổ chức class và config.


## Programming

Hôm nay mình tìm hiểu về cách sử dụng `git rebase` để làm sạch lịch sử commit. Thay vì merge, rebase sẽ đưa branch của mình lên trên branch gốc, giúp lịch sử commit trông gọn gàng hơn. Mình cũng thử dùng `git rebase -i` để chỉnh sửa, squash các commit trước khi push. Cảm giác như code base sẽ dễ review hơn nhiều nếu áp dụng thường xuyên.

Plan: Sẽ áp dụng rebase cho các branch feature nhỏ trước khi tạo pull request.


## DevOps

Hôm nay tìm hiểu về `kubectl debug` - một feature mới trong Kubernetes giúp debug pod mà không cần thay đổi deployment. Có thể attach interactive debug container vào pod đang chạy, rất tiện cho troubleshooting production. Feature này vẫn cần enable flag `EphemeralContainers` trong cluster. Plan sẽ thử nghiệm trong môi trường staging để đánh giá hiệu quả so với cách debug truyền thống.


## DevOps
Hôm nay tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container application. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file `docker-compose.yml`, rất tiện lợi cho việc phát triển và testing. 

- Lợi ích lớn nhất là có thể khởi động toàn bộ stack (app, database, cache) chỉ với một lệnh `docker-compose up`.
- Có thể scale service dễ dàng bằng `docker-compose up --scale`.

Plan: Áp dụng Docker Compose vào dự án cá nhân để tối ưu hóa workflow.


## DevOps

Hôm nay mình tìm hiểu về Docker Compose và cách cấu hình multi-stage build để tối ưu kích thước image. Việc tách riêng build-time và runtime environment giúp giảm đáng kể dung lượng, từ đó tăng tốc độ deploy. Mình sẽ áp dụng ngay vào project hiện tại để test performance.
