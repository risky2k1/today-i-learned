# 2026-04-02

## DevOps

Hôm nay tìm hiểu về việc sử dụng Docker Compose để quản lý multi-container applications. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file `docker-compose.yml`, rất tiện lợi cho việc phát triển và test môi trường. Mình dự định sẽ áp dụng vào dự án cá nhân để đơn giản hóa việc setup dev environment.


## DevOps

Hôm nay mình tìm hiểu về `kubectl exec` và cách chạy command trực tiếp vào container trong pod. Lệnh `kubectl exec -it <pod-name> -- <command>` rất hữu ích khi debug hoặc kiểm tra logs trong môi trường Kubernetes. Lưu ý cần thêm `--` để phân biệt giữa các flag của kubectl và command bên trong container.

Plan: Sẽ áp dụng ngay khi cần debug ứng dụng đang chạy trên K8s cluster.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** và cách cấu hình multi-stage builds để tối ưu hóa kích thước image. Multi-stage builds giúp giảm đáng kể dung lượng ảnh Docker bằng cách chỉ giữ lại những file cần thiết ở stage cuối cùng.

- Sử dụng `FROM` nhiều lần trong Dockerfile, mỗi `FROM` là một stage.
- Copy artifact từ stage build sang stage runtime để giảm thiểu dependency không cần thiết.

Plan: Áp dụng vào dự án hiện tại để giảm thời gian pull image và tối ưu CI/CD pipeline.


## DevOps

Hôm nay tìm hiểu về `docker-compose` để quản lý multi-container application. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file YAML, rất tiện lợi cho việc phát triển và testing môi trường. Plan: sẽ áp dụng vào dự án hiện tại để đơn giản hóa việc setup development environment.


## DevOps

Hôm nay tìm hiểu về việc quản lý secrets trong CI/CD pipeline. Thay vì commit thẳng vào code, nên sử dụng GitHub Secrets hoặc các tools như HashiCorp Vault để bảo mật thông tin nhạy cảm. Đồng thời, nên giới hạn quyền truy cập theo nguyên tắc "least privilege" để giảm thiểu rủi ro.

Plan: Áp dụng ngay cho dự án hiện tại, bắt đầu bằng việc di chuyển các API keys ra khỏi source code và cấu hình trong GitHub Secrets.


## DevOps

Hôm nay tìm hiểu về **GitOps** và cách nó giúp quản lý infrastructure dưới dạng code một cách nhất quán. GitOps sử dụng Git làm single source of truth, mọi thay đổi đều được review và track thông qua pull request, giúp tăng tính bảo mật và khả năng recover.

- GitOps phù hợp với Kubernetes và các nền tảng cloud-native.
- Các công cụ phổ biến: ArgoCD, Flux CD.
- Giảm thiểu human error khi deploy.

Plan: Sẽ thử setup ArgoCD cho project hiện tại để tự động đồng bộ config với cluster.
