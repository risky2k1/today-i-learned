# 2026-04-21

## DevOps

Hôm nay mình tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container applications. Bằng cách định nghĩa services, networks và volumes trong file `docker-compose.yml`, việc triển khai và scale app trở nên dễ dàng hơn nhiều. Mình dự định sẽ áp dụng vào dự án cá nhân để tối ưu hóa CI/CD pipeline.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** để quản lý multi-container application. Docker Compose giúp định nghĩa và chạy nhiều services trong một file YAML, rất tiện cho việc phát triển local environment. Mình sẽ apply vào project hiện tại để đơn giản hóa việc setup dev environment cho team.


## Programming  

Hôm nay tìm hiểu về `git rebase -i` để chỉnh sửa lịch sử commit. Có thể thay đổi thứ tự commit, squash nhiều commit thành một, hoặc sửa message. Rất hữu ích để giữ lịch sử commit sạch sẽ trước khi push lên remote.  

Plan: Áp dụng ngay cho branch feature đang làm để merge vào main một cách gọn gàng hơn.


## DevOps

Hôm nay tìm hiểu về `kubectl debug`, một feature giúp attach debug container vào pod đang chạy mà không cần thay đổi manifest. Rất hữu ích cho troubleshooting production mà không gây downtime.

- `kubectl debug -it {pod-name} --image=busybox` sẽ tạo debug container chung namespace với pod gốc.
- Có thể chỉ định command riêng và volume mount để kiểm tra log hoặc network.

Cá nhân thấy đây là lifesaver khi gặp pod crashloop hay network issue, sẽ apply vào workflow troubleshoot thường xuyên.


## Programming  

Hôm nay tìm hiểu về cách dùng `asyncio` trong Python để xử lý các tác vụ bất đồng bộ hiệu quả. Đặc biệt hữu ích khi làm việc với API hoặc scraping dữ liệu từ nhiều nguồn cùng lúc. Có thể cải thiện performance đáng kể so với cách chạy tuần tự thông thường. Plan sẽ thử áp dụng vào dự án crawler dữ liệu thời gian tới.


## DevOps

Hôm nay tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container applications. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file `docker-compose.yml`, rất tiện cho việc phát triển và test local. Đã thử setup một project nhỏ với backend và database, thấy việc config và deploy đơn giản hơn nhiều so với chạy từng container riêng lẻ. Plan sẽ áp dụng cho dự án sắp tới để tối ưu workflow.


## DevOps  
Hôm nay tìm hiểu về cách sử dụng GitHub Actions để tự động build và deploy static site lên Netlify. Chỉ cần cấu hình workflow YAML đơn giản, mỗi khi push code vào main branch là site tự động cập nhật. Giúp tiết kiệm thời gian deploy thủ công và đảm bảo môi trường staging/production luôn đồng bộ. Plan: sẽ áp dụng cho blog cá nhân để thử nghiệm.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline cho GitHub Actions. Đặc biệt là sử dụng matrix strategy để chạy test song song trên nhiều version Node.js khác nhau, giúp tăng tốc quá trình CI. Đồng thời cũng thêm caching dependencies để giảm thời gian build. Cảm thấy việc này rất hữu ích khi dự án có nhiều environment khác nhau. Plan sẽ áp dụng ngay cho dự án hiện tại để optimize CI/CD.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline với GitHub Actions cho dự án Node.js. Sử dụng `actions/setup-node@v4` để tự động install Node.js và `npm ci` để install dependencies. Điều này giúp build và test nhanh hơn so với dùng `npm install`. 

Nhận thấy việc cache dependencies là rất quan trọng để tối ưu thời gian chạy workflow. Plan sẽ áp dụng ngay vào dự án hiện tại.


## DevOps

Hôm nay tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container application. Đặc biệt là cách viết file docker-compose.yml với version 3.x và cách chạy lệnh `docker-compose up -d` để start services ở background. Cảm thấy rất tiện lợi cho việc local development và testing.

- Sử dụng `depends_on` để định nghĩa service dependencies.
- Có thể scale service với `docker-compose up -d --scale service_name=3`.

Plan: Sẽ apply Docker Compose vào dự án cá nhân để đơn giản hóa việc setup môi trường cho team.
