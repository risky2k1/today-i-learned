# 2026-04-10

## DevOps

Hôm nay mình tìm hiểu về **Docker Compose v2** với cú pháp mới dùng YAML anchors để định nghĩa service chung. Việc này giúp giảm lặp code khi có nhiều service cùng cấu hình network hoặc volume. Mình sẽ áp dụng vào project hiện tại để quản lý môi trường dev đồng nhất hơn.

- Sử dụng `&` để đặt anchor và `<<` để merge vào service.
- Các service có thể tham chiếu lẫn nhau qua anchor, giúp dễ maintain.


## DevOps

Hôm nay tìm hiểu về `docker-compose` và cách dùng nó để quản lý multi-container applications. Cú pháp YAML trong `docker-compose.yml` khá trực quan, giúp định nghĩa services, networks và volumes một cách dễ dàng. Dùng lệnh `docker-compose up -d` để chạy toàn bộ stack một cách nhanh gọn.

Plan: Sẽ thử áp dụng vào dự án cá nhân để thay thế cho việc chạy container thủ công.


## DevOps

Hôm nay tìm hiểu về **GitOps** - một cách quản lý hạ tầng và ứng dụng thông qua Git. Khác với CI/CD truyền thống, GitOps đảm bảo trạng thái thực tế luôn khớp với Git, giúp rollback dễ dàng và audit tốt hơn. Có thể thử nghiệm với ArgoCD hoặc Flux cho dự án kế tiếp.

Cá nhân thấy GitOps phù hợp với team nhỏ, muốn giảm thiểu manual config và tăng tính reproducible.


## DevOps

Hôm nay tìm hiểu về cách cấu hình `kubectl` để quản lý multiple clusters trong Kubernetes. Có thể dùng `kubectl config use-context` để chuyển đổi giữa các clusters một cách nhanh chóng. Việc này giúp tiết kiệm thời gian khi làm việc với nhiều môi trường dev/staging/production.

Plan: Sẽ áp dụng ngay vào dự án đang làm để quản lý cluster staging và production hiệu quả hơn.


## DevOps

Hôm nay tôi tìm hiểu về Docker Compose - công cụ giúp định nghĩa và chạy multi-container applications. Docker Compose cho phép mô tả toàn bộ stack ứng dụng (web, database, cache) trong một file YAML duy nhất, giúp việc triển khai và quản lý trở nên đơn giản hơn nhiều so với việc chạy từng container riêng lẻ. 

- File `docker-compose.yml` thường chứa các services, networks và volumes.
- Lệnh `docker-compose up -d` sẽ start toàn bộ stack ở chế độ background.
- Rất phù hợp cho môi trường development và testing.

Plan: Sẽ áp dụng Docker Compose vào dự án cá nhân để standardize local development environment cho team.


## DevOps

Hôm nay tìm hiểu về `docker-compose` để quản lý multi-container application. Đặc biệt là cách dùng `depends_on` để đảm bảo service khởi động đúng thứ tự, tránh trường hợp database chưa sẵn mà app đã kết nối.

```yaml
services:
  db:
    image: postgres
  web:
    build: .
    depends_on:
      - db
```

Plan: Sẽ áp dụng vào project demo CI/CD để rút gọn quy trình deploy.


## DevOps

Hôm nay tìm hiểu về **AWS CodePipeline** và **CodeDeploy** để tự động hóa việc deploy ứng dụng. Có thể kết hợp với **CodeBuild** để build source và test tự động trước khi deploy. Cần tìm hiểu thêm về cách cấu hình pipeline cho môi trường staging và production.

Plan: Setup 1 pipeline đơn giản cho dự án hiện tại để rút ngắn thời gian deploy.


## DevOps

Hôm nay tìm hiểu về cách cấu hình `nginx` làm reverse proxy cho ứng dụng Node.js. Điểm quan trọng là phải chỉnh `proxy_set_header` để pass đúng IP client và các headers cần thiết. Ngoài ra, nên bật `gzip` để tăng performance.

Cá nhân thấy việc config nginx vẫn hơi phức tạp so với dùng load balancer của cloud, nhưng lại linh hoạt và tiết kiệm chi phí hơn cho các dự án nhỏ.


## DevOps

Hôm nay tìm hiểu về **AWS CodeDeploy** - dịch vụ tự động triển khai ứng dụng trên EC2, Fargate, Lambda. CodeDeploy hỗ trợ blue/green deployment, giúp giảm downtime và rollback dễ dàng. Đặc biệt phù hợp cho CI/CD pipeline khi kết hợp với CodePipeline.

Cá nhân thấy đây là một công cụ mạnh mẽ, nhưng cần cấu hình cẩn thận để tránh chi phí phát sinh. Plan: sẽ thử nghiệm trên một dự án demo nhỏ trước khi áp dụng vào production.


## DevOps

Hôm nay tôi học được cách sử dụng GitHub Actions để tự động deploy ứng dụng lên Heroku mỗi khi push code lên branch main. Bằng cách tạo workflow file `.github/workflows/deploy.yml` và cấu hình Heroku API key trong secrets, quá trình deploy trở nên hoàn toàn tự động, giảm thiểu lỗi do thao tác thủ công.

Mình thấy việc tự động hóa này rất hữu ích cho các dự án nhỏ, giúp tiết kiệm thời gian và đảm bảo môi trường staging/production luôn đồng bộ. Plan tiếp theo là sẽ tìm hiểu thêm về caching dependencies để tăng tốc độ build.
