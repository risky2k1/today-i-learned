# 2026-04-05

## DevOps

Hôm nay tôi tìm hiểu về **CI/CD pipeline với GitHub Actions**. Tôi đã cấu hình một workflow tự động chạy test và deploy khi push code lên branch `main`. Workflow sử dụng `ubuntu-latest` làm runner và chạy lệnh `npm test` trước khi deploy lên Heroku.

Điểm thú vị là GitHub Actions cho phép tích hợp dễ dàng với nhiều services khác nhau, giúp tự động hóa quy trình release. Plan tiếp theo của tôi là thêm bước check code quality với ESLint trước khi chạy test.


## DevOps

Hôm nay tìm hiểu về cách sử dụng GitHub Actions để tự động deploy static website lên Netlify. Chỉ cần tạo workflow file trong `.github/workflows/` và config provider là Netlify, toàn bộ quá trình deploy sẽ chạy tự động khi push code lên branch `main`.

Thấy cách này tiện hơn việc phải manual deploy từng lần, đặc biệt cho các dự án cá nhân nhỏ. Plan sẽ áp dụng cho blog cá nhân trong tuần tới.


## DevOps

Hôm nay tìm hiểu về `kubectl exec` để chạy lệnh trực tiếp vào container trong cluster Kubernetes. Lệnh này rất hữu ích khi debug hoặc kiểm tra trạng thái bên trong container mà không cần SSH vào node. Ví dụ: `kubectl exec -it <tên-pod> -- bash`. Kế hoạch là sẽ áp dụng vào pipeline CI/CD để tự động kiểm tra log khi deploy.

---

## Automation

Hôm nay tìm hiểu cách sử dụng GitHub Actions để tự động deploy static site lên Netlify. Chỉ cần tạo workflow file trong `.github/workflows/`, mỗi khi push code lên branch `main` là site sẽ tự build và deploy. Tiết kiệm thời gian và tránh lỗi thủ công.

---

## Travel Tech

Hôm nay tìm hiểu về ứng dụng Maps.me khi đi trekking ở Tà Năng - Phan Dũng. Ứng dụng này cho phép tải map offline và đánh dấu waypoints rất tiện lợi, giúp theo dõi lộ trình và an toàn hơn trên đường mòn. Kế hoạch là sẽ test thử trước khi đi thực tế.

---

## Programming

Hôm nay học về kỹ thuật memoization trong JavaScript để cache kết quả của các hàm computationally expensive. Áp dụng vào function tính Fibonacci giúp tăng performance đáng kể. Kế hoạch là sẽ refactor lại một vài hàm trong project cũ để tối ưu hơn.

---

## Trekking - Hiking - Camping

Hôm nay tìm hiểu về địa điểm cắm trại ở Hồ Đá Xanh, Bình Dương. Nơi đây có view hồ đẹp, không khí trong lành, phù hợp cho nhóm nhỏ nghỉ cuối tuần. Kế hoạch là sẽ tổ chức trip vào tháng sau nếu thời tiết ổn định.


## DevOps

Hôm nay tìm hiểu về Docker Compose. Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file `docker-compose.yml`. Rất tiện cho việc setup môi trường dev với nhiều service như web, db, cache. Sẽ áp dụng cho dự án cá nhân để đơn giản hóa quá trình deploy.

## Automation

Học được cách tự động hóa backup database bằng cronjob và shell script. Script sẽ export database ra file SQL và upload lên S3. Cronjob chạy hàng đêm để đảm bảo dữ liệu luôn được backup an toàn. Plan sẽ setup cho cả database production.

## Travel Tech

Tìm hiểu về app Maps.me - ứng dụng bản đồ offline rất hữu ích khi đi trekking ở những nơi không có internet. App có sẵn rất nhiều đường mòn và điểm tham quan. Sẽ tải về và test trước chuyến đi Fansipan sắp tới.

## Programming

Hôm nay học về Design Patterns - Singleton. Singleton đảm bảo chỉ có duy nhất một instance của class và cung cấp global point để truy cập. Pattern này hữu ích khi cần quản lý connection pool hoặc logger. Sẽ áp dụng vào dự án Java đang làm.

## Trekking - Hiking - Camping

Địa điểm trekking mới: Núi Bà Đen ở Tây Ninh. Núi có độ cao 986m, đường trek xuyên rừng với nhiều đá. Có cáp treo nhưng trekking vẫn là trải nghiệm thú vị. Plan sẽ đi vào cuối tháng này cùng nhóm bạn.


## DevOps

Hôm nay tìm hiểu về cách cấu hình multi-stage Docker builds để giảm kích thước image. Bằng cách tách riêng giai đoạn build và runtime, có thể loại bỏ hoàn toàn dependencies build không cần thiết ở production. Kết hợp với .dockerignore và sử dụng base image nhẹ như Alpine cũng giúp giảm đáng kể dung lượng.

Plan: Áp dụng ngay vào dự án hiện tại để tối ưu CI/CD pipeline.
