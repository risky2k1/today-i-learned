# 2026-04-06

## DevOps

Hôm nay tôi tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container application. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file `docker-compose.yml`, rất tiện lợi cho việc phát triển và test local.

Tôi đã thử setup một ứng dụng Node.js kết hợp với MySQL, và nhận ra việc config network và volume trong Compose file rất quan trọng để đảm bảo các service giao tiếp với nhau một cách ổn định.


## DevOps

Hôm nay tìm hiểu về **CI/CD pipeline với GitHub Actions**. Tự động build và test mỗi khi push code, giúp phát hiện lỗi sớm. Có thể tích hợp với Docker để deploy container tự động.

Plan: Sẽ thử setup pipeline cho dự án cá nhân để làm quen.


## DevOps

Hôm nay tôi tìm hiểu về `docker-compose` và cách sử dụng `.env` file để quản lý biến môi trường. Việc này giúp tránh hardcode config vào file compose, dễ dàng deploy trên nhiều môi trường khác nhau. Ngoài ra, tôi cũng biết thêm lệnh `docker-compose -f` để override file compose khi cần.

Plan: Sẽ áp dụng ngay vào dự án đang làm để tách biệt config dev và prod.


## DevOps  
Hôm nay tìm hiểu về việc sử dụng GitHub Actions để tự động deploy ứng dụng lên AWS EC2. Bằng cách cấu hình workflow với SSH key, có thể tự động chạy lệnh pull và restart service trên server mỗi khi push code. Điều này giúp tiết kiệm thời gian và giảm thiểu lỗi thủ công. Kế hoạch sắp tới là tích hợp thêm bước test trước khi deploy để đảm bảo chất lượng code.


## DevOps  
Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline cho GitLab CI, cụ thể là sử dụng `.gitlab-ci.yml` để tự động deploy ứng dụng lên AWS EC2. Phát hiện ra có thể dùng `only: [master]` để giới hạn chỉ merge vào nhánh master mới trigger.  

Plan: Sẽ thử setup thử nghiệm trên project cá nhân để làm quen trước khi áp dụng vào team.


## Programming  

Hôm nay mình tìm hiểu về `git rebase` và cách nó giúp làm sạch lịch sử commit trong một branch. Thay vì dùng `merge`, rebase sẽ di chuyển toàn bộ commit của branch feature lên đầu branch chính, giúp lịch sử commit trông tuyến tính và dễ theo dõi hơn. Tuy nhiên cần cẩn thận vì rebase sẽ thay đổi hash của commit, nên không nên rebase branch đã push lên remote. Plan là sẽ áp dụng rebase cho các feature branch local trước khi merge vào main.


## DevOps

Hôm nay tìm hiểu về việc sử dụng multi-stage build trong Docker để tối ưu hóa kích thước image. Việc này giúp giảm thiểu image từ vài trăm MB xuống còn vài chục MB bằng cách tách biệt môi trường build và runtime. Mình sẽ áp dụng ngay vào dự án hiện tại để cải thiện tốc độ deploy.

## Programming

Học được cách sử dụng `git rebase -i` để chỉnh sửa lịch sử commit một cách hiệu quả. Thay vì tạo ra nhiều commit thừa, có thể squash chúng lại thành một commit clean hơn. Điều này giúp lịch sử git dễ đọc và chuyên nghiệp hơn.

## Travel Tech

Tìm hiểu về ứng dụng Maps.me khi trekking ở Cát Tiên. Ứng dụng này cho phép download map offline và có sẵn các trail trekking, rất hữu ích khi không có internet. Mình sẽ thử dùng nó trong chuyến đi sắp tới để tránh lạc đường.

## Trekking - Hiking - Camping

Khám phá được một địa điểm camping mới ở Bà Nà Hills - khu vực đồi thông gần đỉnh núi. Không khí mát mẻ, view đẹp, thích hợp cho camping cuối tuần. Tuy nhiên cần chuẩn bị kỹ vì đường lên khá dốc và xa trung tâm.


## DevOps

Hôm nay tôi tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container applications. Docker Compose giúp định nghĩa và chạy nhiều container cùng lúc thông qua file YAML, rất tiện lợi cho việc phát triển và testing môi trường.

Tôi sẽ thử áp dụng Docker Compose vào dự án cá nhân để đơn giản hóa quá trình setup database và backend services.


## DevOps  
Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trong GitLab với Docker multi-stage builds. Nhận ra rằng việc tách biệt môi trường build và runtime giúp giảm đáng kể kích thước image, từ đó tăng tốc độ deploy và tiết kiệm tài nguyên. Sẽ áp dụng ngay vào dự án hiện tại để tối ưu CI/CD pipeline.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline cho dự án sử dụng GitHub Actions. Có thể tự động build, test và deploy ứng dụng lên AWS EC2 chỉ với vài dòng YAML đơn giản. Điều này giúp tiết kiệm thời gian và giảm thiểu lỗi do thao tác thủ công.

Plan: Sẽ áp dụng vào dự án hiện tại để tối ưu quy trình release.
