# 2026-04-09

## DevOps

Hôm nay tìm hiểu về cách dùng `kubectl debug` để tạo một pod debug tạm thời mà không ảnh hưởng đến pod đang chạy. Lệnh này rất hữu ích khi cần kiểm tra trạng thái của một container đang gặp sự cố mà không phải restart lại pod gốc. Plan: sẽ áp dụng trong dự án Kubernetes để rút ngắn thời gian debug.

## Automation

Phát hiện ra rằng GitHub Actions có thể tự động trigger workflow khi có commit vào branch cụ thể bằng `on: push: branches: [feature/*]`. Giúp tự động test cho các branch feature mà không cần can thiệp thủ công. Plan: sẽ cấu hình cho project hiện tại để tăng tính tự động.

## Travel Tech

Tìm hiểu về ứng dụng Maps.me có thể download offline map cho các vùng ở Việt Nam, rất tiện khi trekking ở những nơi không có sóng. Đặc biệt hữu ích cho các chuyến đi vùng cao như Hoàng Su Phì hay Mù Cang Chải. Plan: sẽ test thử trước chuyến đi Fansipan sắp tới.

## Programming

Hôm nay học được cách dùng `git rebase -i` để squash nhiều commit nhỏ thành một commit có ý nghĩa trước khi merge vào main branch. Giúp lịch sử commit sạch sẽ và dễ theo dõi hơn. Plan: sẽ áp dụng cho team khi review code.

## Trekking - Hiking - Camping

Khám phá ra khu vực Tà Năng - Phan Dũng có thể trekking quanh năm, nhưng đẹp nhất là từ tháng 11 đến tháng 4 khi trời khô ráo. Cảnh quan đồi cỏ và rừng thông rất thích hợp cho camping qua đêm. Plan: sẽ lên lịch cho một chuyến đi cuối năm cùng nhóm bạn.


## DevOps

Hôm nay tìm hiểu về **Docker multi-stage builds** để tối ưu hóa kích thước image. Bằng cách tách biệt giai đoạn build và runtime, có thể loại bỏ hoàn toàn các dependencies không cần thiết cho production. Ví dụ: dùng `golang:alpine` để build, rồi copy binary sang `scratch` để chạy, giảm từ ~800MB xuống còn ~10MB. Cảm thấy đây là kỹ thuật nên áp dụng cho mọi service mới để tiết kiệm storage và tăng tốc CI/CD.


## Programming

Hôm nay tìm hiểu về `git rebase -i` để chỉnh sửa commit history trước khi push. Có thể thay đổi thứ tự, squash commit, hoặc edit message trực tiếp trong editor. Rất hữu ích để giữ commit history clean và dễ đọc. Plan sẽ thử áp dụng khi làm feature mới để luyện tập.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline cho dự án Node.js sử dụng GitHub Actions. Điểm quan trọng là cần tách biệt môi trường development và production trong workflow để tránh deploy nhầm. Plan sẽ thử áp dụng vào dự án đang làm để rút ngắn thời gian deploy.


## DevOps  
Hôm nay tôi tìm hiểu về cách cấu hình Docker Compose để chạy nhiều services cùng lúc. Việc định nghĩa networks và volumes trong docker-compose.yml giúp các containers giao tiếp với nhau một cách dễ dàng và an toàn. Tôi cũng nhận ra rằng nên đặt tên volume rõ ràng để quản lý dữ liệu tốt hơn.  

Plan: Sẽ áp dụng vào dự án cá nhân để thử nghiệm CI/CD pipeline.


## DevOps

Hôm nay tìm hiểu về cách dùng `docker-compose` để chạy multi-container application. Tìm hiểu cách viết file `docker-compose.yml` với services, networks và volumes. Điều này giúp đơn giản hóa việc deploy và quản lý các container liên quan.

Sẽ áp dụng vào dự án cá nhân để test local trước khi deploy lên production.


## DevOps

Hôm nay tìm hiểu về Docker multi-stage builds. Bằng cách sử dụng nhiều FROM trong Dockerfile, có thể tách riêng môi trường build và runtime, giúp giảm kích thước image cuối cùng đáng kể. Ví dụ build ứng dụng Go trong stage 1 rồi copy binary sang stage 2 dùng Alpine.

Thấy cách này rất hữu ích cho CI/CD pipeline, sẽ thử áp dụng vào dự án hiện tại để optimize Docker image.


## Programming

Hôm nay mình tìm hiểu về cách sử dụng `asyncio` trong Python để xử lý các tác vụ bất đồng bộ. Bằng cách kết hợp `async` và `await`, có thể cải thiện hiệu năng đáng kể cho các ứng dụng I/O-bound như crawler hay API calls. Tuy nhiên, cần chú ý không trộn lẫn code sync và async để tránh deadlock.

- Mình sẽ áp dụng vào dự án crawler dữ liệu để giảm thời gian chạy.
- Cần đọc thêm về `asyncio.gather()` để chạy nhiều task song song hiệu quả.


## DevOps
Hôm nay tìm hiểu về `kubectl debug`, một command giúp tạo debug pod từ một pod đang chạy mà không cần phải restart lại pod gốc. Rất hữu ích cho việc debug ứng dụng trong môi trường production mà không làm gián đoạn service. Sẽ thử áp dụng vào dự án Kubernetes của team trong tuần sau.

## Travel Tech
Vừa đọc được bài viết về ứng dụng "Cổng Trời Mường Hoa" ở Sapa, giúp du khách đặt vé cáp treo và hướng dẫn tham quan tự động. Công nghệ AR được tích hợp để giới thiệu văn hóa địa phương khi tham quan. Nghĩ là nên thử đi Sapa vào mùa lúa chín năm nay.

## Programming
Học được cách sử dụng `Object.seal()` trong JavaScript để ngăn không cho thêm hoặc xóa properties của object, nhưng vẫn cho phép chỉnh sửa giá trị hiện có. Khác với `Object.freeze()` là cũng không cho chỉnh sửa. Sẽ cân nhắc dùng khi cần bảo vệ cấu trúc object nhưng vẫn cần mutability.

## Trekking - Hiking - Camping
Đỉnh Bà Đen ở Tây Ninh vừa mở thêm cung đường trekking mới, khó hơn nhưng cảnh quan đẹp hơn so với đường cáp treo. Độ dài khoảng 3.5km với độ cao 400m, thích hợp cho beginner muốn thử thách. Cuối tuần sau có thể rủ team đi trải nghiệm.


## DevOps

Hôm nay tôi tìm hiểu về **AWS CodePipeline** - một dịch vụ CI/CD giúp tự động hóa việc triển khai ứng dụng từ mã nguồn đến môi trường sản xuất. CodePipeline tích hợp tốt với GitHub, CodeBuild và Elastic Beanstalk, giúp giảm thiểu công đoạn thủ công và tăng tính nhất quán. Tôi dự định sẽ thử setup một pipeline đơn giản cho dự án cá nhân để làm quen với workflow.
