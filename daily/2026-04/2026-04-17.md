# 2026-04-17

## DevOps

Hôm nay tìm hiểu về cách setup CI/CD pipeline cho một dự án Node.js sử dụng GitHub Actions. Cấu hình bao gồm các bước test, build và deploy tự động lên AWS Elastic Beanstalk. Một điểm hay là có thể dùng secrets để bảo mật thông tin nhạy cảm như AWS credentials. Plan sắp tới là sẽ tích hợp thêm Slack notification để theo dõi trạng thái deploy.


## Programming  

Hôm nay tìm hiểu về cách sử dụng `git worktree` để quản lý nhiều branch cùng lúc trong cùng một repository. Lệnh này giúp tạo ra các thư mục làm việc riêng biệt cho từng branch, tránh xung đột khi chuyển đổi giữa các branch. Đây là một công cụ hữu ích khi cần làm song song nhiều tính năng hoặc fix bug. Plan là sẽ áp dụng trong dự án sắp tới để tăng hiệu suất làm việc nhóm.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trên GitHub Actions để tự động deploy ứng dụng lên AWS ECS. Có thể sử dụng `on: [push]` để trigger workflow và dùng `aws-actions/configure-aws-credentials` để thiết lập quyền truy cập AWS.

- Sử dụng `docker/build-push-action@v4` để build và push image lên ECR.
- Kết hợp `aws-actions/ecs-deploy` để tự động deploy lên cluster ECS.

Plan: Áp dụng vào dự án cá nhân để rút ngắn thời gian deploy và tăng tính tự động hóa.


## DevOps

Hôm nay tìm hiểu về **Docker Compose v2** và cách sử dụng file `compose.yaml` thay vì `docker-compose.yaml`. Điểm thú vị là syntax đã được đơn giản hóa, không còn phải dùng `services` ở đầu file nữa. Ví dụ:

- File `compose.yaml` chỉ cần định nghĩa services trực tiếp.
- Lệnh chạy vẫn là `docker compose up` (không có dấu gạch ngang).

Cảm thấy syntax mới gọn gàng hơn, sẽ áp dụng cho project nhỏ sắp tới để làm quen.


## Programming

Hôm nay tìm hiểu về `git rebase -i` để chỉnh sửa, sắp xếp lại lịch sử commit. Thấy cách này rất hữu ích khi muốn làm sạch commit history trước khi push lên remote, giúp code review dễ hơn.

- Dùng `git rebase -i HEAD~3` để sửa 3 commit gần nhất.
- Có thể `squash` (gộp) hoặc `fixup` (gộp ko giữ message) các commit liên quan.

Plan: Áp dụng khi làm feature mới để tránh commit "fix typo" lộn xộn.


## DevOps

Hôm nay tìm hiểu về cách cấu hình Nginx làm reverse proxy cho nhiều service trên cùng một server. Dùng server blocks (virtual hosts) để map domain/subdomain tới các container khác nhau, giúp tối ưu tài nguyên và dễ quản lý. Cũng thử nghiệm với certbot để tự động generate SSL certificate cho từng domain.

Nhận xét: Cách làm này khá phổ biến trong môi trường production nhỏ, giúp giảm chi phí server mà vẫn đảm bảo tách biệt service. Plan: Áp dụng cho project cá nhân để làm quen trước khi triển khai cho team.


## DevOps

Hôm nay tìm hiểu về cách cấu hình multi-stage build trong Dockerfile để tối ưu Docker image cho production. Bằng cách tách biệt môi trường build và runtime, giúp giảm kích thước image đáng kể và tăng tính bảo mật. Sẽ áp dụng ngay vào dự án đang làm để cải thiện CI/CD pipeline.


## DevOps  

Hôm nay mình biết được cách cấu hình multi-stage build trong Dockerfile để tối ưu kích thước image. Bằng cách tách riêng giai đoạn build và runtime, image cuối cùng chỉ giữ lại các file cần thiết, giúp giảm đáng kể dung lượng. Mình sẽ áp dụng ngay vào dự án hiện tại để cải thiện tốc độ deploy.


## DevOps

Hôm nay tìm hiểu về Docker Compose, công cụ giúp định nghĩa và chạy multi-container applications. Compose sử dụng file YAML để cấu hình services, networks và volumes, giúp đơn giản hóa việc quản lý môi trường development. Đặc biệt hữu ích khi cần chạy đồng thời nhiều services như web app, database, cache trong một project.

Sẽ áp dụng vào project cá nhân để thay thế cách chạy thủ công từng container hiện tại.
