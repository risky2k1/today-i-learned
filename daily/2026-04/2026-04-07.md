# 2026-04-07

## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trên GitHub Actions cho dự án Node.js. Có thể dùng workflow `.yml` để tự động chạy test và deploy khi push code lên branch `main`. Điều này giúp tiết kiệm thời gian deploy thủ công và đảm bảo code luôn được test trước khi lên production.

Plan: Áp dụng workflow này cho dự án cá nhân để làm quen với GitHub Actions.


## Programming

Hôm nay tìm hiểu về cách sử dụng Python's `concurrent.futures` để thực thi các tác vụ song song. Module này giúp đơn giản hóa việc quản lý thread và process pool, đặc biệt hữu ích khi cần xử lý nhiều request HTTP hoặc tính toán độc lập. So với `threading` và `multiprocessing` truyền thống, API của nó dễ dùng hơn và ít lỗi hơn.

Định thử áp dụng vào script download file từ nhiều nguồn để tăng tốc độ.


## Programming

Hôm nay tìm hiểu về cách sử dụng `asyncio` trong Python để chạy các tác vụ không đồng bộ. Bằng cách dùng `async def` và `await`, có thể cải thiện hiệu năng khi xử lý nhiều request cùng lúc. Đặc biệt hữu ích cho các ứng dụng web hoặc API.

Cần thử áp dụng vào project crawler dữ liệu để giảm thời gian chạy.


## DevOps

Hôm nay tìm hiểu về `kubectl debug`, một feature giúp debug pod trực tiếp mà không cần restart lại container. Trước đây thường dùng `kubectl exec` nhưng bị giới hạn khi process chính đã crash. Giờ có thể attach một debug container vào pod đang chạy để kiểm tra logs, network, filesystem. Sẽ thử áp dụng vào dự án Kubernetes production để rút ngắn thời gian troubleshoot.

## Automation

Học được cách dùng `expect` để tự động hóa các tác vụ SSH tương tác. Trước đây toàn phải viết script phức tạp với `sshpass`, giờ chỉ cần match các prompt và gửi input tự động. Ví dụ automate login vào server, nhập password, rồi chạy lệnh. Sẽ dùng để backup config các thiết bị mạng.

## Travel Tech

Khám phá ra app Maps.me có thể download offline map của các vùng sâu, vùng xa ở Việt Nam rất chi tiết, kể cả đường mòn trekking. Trước đây toàn phải mang theo giấy, giờ chỉ cần phone là đủ. Sẽ test thử ở Pù Luông cuối tháng này.

## Programming

Hôm nay biết thêm về `WeakSet` trong JavaScript - một tập hợp chỉ chứa object references và không prevent GC. Khác với `Set` là có thể hold primitive values. Dùng hợp lý sẽ giúp tránh memory leak khi theo dõi các object tạm thời. Sẽ refactor lại phần caching trong dự án.

## Trekking - Hiking - Camping

Vừa đọc review về cung trekking Tà Năng - Phan Dũng mùa này rất đẹp, đồi cỏ xanh mướt và không quá nắng. Tuy nhiên nước khá hiếm nên cần chuẩn bị kỹ. Plan là cuối tháng 10 sẽ rủ team đi 3 ngày 2 đêm, kết hợp teambuilding.


## DevOps

Hôm nay tìm hiểu về **Docker multi-stage builds**. Kỹ thuật này giúp giảm kích thước image cuối cùng bằng cách tách riêng các bước build và runtime. Ví dụ, có thể dùng một image lớn chứa môi trường build để compile code, sau đó copy kết quả vào một image nhẹ hơn để deploy.

Điều này giúp tiết kiệm không gian lưu trữ và tăng tốc độ pull/push image. Sẽ áp dụng cho dự án CI/CD để tối ưu pipeline.
