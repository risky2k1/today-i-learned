# 2026-04-20

## DevOps

Hôm nay tôi tìm hiểu về `kubectl debug` trong Kubernetes. Lệnh này cho phép tạo một pod tạm thời gắn vào namespace của pod gốc để debug mà không cần thay đổi trực tiếp pod đang chạy. Rất hữu ích khi pod bị crash loop backoff mà không thể exec vào được. Tôi sẽ thử áp dụng trong dự án sắp tới để rút ngắn thời gian debug.


## Programming  

Hôm nay tìm hiểu về việc dùng `git rebase` để làm sạch lịch sử commit. Thay vì merge, rebase giúp giữ history thẳng tắp và dễ đọc hơn. Tuy nhiên cần cẩn thận khi rebase branch công cộng vì có thể gây conflict cho người khác. Sắp tới sẽ áp dụng vào các feature branch nhỏ để thực hành thêm.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trong GitLab CI cho một dự án Python. Điểm quan trọng là dùng `before_script` để cài đặt dependencies và `artifacts` để lưu lại kết quả test. Nhận thấy việc chia nhỏ stages (test, build, deploy) giúp dễ debug hơn khi có lỗi xảy ra. Plan: sẽ áp dụng cho dự án cá nhân để làm quen dần với workflow này.


## Programming

Hôm nay tìm hiểu về cách sử dụng `git worktree` để quản lý nhiều branch cùng lúc mà không cần phải clone lại repository. Thay vì phải chuyển branch liên tục, có thể tạo thêm worktree cho branch khác và làm việc song song. Tiết kiệm thời gian và tránh xung đột khi cần so sánh code giữa các branch.

Plan: Sẽ áp dụng cho dự án có nhiều feature branch cần review cùng lúc.


## DevOps  
Hôm nay tìm hiểu về cách sử dụng GitHub Actions để tự động deploy static site lên GitHub Pages. Cấu hình workflow đơn giản chỉ cần tạo file `.github/workflows/main.yml` với các bước build và deploy. Việc này giúp tiết kiệm thời gian và đảm bảo site luôn được cập nhật mới nhất mỗi khi push code. Plan: sẽ áp dụng cho blog cá nhân trong tuần tới.

## Programming  
Biết thêm một mẹo nhỏ khi debug JavaScript: dùng `console.table()` để hiển thị mảng hoặc object dưới dạng bảng, giúp dễ đọc hơn so với `console.log()`. Đặc biệt hữu ích khi làm việc với dữ liệu lớn. Plan: nhớ áp dụng khi cần inspect data trong dự án sắp tới.

## Travel Tech  
Khám phá ứng dụng Maps.me khi trekking ở rừng Cúc Phương. Ứng dụng này cho phép tải map offline và track lộ trình GPS khá chính xác, rất tiện lợi ở những nơi sóng yếu. Plan: sẽ thử nghiệm thêm các tính năng ghi chú waypoint cho chuyến đi sau.

## Trekking - Hiking - Camping  
Địa điểm: Đỉnh Bà Nà (Đà Nẵng). Đã thử cắm trại qua đêm tại khu vực gần chân cáp treo, không khí mát mẻ và view đẹp. Lưu ý mang theo áo ấm vì nhiệt độ chênh lệch khá lớn so với dưới xuôi. Plan: muốn thử cắm trại ở đỉnh cao hơn vào mùa thu năm nay.


## DevOps  
Hôm nay tôi tìm hiểu về Kubernetes CronJob và cách cấu hình schedule với thời gian UTC. Lưu ý là khi khai báo `schedule: "0 0 * * *"` trong cronjob spec, nó sẽ chạy đúng 0h UTC, không phải 0h giờ địa phương. Để tránh nhầm lẫn, nên dùng `TZ` environment variable hoặc convert time sang UTC trước khi viết schedule.  

Plan: Sẽ apply vào hệ thống backup tự động để đảm bảo chạy đúng giờ VN (7h sáng UTC+7).


## DevOps

Hôm nay tôi tìm hiểu về **GitOps** và cách nó giúp tự động hóa triển khai ứng dụng thông qua Git làm source of truth. Các công cụ như ArgoCD hay Flux giúp theo dõi thay đổi trong Git và tự động cập nhật môi trường production. Điều này giúp giảm thiểu lỗi do thao tác thủ công và dễ dàng audit hơn.

Tôi sẽ thử áp dụng GitOps cho dự án nhỏ của team để xem hiệu quả thực tế ra sao.


## DevOps  
Hôm nay tìm hiểu về cách cấu hình multi-stage builds trong Docker để tối ưu hóa kích thước image. Việc tách riêng giai đoạn build và runtime giúp giảm đáng kể dung lượng, đồng thời vẫn giữ được tính bảo mật. Plan: áp dụng ngay cho dự án hiện tại để cải thiện tốc độ deploy.


## DevOps

Hôm nay tìm hiểu về `kubectl` plugin để quản lý Kubernetes cluster hiệu quả hơn. Plugin `kubectl-tree` giúp visualize resource dependency trong cluster, rất hữu ích khi troubleshoot. Mình đã cài đặt và thử nghiệm, giao diện tree view giúp dễ dàng hiểu quan hệ giữa các pods, services và deployments. Plan sẽ apply vào dự án hiện tại để giảm thời gian debug.


## DevOps

Hôm nay tìm hiểu về **GitOps workflow** - một cách tiếp cận mới để quản lý infrastructure và app deployments. Ý tưởng là mọi thay đổi đều được thực hiện thông qua Git repository, tự động sync với môi trường production. Điều này giúp tăng tính minh bạch, audit và rollback dễ dàng.

- GitOps phù hợp với Kubernetes và các hệ thống cloud-native.
- Công cụ phổ biến: ArgoCD, Flux CD.

Sắp tới sẽ thử setup ArgoCD cho dự án Kubernetes của team.
