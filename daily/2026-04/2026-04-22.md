# 2026-04-22

## DevOps

Hôm nay tìm hiểu về việc quản lý secrets trong CI/CD pipeline bằng GitHub Actions. Thay vì hardcode credentials vào workflow, nên sử dụng GitHub Secrets hoặc external secrets manager như HashiCorp Vault để tăng tính bảo mật. Đồng thời, cần hạn chế expose secrets trong logs để tránh rò rỉ thông tin nhạy cảm. Plan: sẽ áp dụng vào dự án hiện tại để thay thế cách lưu credentials cũ.


## DevOps  
Hôm nay tìm hiểu về cách cấu hình `docker-compose` để chạy nhiều services trong cùng một network. Nhận ra rằng việc đặt tên cho các service và network rõ ràng sẽ giúp việc quản lý và debug dễ dàng hơn rất nhiều. Sắp tới sẽ áp dụng vào project cá nhân để test môi trường production.


## DevOps

Hôm nay tìm hiểu về `docker-compose` để quản lý multi-container applications. Tìm ra cách sử dụng `.env` file để config environment variables cho từng service, giúp tránh hardcode và dễ deploy hơn. Plan sẽ thử áp dụng cho project nhỏ đang làm để tối ưu hóa CI/CD pipeline.

## Automation

Khám phá được tính năng "Quick Steps" trong Outlook, cho phép tự động hóa các tác vụ lặp lại như forward mail, gắn tag, hoặc reply template. Rất tiện cho công việc hàng ngày. Plan sẽ setup vài Quick Steps cho các quy trình thường xuyên để tiết kiệm thời gian.

## Travel Tech

Hôm nay biết thêm về ứng dụng "Google Maps Go" - phiên bản nhẹ của Google Maps, phù hợp với điện thoại cấu hình thấp hoặc khi cần tiết kiệm data. Đặc biệt hữu ích khi đi du lịch vùng sâu vùng xa ở Việt Nam. Plan sẽ cài đặt và test thử trước chuyến đi sắp tới.

## Programming

Học được cách sử dụng `git rebase -i` để chỉnh sửa, sắp xếp lại commit history trước khi merge vào main branch. Giúp code review dễ dàng hơn và lịch sử commit sạch sẽ. Plan sẽ áp dụng cho các feature branch trong dự án team.

## Trekking - Hiking - Camping

Khám phá được địa điểm trekking mới ở Đà Lạt: Langbiang Mountain Trail. Đường mòn khá thử thách nhưng view rất đẹp, phù hợp cho những ai muốn trải nghiệm cảm giác mạo hiểm. Plan sẽ lên lịch trekking vào cuối tháng này và chuẩn bị đầy đủ trang thiết bị cần thiết.


## DevOps

Hôm nay tìm hiểu về cách sử dụng GitHub Actions để tự động deploy static website lên GitHub Pages. Chỉ cần thêm một workflow YAML đơn giản vào thư mục `.github/workflows/`, mỗi lần push code lên main branch là website sẽ tự động cập nhật. Rất tiện lợi cho các dự án cá nhân không cần server riêng.

Plan: Sẽ thử nghiệm với blog cá nhân để rút ngắn thời gian deploy.


## DevOps

Hôm nay tôi học được cách sử dụng Docker Compose để quản lý multi-container application. Thay vì chạy từng container riêng lẻ, tôi có thể định nghĩa tất cả trong một file `docker-compose.yml` và start/stop chúng cùng lúc. Điều này giúp môi trường development trở nên nhất quán hơn giữa các thành viên trong team. Plan tiếp theo là sẽ thử migrate một project hiện tại sang dùng Docker Compose để đơn giản hóa quá trình setup.


## DevOps

Hôm nay tìm hiểu về Kubernetes, một hệ thống container orchestration mạnh mẽ. K8s giúp quản lý, scale và deploy ứng dụng container một cách tự động. Nó cũng hỗ trợ load balancing và tự động phục hồi khi có pod bị lỗi.

Mình thấy K8s khá phức tạp nhưng rất hữu ích cho các hệ thống lớn. Plan là sẽ setup một cluster K8s nhỏ để test thử nghiệm.


## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline với GitHub Actions cho dự án Node.js. Có thể tối ưu bằng cách dùng caching cho node_modules để giảm thời gian build. Plan: Áp dụng vào dự án cá nhân để rút kinh nghiệm trước khi triển khai ở công ty.
