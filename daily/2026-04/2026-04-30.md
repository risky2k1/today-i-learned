# 2026-04-30

Tuyệt vời! Mình sẽ bắt đầu ghi lại Today I Learned cho bản thân.

## Programming

*   **`str.startswith()` và `str.endswith()`:** Hai hàm này cực kỳ hữu ích để kiểm tra xem một chuỗi có bắt đầu hoặc kết thúc bằng một chuỗi con cụ thể hay không. Mình đã từng dùng `if string.startswith("http")` để lọc các URL.
*   **List Comprehensions:**  Cách viết ngắn gọn và hiệu quả để tạo ra một danh sách mới dựa trên một danh sách hiện có. Ví dụ: `[x * 2 for x in range(10)]` sẽ tạo ra một danh sách các số từ 0 đến 18.
*   **`f-strings` (Formatted String Literals):**  Cách dễ dàng nhất để chèn các biến vào chuỗi. Ví dụ: `name = "Lan"; print(f"Chào {name}!")`

**Nhận xét:** Mình thấy list comprehensions rất mạnh mẽ và sẽ cố gắng sử dụng chúng nhiều hơn trong các dự án sắp tới để code ngắn gọn và dễ đọc hơn.  Mình cũng cần luyện tập thêm để sử dụng `startswith()` và `endswith()` một cách hiệu quả hơn.



## DevOps  
Hôm nay tôi học cách quản lý secrets trong Kubernetes sử dụng **Sealed Secrets** để mã hóa và bảo mật thông tin nhạy cảm. Thay vì lưu trực tiếp các biến môi trường như mật khẩu hoặc khóa API trong config maps, Sealed Secrets cho phép chỉ giải mã trong cụm cluster tương ứng. Mình sẽ thử áp dụng công nghệ này vào dự án hiện tại để tăng cường bảo mật hệ thống.


Tuyệt vời! Đây là Today I Learned của mình:

## Programming

Hôm nay mình tìm hiểu về cách sử dụng `async/await` trong Python để viết code bất đồng bộ (asynchronous) dễ đọc và dễ bảo trì hơn. Thay vì dùng callbacks lồng nhau, `async/await` giúp code trông giống như code đồng bộ, nhưng lại tận dụng tối đa tài nguyên khi chờ đợi các tác vụ I/O.  Việc này đặc biệt hữu ích khi làm việc với các API hoặc database.

*   `async` định nghĩa một hàm là coroutine.
*   `await` dùng để tạm dừng thực thi hàm cho đến khi một coroutine khác hoàn thành.

Mình sẽ thử áp dụng `async/await` vào project xử lý dữ liệu từ nhiều API để cải thiện hiệu suất.


## Programming
- Tìm hiểu thêm `Promise.withResolvers()` để dễ dàng expose `resolve`, `reject` ra ngoài mà không cần viết executor rườm rà.
- Pattern này rất hợp để bridge giữa event callback và async/await mà vẫn giữ được tính tường minh.
- Mình sẽ áp dụng vào việc wrap các event từ 3rd-party SDK để code trông sạch hơn và dễ test.

Cảm nhận: nhỏ nhưng gọn, tiện bủn tay áo khi cần điều khiển promise từ xa.


## DevOps  

- Hôm nay mình thử cài Docker trên EC2 và cấu hình CI/CD đơn giản bằng GitHub Actions.  
- Khám phá cách monitor resource utilization với CloudWatch và thiết lập alarm cho CPU > 80%.  - Kết luận: Docker + Actions giúp triển khai nhanh mà không cần server quản lý.  

Mình sẽ áp dụng pipeline CI/CD này cho dự án nhỏ trong tuần tới để giảm thiểu downtime.


## Automation

- Khi sử dụng **GitHub Actions**, mình phát hiện được cách chạy workflow chỉ khi có thay đổi ở một thư mục nhất định bằng `paths` filter, giúp giảm thiểu thời gian CI/CD đáng kể.  
- Thêm một bước `cache` cho các dependencies (npm, pip…) trong job đầu tiên giúp giảm thời gian cài đặt lên tới 40% cho các pipeline kế tiếp.  

> Sắp tới mình sẽ triển khai pattern này cho mọi repository trong team để tối ưu hoá pipeline và giảm chi phí build.


# DevOps  
Mỗi buổi phản hồi này đều giúp tôi tiếp cận thói quen hiệu quả hơn trong các hoạt động sản xuất điện tử.

- Tôi thấy sự kết hợp nội dung Automation và Programing trở nên rõ ràng hơn.  
- Học hỏi này sẽ giúp tôi tối ưu hơn trong các dự án lớn trong những tháng tới.  
- Một nghĩ viết tốt hơn khi liệt kiện gì đó mình yêu thích nhiều nhất.

💡 Cần tập trung ngắn gọn vào ví dụ để rèn luyện phương pháp này.  

**Nhận xét:** Nên ôn lại vấn đề qua hội thảo xem lại.  
Plan này chiến lược cho tôi làm chuâu kệch trong tuần tới!


## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)

Hôm nay tìm hiểu về cung trekking **Đường hầm Mộc Châu (Sơn La)**, một tuyến đường mới nổi hấp dẫn thay thế cho Fansipan. Cung đường dài khoảng 15km, kết hợp leo núi, vượt suối và trải nghiệm văn hóa dân tộc Thái. Thời điểm đẹp nhất là từ tháng 10 đến tháng 12 khi trời khô ráo và cảnh sắc hoàng hôn trên đỉnh núi đẹp mê hoặc.  

* Nhận xét: Tuy nhiên cần lưu ý cung đường có khá nhiều đoạn dốc险峻, nên đi theo hướng dẫn viên địa phương và trang bị giày chống trơn. Plan sắp xếp một chuyến đi cuối năm để khám phá “vùng đất bí mật” ít người biết này!



## Programming

Hôm nay mình phát hiện **structured concurrency** trong Java Virtual Threads (Project Loom) giúp quản lý thread lifecycle tự động hơn. Khi một parent thread kết thúc, tất cả child threads sẽ được cancel và join tự động, tránh được vấn đề memory leak hay orphaned threads thường gặp với traditional threading. Pattern này rất hữu ích cho các ứng dụng cần xử lý đồng thời nhiều tác vụ I/O.

*TIL này khiến mình muốn thử nghiệm áp dụng vào dự án microservice đang phát triển để cải thiện performance.*
