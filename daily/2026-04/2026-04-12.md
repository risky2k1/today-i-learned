# 2026-04-12

## Programming

Hôm nay mình tìm hiểu về cách sử dụng `git rebase` để làm sạch lịch sử commit trước khi push code lên remote. Thay vì dùng `git merge`, rebase sẽ giúp các commit được xếp thẳng hàng, dễ theo dõi hơn. Mình đã thử với một branch feature và thấy code base trở nên gọn gàng hơn hẳn. Plan tiếp theo là sẽ áp dụng vào workflow của team để mọi người cùng thống nhất cách quản lý branch.

---

## Travel Tech

Mình vừa phát hiện ra ứng dụng Maps.me có thể download bản đồ offline cho các khu vực trekking ở Việt Nam như Sapa, Đà Lạt. Điều này rất hữu ích khi đi vào vùng sóng yếu. Mình đã thử với map của Fansipan và thấy độ chi tiết khá tốt, có cả các trail nhỏ. Plan là sẽ test thử ở Cát Tiên National Park vào cuối tháng này.


## DevOps  
Hôm nay mình tìm hiểu về cách cấu hình CI/CD pipeline trong GitLab CI cho một project Spring Boot. Mình sử dụng docker để build và push image lên registry, đồng thời deploy tự động lên Kubernetes cluster. Việc này giúp tiết kiệm thời gian deploy và đảm bảo tính nhất quán giữa các môi trường. Plan: sẽ tối ưu lại pipeline để thêm bước test tự động trước khi deploy.


## DevOps

Hôm nay tìm hiểu về **Docker Compose** để quản lý multi-container applications. Docker Compose giúp định nghĩa và chạy nhiều containers cùng lúc thông qua file `docker-compose.yml`, rất tiện cho việc phát triển local environment. Mình sẽ áp dụng ngay vào project hiện tại để đơn giản hóa việc setup dev environment cho team.

## Programming

Học được cách dùng **async/await** trong JavaScript để xử lý promise một cách gọn gàng hơn. Thay vì `.then()` chain, dùng async/await giúp code dễ đọc và debug hơn. Sẽ refactor lại một vài function trong project để cải thiện maintainability.

## Travel Tech

Khám phá app **Google Maps** có tính năng **Offline Maps** rất hữu ích khi đi trekking ở vùng không có internet. Có thể download bản đồ trước và sử dụng offline, tránh bị lạc đường. Mình sẽ thử nghiệm trong chuyến đi Fansipan sắp tới.

## Trekking - Hiking - Camping

Địa điểm **Ba Vì** gần Hà Nội rất phù hợp cho một chuyến camping cuối tuần. Có nhiều trails trekking vừa sức, view đẹp và không khí trong lành. Mình dự định tổ chức một chuyến đi nhỏ với team để kết hợp teambuilding và nghỉ dưỡng.


## DevOps
Hôm nay tìm hiểu về cách cấu hình `docker-compose` để chạy nhiều services cùng lúc trong một project. Đặc biệt là cách link các services với nhau qua network alias và volume mount. Mình thấy việc này giúp đơn giản hóa môi trường development rất nhiều so với việc cài đặt từng service riêng lẻ.

## Automation
Học được cách sử dụng GitHub Actions để tự động deploy website lên Netlify mỗi khi push code vào nhánh `main`. Cấu hình `.yml` đơn giản nhưng hiệu quả, giúp tiết kiệm thời gian deploy thủ công. Mình sẽ áp dụng workflow này cho các dự án cá nhân sắp tới.


## DevOps

Hôm nay tìm hiểu về cách cấu hình Docker Compose để chạy nhiều services trong cùng 1 network. Điểm hay là chỉ cần define network trong `docker-compose.yml` là các containers có thể giao tiếp với nhau qua tên service. 

- Lợi ích: dễ quản lý, không cần expose port thừa.
- Kế hoạch: sẽ áp dụng cho dự án microservices để đơn giản hóa việc deploy local.

Cá nhân thấy Docker Compose rất tiện cho việc phát triển và test nhanh.


## Programming

Hôm nay tìm hiểu về cách dùng `asyncio` trong Python để chạy các tác vụ không đồng bộ, giúp tăng hiệu suất khi làm việc với I/O-bound. Đặc biệt hữu ích khi gọi nhiều API cùng lúc mà không cần đợi từng cái một. Tuy nhiên cần cẩn thận với blocking code vì nó sẽ làm mất lợi ích của async. Plan: refactor một script cũ đang gọi API tuần tự thành async để test performance.

---

## Travel Tech

Biết thêm về ứng dụng "Google Maps Go" - bản rút gọn, nhẹ băng nửa so với Maps gốc, phù hợp với điện thoại cấu hình thấp hoặc khi cần tiết kiệm data. Giao diện vẫn đủ dùng cho việc xem bản đồ, chỉ đường và tìm địa điểm. Tiện lợi khi trekking ở vùng sóng yếu vì load nhanh hơn. Plan: cài thử trên máy dự phòng cho chuyến đi Sapa tới.

---

## DevOps

Học cách dùng multi-stage build trong Dockerfile để tối ưu kích thước image: build source trong một stage, copy artifact sang stage runtime chỉ với runtime dependencies. Giúp giảm từ vài trăm MB xuống vài chục MB. Lưu ý phải đặt lệnh `COPY --from=builder` đúng cách. Plan: áp dụng cho dự án Spring Boot đang deploy trên K8s.

---

## Trekking - Hiking - Camping

Khám phá được địa điểm cắm trại mới ở Đà Lạt: Đồi Đa Phú, view hồ Tuyền Lâm rất đẹp, đường vào vừa phải, phù hợp nhóm nhỏ. Cần chuẩn bị lều chống gió vì trên đồi cao. Nên đi vào sáng sớm để săn mây. Plan: lên kế hoạch cắm trại cuối tháng này cùng team.

---

## Automation

Tìm hiểu cách dùng GitHub Actions kết hợp với Dependabot để tự động rebase khi có conflict trong PR. Giảm thời gian manual maintenance. Cấu hình đơn giản với `actions/stale` và `dependabot` version updates. Plan: setup cho repo công ty để giữ branch up-to-date.

---

## Programming

Hôm nay biết về pattern "Circuit Breaker" trong Java với Resilience4j, giúp ngăn chặn cascaded failure khi downstream service không ổn định. Có thể config timeout, retry, fallback dễ dàng. Đặc biệt hữu ích cho microservice. Plan: thử nghiệm trên service payment của dự án.

---

## Travel Tech

Tìm hiểu về app "Maps.me" - bản đồ offline hoàn chỉnh, hỗ trợ hiking trails và POI ở vùng sâu. Data được tải về trước nên không cần data roaming. Giao diện trực quan, tích hợp cả tracking GPS. Plan: dùng cho chuyến trek Fansipan tới.

---

## DevOps

Học cách dùng Terraform Workspaces để quản lý multi-environment (dev/staging/prod) trong cùng một repo. Giúp tránh duplicate code, dễ so sánh state. Lưu ý phải cẩn thận với backend config. Plan: refactor infra repo đang tách thành nhiều folder.

---

## Trekking - Hiking - Camping

Khám phá được địa điểm trekking mới ở Vườn Quốc gia Cúc Phương: đường mòn vào hang Đăng Mòi vừa phải, rừng già và suối mát. Nên đi từ tháng 4-6 vì mùa mưa. Plan: tổ chức team building cuối tháng 5.

---

## Automation

Tìm hiểu cách viết GitHub Action để tự động comment checklist vào PR mới, nhắc reviewer các bước cần check (test, docs, migration). Giảm thiểu sót bước thủ công. Plan: deploy cho team frontend.

---

## Programming

Hôm nay biết cách dùng `@Value` trong Spring Boot để inject properties từ nhiều nguồn (YAML, environment, command line args) với độ ưu tiên rõ ràng. Giúp config linh hoạt khi deploy multi-env. Plan: refactor config service hiện tại.

---

## Travel Tech

Biết về app "PeakVisor" - nhận diện đỉnh núi qua camera, rất hữu ích khi trekking. Hỗ trợ cả 3D compass và đường đi. Data offline sau khi tải về. Plan: thử trên chuyến đi Tà Xùa.

---

## DevOps

Học cách dùng ArgoCD để triển khai GitOps trên K8s, tự động sync config từ repo vào cluster. Giảm thiểu sai sót manual. Có UI dashboard và history rõ ràng. Plan: đề xuất thay thế Jenkins CD hiện tại.

---

## Trekking - Hiking - Camping

Khám phá được địa điểm cắm trại ở Mộc Châu: rừng thông bản Áng, gần hồ, không khí mát mẻ, thích hợp cuối tuần. Nên đặt trước vì đông vào dịp lễ. Plan: book trip cho team 10 người tháng sau.

---

## Automation

Tìm hiểu cách viết bash script để tự động backup database và upload lên S3 với rotation policy. Kết hợp cronjob chạy hàng đêm. Plan: áp dụng cho dự án CRM của công ty.

---

## Programming

Hôm nay biết về pattern "Builder" trong Java 17, giúp tạo object immutable dễ dàng hơn so với constructor nhiều tham số. Có thể dùng Lombok `@Builder` hoặc record với builder pattern. Plan: refactor entity User trong dự án.

---

## Travel Tech

Tìm hiểu về app "PeakVisor" - nhận diện đỉnh núi qua camera, rất hữu ích khi trekking. Hỗ trợ cả 3D compass và đường đi. Data offline sau khi tải về. Plan: thử trên chuyến đi Tà Xùa.

---

## DevOps

Học cách dùng Terraform Workspaces để quản lý multi-environment (dev/staging/prod) trong cùng một repo. Giúp tránh duplicate code, dễ so sánh state. Lưu ý phải cẩn thận với backend config. Plan: refactor infra repo đang tách thành nhiều folder.

---

## Trekking - Hiking - Camping

Khám phá được địa điểm trekking mới ở Vườn Quốc gia Cúc Phương: đường mòn vào hang Đăng Mòi vừa phải, rừng già và suối mát. Nên đi từ tháng 4-6 vì mùa mưa. Plan: tổ chức team building cuối tháng 5.

---

## Automation

Tìm hiểu cách viết GitHub Action để tự động comment checklist vào PR mới, nhắc reviewer các bước cần check (test, docs, migration). Giảm thiểu sót bước thủ công. Plan: deploy cho team frontend.

---

## Programming

Hôm nay biết cách dùng `@Value` trong Spring Boot để inject properties từ nhiều nguồn (YAML, environment, command line args) với độ ưu tiên rõ ràng. Giúp config linh hoạt khi deploy multi-env. Plan: refactor config service hiện tại.

---

## Travel Tech

Biết về app "Maps.me" - bản đồ offline hoàn chỉnh, hỗ trợ hiking trails và POI ở vùng sâu. Data được tải về trước nên không cần data roaming. Giao diện trực quan, tích hợp cả tracking GPS. Plan: dùng cho chuyến trek Fansipan tới.

---

## DevOps

Học cách dùng multi-stage build trong Dockerfile để tối ưu kích thước image: build source trong một stage, copy artifact sang stage runtime chỉ với runtime dependencies. Giúp giảm từ vài trăm MB xuống vài chục MB. Lưu ý phải đặt lệnh `COPY --from=builder` đúng cách. Plan: áp dụng cho dự án Spring Boot đang deploy trên K8s.

---

## Trekking - Hiking - Camping

Khám phá được địa điểm cắm trại mới ở Đà Lạt: Đồi Đa Phú, view hồ Tuyền Lâm rất đẹp, đường vào vừa phải, phù hợp nhóm nhỏ. Cần chuẩn bị lều chống gió vì trên đồi cao. Nên đi vào sáng sớm để săn mây. Plan: lên kế hoạch cắm trại cuối tháng này cùng team.

---

## Automation

Tìm hiểu cách dùng GitHub Actions kết hợp với Dependabot để tự động rebase khi có conflict trong PR. Giảm thời gian manual maintenance. Cấu hình đơn giản với `actions/stale` và `dependabot` version updates. Plan: setup cho repo công ty để giữ branch up-to-date.

---

## Programming

Hôm nay biết về pattern "Circuit Breaker" trong Java với Resilience4j, giúp ngăn chặn cascaded failure khi downstream service không ổn định. Có thể config timeout, retry, fallback dễ dàng. Đặc biệt hữu ích cho microservice. Plan: thử nghiệm trên service payment của dự án.


## DevOps

Hôm nay tìm hiểu về cách cấu hình `docker-compose` để chạy multi-stage builds trong một file. Việc này giúp tối ưu Docker image bằng cách chỉ giữ lại các layer cần thiết cho production, giảm kích thước image đáng kể. Đặc biệt hữu ích khi muốn tách biệt môi trường build và runtime trong CI/CD pipeline.
