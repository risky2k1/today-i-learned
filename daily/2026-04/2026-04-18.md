# 2026-04-18

## DevOps

Hôm nay tìm hiểu về cách cấu hình CI/CD pipeline trên GitLab với Docker multi-stage builds để tối ưu kích thước image. Kết hợp với `.gitlab-ci.yml` để tự động test và deploy khi có commit vào branch `main`. Plan sẽ thử nghiệm trên dự án cá nhân để giảm thời gian build và cải thiện performance.


## Programming

Hôm nay tìm hiểu về `asyncio` trong Python, nhận ra việc sử dụng `async`/`await` giúp tăng hiệu năng đáng kể khi xử lý nhiều request song song. Đã thử benchmark với `aiohttp` so với `requests`, thấy tốc độ nhanh hơn gấp 3-4 lần trong điều kiện network latency cao. Plan sẽ áp dụng vào project crawler dữ liệu để giảm thời gian chạy.


## DevOps

Hôm nay tìm hiểu về cách sử dụng Docker Compose để quản lý multi-container application. Compose giúp định nghĩa và chạy nhiều container thông qua file YAML, rất tiện cho việc phát triển và testing. Mình sẽ thử áp dụng cho dự án web nhỏ đang làm để đơn giản hóa việc setup môi trường.

- `docker-compose.yml` cho phép khai báo services, networks, volumes.
- Lệnh `docker-compose up` start toàn bộ stack chỉ trong 1 command.


## Programming

Hôm nay tìm hiểu về cách sử dụng `asyncio.run()` trong Python 3.7+ để chạy các coroutine một cách đơn giản hơn, thay vì phải tự tạo event loop. Điều này giúp code trở nên gọn gàng và dễ đọc hơn, đặc biệt cho các script nhỏ. Mình sẽ thử áp dụng vào script crawler dữ liệu để cải thiện performance.


## DevOps

Hôm nay tìm hiểu về `kubectl debug`, một feature mới giúp debug pod mà không cần thay đổi deployment. Có thể attach vào pod đang chạy hoặc tạo pod debug tạm thời với cùng network namespace. Rất tiện cho trường hợp pod bị crash hoặc không thể exec vào vì thiếu tools. Plan sẽ thử áp dụng trong môi trường staging để rút ngắn thời gian debug production.
