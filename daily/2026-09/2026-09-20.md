# 2026-09-20

## DevOps

Hôm nay mình học được rằng `docker compose down -v` sẽ xóa cả volume, nên chỉ nên dùng khi thật sự muốn reset dữ liệu. Nếu chỉ muốn dừng service thì dùng `docker compose down` là đủ.  
Plan: thêm cảnh báo vào script local để tránh chạy nhầm lệnh xóa data.
