# 2026-09-13

## Automation
- Hôm nay tôi học cách dùng GitHub Actions để tự động chạy linter và test mỗi khi push code vào nhánh develop.  
- Thiết lập workflow yaml với job lint sử dụng `actions/setup-node` và chạy ESLint, sau đó job test chạy Jest.  
- Cá nhân tôi thấy điều này giúp giảm lỗi sót và tăng độ tin cậy của CI/CD; tuần tới tôi plan mở rộng thêm step build Docker image và push lên registry khi release tag được tạo.


## Automation
- Hôm nay tôi học cách sử dụng GitHub Actions để tự động chạy lint và test mỗi khi push code.  
- Khi cấu hình workflow, mình thấy việc dùng matrix strategy giúp test trên nhiều phiên bản Node.js dễ dàng hơn.  
- Cá nhân thấy rất tiện, sẽ áp dụng cho dự án side‑project tiếp theo để giảm thời gian kiểm tra thủ công.


## DevOps

Hôm nay mình học được cách dùng `docker-compose` để quản lý nhiều container cùng lúc, thay vì phải run từng container riêng lẻ. Rất tiện khi triển khai ứng dụng gồm nhiều service như backend, database và redis.

- Dùng `docker-compose up -d` để khởi động toàn bộ service ngay cả khi terminal đã đóng.
- File `docker-compose.yml` giúp team đồng bộ hoá môi trường phát triển dễ dàng hơn.

Mình sẽ áp dụng cách này vào dự án API sắp tới để tối ưu thời gian build môi trường.


## Trekking - Hiking - Camping

Hôm nay em nắm vững mẹo "Leave No Trace" cụ thể cho các con đường leo đồi Việt Nam, đặc biệt là cách bỏ rác rác thải biến biện và sử dụng bã cà phê kháng cú hạp trên đồng cỏ Sapa.

- Áp kế hoạch này trên chuyến trekking tới Fansipan vào cuối tháng.


## Automation

Tôi vừa phát hiện ra rằng sử dụng `cron` kết hợp với `systemd timers` giúp đơn giản hóa việc lập lịch tác vụ nền trên các máy chủ Linux hiện đại. `systemd timers` cung cấp khả năng tích hợp tốt hơn, hỗ trợ múi giờ và ghi nhật ký chi tiết hơn so với cron truyền thống. Nhờ đó, việc triển khai các tác vụ định kỳ giờ đây trở nên đáng tin cậy và dễ theo dõi hơn nhiều.

*Bài học này giúp tôi dễ dàng hơn trong việc thiết lập các job định kỳ cho các project mới trong tương lai.*


## DevOps

Hôm nay tìm hiểu thêm về `docker image prune --filter "until=24h"` — giúp xóa các image không có tag (dangling) cũ hơn 24h mà không ảnh hưởng image đang dùng. Trước giờ hay để Docker chiếm hết ổ cứng vì quên dọn dẹp.

👉 Áp dụng luôn vào cron job trên VPS mỗi đêm để tránh hết disk khi deploy liên tục.


## Automation
- Hôm nay thử dùng GitHub Actions để auto deploy site tĩnh khi push lên `main`, thay vì cứ phải FTP thủ công mỗi lần sửa CSS.
- Chỉ cần 1 file `.github/workflows/deploy.yml` vài dòng là xong, nhanh hơn hẳn cách cũ.
- Định áp dụng luôn cho mấy project cá nhân để đỡ quên bước deploy.


## Tự động hóa

Hôm nay tôi đã thử nghiệm việc sử dụng `cron` cùng với `tmux` để lên lịch tác vụ dọn dẹp nhật ký tự động trong dự án backend. Nhờ ghi nhật ký vào một tệp tạm thời được chia sẻ và để script `cron` chạy ở chế độ nền, quá trình dọn dẹp giờ đây được thực hiện sau mỗi giờ mà không cần giám sát thủ công. Kế hoạch trong tuần tới là mở rộng script để tích hợp với công cụ giám sát, giúp tự động gửi cảnh báo khi dung lượng đĩa vượt quá ngưỡng cho phép.




## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)

*   **Hôm nay mình học được rằng** khi đi trekking ở các vùng núi sâu như Ta Nung (Đà Lạt) hay các cung đường ở Hà Giang, tín hiệu di động có thể mất hoàn toàn ở những thung lũng sâu, khiến tính năng định vị real-time trên Google Maps trở nên vô dụng nếu không có bản đồ offline.
*   **Lưu ý quan trọng:** Nên tải xuống sẵn bản đồ ngoại tuyến (offline maps) trước khi lên đường, và mang theo dự phòng pin dự phòng vì chạy GPS ở chế độ offline tiêu hao nhiều pin.
*   **Điều thú vị:** Dù có sự chuẩn bị kỹ về công nghệ, sự chuẩn bị về thể lực và trang bị giày leo núi tốt vẫn là yếu tố then chốt để tận hưởng trọn vẹn vẻ đẹp của thiên nhiên Việt Nam.

*   *Cá nhân:* Mình đang lên kế hoạch áp dụng mẹo tải bản đồ offline này cho hành trình sắp tới tại dãy Hoàng Liên Sơn, để không phải lo mất định vị giữa đường mòn mờ.
