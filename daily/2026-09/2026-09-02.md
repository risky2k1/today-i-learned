# 2026-09-02

## Programming

- Hôm nay mình phát hiện ra trong Python làn đề `enumerate()` có thể nhận tham số `start` để bắt đầu chỉ số từ giá trị khác nhau, nhiều dự án dùng để đánh số lại danh sách con.
- Với `enumerate(items, start=1)`, chỉ số đếm bắt đầu từ 1 thay vì mặc định 0, giúp code đọc hiểu hơn khi làm việc với người dùng cuối.
- Mình đang cải thiện script xử lý dữ liệu bằng cách chuyển từ vòng lặp `for i in range(len(items))` sang `for index, item in enumerate(items, start=1)`.


## DevOps

Hôm nay mình học được cách sử dụng `systemd` để quản lý service trên Linux thay vì dùng `supervisor` như trước. `systemd` được tích hợp sẵn trên hầu hết các distro hiện nay, hỗ trợ tự động khởi động lại khi service lỗi và quản lý log thông qua `journalctl` rất tiện.

- Cấu hình service chỉ cần tạo file `.service` trong `/etc/systemd/system/`
- Dùng `systemctl enable/start/status` để quản lý vòng đời service

Mình sẽ thử áp dụng cấu hình này cho dự án Node.js đang chạy trên máy chủ ảo next week.


## DevOps

Hôm nay mình phát hiện ra `docker system prune` chỉ xóa các resource **đang không được sử dụng** (stopped containers, dangling images, unused networks), chứ không động đến những image đang được container tham chiếu. Muốn dọn mạnh hơn thì phải thêm flag `--all` (hoặc `-a`) để xóa cả image không có container nào dùng. Hay ho hơn nữa là `docker system df` cho xem chi tiết dung lượng đang chiếm theo từng loại — rất tiện để debug lúc ổ disk trên server sắp hết.

*Plan: thêm alias `dclean="docker system prune -af --volumes"` vào `.zshrc` và dùng hàng tháng thay vì `docker rm -f $(docker ps -aq)` như trước.*


## DevOps
- Hôm nay tôi tìm hiểu về **GitHub Actions** và cách cấu hình pipeline tự động để build và test code.  
- Việc sử dụng **caching** giúp giảm thời gian build đáng kể, đặc biệt khi chạy nhiều commit liên tiếp.  

Tôi sẽ thử triển khai pipeline này vào dự án hiện tại vào tuần tới.


## Programming  
- Hôm nay mình phát hiện thú vị khi dùng `npx` để chạy các lệnh CLI từ các package npm mà không cần cài đặt toàn cục. Ví dụ như `npx live-server` để chạy server phát triển nhanh chóng cho dự án static.  
- Điều này giúp giảm độ trùng lặp phiên bản và giảm độ phức tạp khi phát triển thử nghiệm.  
- Cá nhân mình sẽ thử áp dụng `npx` cho các công cụ thường ngày như `eslint` hoặc `prettier` khi không muốn thêm vào dependencies chính thức của dự án.


## Programming

Hôm nay mình phát hiện ra `Array.prototype.flat()` trong JS có thể truyền tham số `depth` để chỉ định mức độ làm phẳng, thay vì phải map thủ công nhiều lần. Ví dụ `[1, [2, [3, [4]]]].flat(2)` sẽ ra `[1, 2, 3, [4]]` thay vì flat hết một lần.

```js
// Trước đây mình hay làm thế này
const result = arr.map(x => x.name).filter(Boolean).flat();
// Giờ biết thêm flatMap để gộp luôn
const result = arr.flatMap(x => x.name ? [x.name] : []);
```

Cá nhân thấy `flatMap` khá tiện khi xử lý data có điều kiện, vừa map vừa filter trong cùng một bước — code gọn hơn rõ rệt so với chain `map().filter().flat()`. Plan sắp tới sẽ refactor mấy chỗ đang dùng `reduce` rườm rà trong dự án.
