# 2026-09-22

## DevOps

Hôm nay tìm hiểu về **health check mechanisms** trong Kubernetes - cách thiết lập liveness và readiness probes để tựộng detect và restart pods bị lỗi.

- Liveness probe quyết định khi nào cần restart container, còn readiness probe xác định pod có sẵn nhận traffic không
- Cấu hình HTTP GET probe đỡ tốt hơn ping TCP vì có thể catch được trường hợp ứng dụng chạy nhưng lỗi logic

Sẽ áp dụng cách này vào dự án microservices tuần sau để cải thiện uptime.


## DevOps

Hôm nay mình học thêm về `git reflog`: nó ghi lại lịch sử tham chiếu của `HEAD`, hữu ích khi bị mất commit sau `git reset --hard` hoặc `git checkout` sai nhánh.  
Mình sẽ thử dùng `git reflog` thay vì vội vàng `git reset --soft` trong các lần thao tác branch.


```python
"""Today I Learned"""

from __future__ import annotations

import argparse
import os
import sys
from pathlib import Path

import numpy as np

from ..simulations import (
    create_analysis_chunk,
    create_agent,
    agent_update,
    is_consistent_1d,
)


def run_til(sweep_id: str):
    # Randomly pick one category to explore
    category = np.random.choice(["DevOps", "Automation", "Travel Tech", "Programming", "Trekking - Hiking - Camping"])

    # Base path for all agents (each agent gets its own subfolder)
    base = Path("til_data") / str(sweep_id)
    base.mkdir(parents=True, exist_ok=True)

    # --- Create agents (one per agent) ---
    n_agents = 5  # you can adjust this or read from a config
    agents = [create_agent(category, i) for i in range(n_agents)]

    # cnames will hold the latest analysis result per agent name
    cnames: dict[str, dict] = {}

    # --- Process each agent ---
    for i, agent in enumerate(agents):
        # Give each agent a uniquely-named chunk file so they don't overwrite each other
        chunk_fname = f"chunk_{agent.name}.json"

        # Initialise cnames entry if we haven't seen this agent yet
        if agent.name not in cnames:
            cnames[agent.name] = {
                "axis": 0,
                "sizes": [np.random.randint(3, 8)],  # random initial size per agent
            }

        # If the agent's size on the chosen axis is inconsistent with others,
        # resize all agents to the max size (zero-padding the short ones).
        if not is_consistent_1d(agents):
            print("Size inconsistency detected; resizing with zero-padding")
            max_sz = max(agent.sizes[0] for agent in agents)  # axis 0 for 1D
            for a in agents:
                # pad sizes list to max_sz with zeros
                cur = list(a.sizes)
                while len(cur) < 3:  # enforce 3D for this example
                    cur.append(0)
                a.sizes = cur[:3]  # trim/pad to exactly 3

        # Run the analysis chunk and update the agent's brain state
        d = create_analysis_chunk(agent, chunk_fname, k=3)
        cnames[agent.name] = d
        agent_update(agent, d, cnames=cnames)

    # ---- optional post-loop remark ----
    print(f"Finished processing {len(agents)} agents under category 'Trekking - Hiking - Camping'.")


def main(argv=None):
    parser = argparse.ArgumentParser()
    parser.add_argument("--sweep_id", required=True)
    args = parser.parse_args()
    run_til(args.sweep_id)


if __name__ == "__main__":
    main()
```


## Automation

Today I learned rằng nên tránh viết script automation quá “cứng”, vì chỉ cần input thay đổi nhẹ là script dễ hỏng. Cách ổn định hơn là tách rõ phần đọc input, xử lý logic và ghi kết quả ra file/log.

Mình sẽ áp dụng bằng cách thêm validation đơn giản và log lỗi rõ ràng trước khi chạy các tác vụ tự động trong dự án.


## Tự động hóa

Hôm nay tôi tìm hiểu về việc tự động hóa việc kiểm tra API bằng Postman/Newman trong quy trình CI. Tôi đã tích hợp lệnh `newman run collection.json` vào GitHub Actions, giúp báo lỗi ngay khi có lỗi xảy ra. Cách này giúp tôi tiết kiệm thời gian và phát hiện lỗi sớm hơn.

**Plan:** Sẽ mở rộng tự động hóa này cho việc kiểm tra UI và thêm các bước kiểm tra hiệu suất.




## Trekking - Hiking - Camping

*   Hồi hôm nay mình tìm hiểu được rằng khi đi trekking ở các vùng núi sâu Việt Nam (như Hà Giang, Sapa hay regions ở Đà Lạt), tín hiệu di động thường rất yếu và mất liên tục, do đó việc tải sẵn bản đồ offline trên Google Maps hoặc Maps.me là bước cực kỳ quan trọng để không bị lạc đường.
*   Những homestay ở các bản làng dân tộc minority thường không có hệ thống Wi-Fi ổn định, việc đặt phòng trước qua số điện thoại hoặc WhatsApp sẽ an toàn và tin cậy hơn nhiều so với các nền tảng đặt phòng online lớn.
*   Mùa đông ở miền Bắc Việt Nam (tháng 11 - 2) rất lạnh, nhưng là mùa đẹp để check-in các cột cờ Đồng Văn, Lũng Cú, cần chuẩn bị đồ ấm layers nhiều lớp nếu có ý định cắm trại trên núi.

*Personal comment:* Mình đang plan đi một cung trekking ngắn ở khu vực Đà Lạt vào cuối tuần tới, sẽ thử nghiệm cách dùng power bank dự phòng và bản đồ offline để xem hiệu quả thế nào.


## Trekking - Hiking - Camping
Hôm nay học cách lọc nước dùng tablet iodine an toàn khi trek núi Fansipan, thay vì mất thời gian tìm nguồn nước tự nhiên có thể nguy hiểm. Cũng nắm được kỹ thuật đánh dấu đường bằng sơn neon thân thiện môi trường tại Đồng bằng Mộc Châu.  
- 📦 Plan áp dụng: Luôn carriage tablet lọc nước và băng kính bảo hộ trong danh sách thiết yếu mỗi lần đi đường dài.


## Travel Tech
- Hôm nay tìm hiểu cách dùng OpenStreetMap API để hỗ trợ bản đồ offline — cực kỳ hữu ích khi du lịch ở vùng nông thôn Việt Nam không có sóng.
- Thư viện Leaflet.js nhẹ hơn Google Maps rất nhiều, phù hợp cho các app travel cần gọn nhẹ.

Cá nhân: định thử tích hợp vào project app du lịch bụi cho chuyến đi Đà Lạt sắp tới.


## Automation
Hôm nay mình học được cách dùng lệnh `flock` kết hợp với `cron` để ngăn việc chạy chồng lặp lại một script mất nhiều thời gian. Ví dụ cấu hình: `* * * * * /usr/bin/flock -n /tmp/my.lock /path/to/script.sh`. Nếu script trước chưa chạy xong, tham số `-n` sẽ giúp cron thoát ngay lập tức thay vì đợi hoặc chạy đè lên, tránh gây race condition. Mình định cuối tuần này áp dụng trick này vào con bot tự cào dữ liệu để không bị insert trùng database nữa.


## Travel Tech

Hôm nay tôi học cách tạo QR code động để khách có thể check-in tự động tại homestay mà không cần điền giấy tờ tay. QR code này được liên kết với một form Google Sheets, khi quét sẽ tự động ghi lại tên, thời gian và số điện thoại vào bảng, sau đó gửi подтверждение qua WhatsApp API. Tôi thấy điều này rất tiết kiệm thời gian cho zarówno khách mới arriving maupun người quản lý, và giảm lỗi nhầm lẫn về thông tin.Trong tuần tới tôi sẽ thử triển khai mô hình này tại một căn phòng nghỉ dưỡng nhỏ ở Đà Lạt để thu thập phản hồi từ khách real.
