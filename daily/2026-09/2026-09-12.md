# 2026-09-12

## DevOps
Hôm nay tôi đọc bài hướng dẫn cách cấu hình GitLab CI/CD để tự động chạy test unit, integration test và deploy lên staging chỉ trong vài phút. Pipeline được kích hoạt tự động khi có commit mới, giúp team không cần can thiệp thủ công. Tôi cũng thấy lợi ích khi kết nối với GitHub Actions để chia sẻ code base giữa các dự án.

*Ý kiến cá nhân:* Sẽ thử triển khai pipeline này vào dự án hiện tại vào tuần tới để đo lường thời gian release thực tế.


## Programming

Hôm nay mình mới biết đến `Array.prototype.at()` trong JavaScript — cho phép dùng chỉ số âm để truy cập phần tử từ cuối mảng luôn, ví dụ `arr.at(-1)` thay vì `arr[arr.length - 1]`. Tiện thật, trước mình luôn phải viết vậy rồi lại dễ bị bug nếu mảng rỗng.

- Cú pháp gọn hơn, dễ đọc hơn rõ ràng
- Hỗ trợ cả chỉ số dương và âm
- Hỗ trợ trên hầu hết các browser hiện đại

Mình định rà soát lại vài project cũ để thay thế dần, code sẽ sạch hơn một chút. 🚀




## DevOps

*   Tìm thấy công cụ cực kỳ hay gọi là `dive` (Docker Image Viewer), giúp phân tích chi tiết từng layer của Docker image trực quan ngay trên terminal.
*   Nhờ đó có thể dễ dàng kiểm tra dung lượng của từng layer và phát hiện ra những file hoặc lệnh `RUN` không cần thiết làm tăng kích thước ảnh unnecessarily.
*   Mình plan sẽ cài thử công cụ này và quét qua Dockerfile của dự án hiện tại để tối ưu kích thước ảnh, giúp quá trình pull và deploy trên server được nhanh hơn đáng kể.


## DevOps

Hôm nay mình học lại cách tối ưu cache trong Docker build: nên `COPY` phần file cấu hình dependency trước, rồi mới copy source code, để mỗi lần sửa code nhỏ thì Docker không phải rebuild cả mảng package. Cách này giúp build nhanh hơn và tiết kiệm băng thông khi đẩy image lên registry.

Mình sẽ áp dụng ngay với các project backend đang build bằng Dockerfile.


## Trekking - Hiking - Camping

- Hôm nay biết thêm rằng đỉnh Phan Xi Păng (Sa Pa, Lào Cai) là đỉnh núi cao nhất Việt Nam ở độ cao ~3.143m, và đường trek lên đỉnh thường mất khoảng 2 ngày nếu xuất phát từ cổng Fansipan Legend.
- Vào mùa mưa (tháng 5–9), mây phủ kín đỉnh nên rất khó nhìn thấy view, nên plan đi vào mùa khô (tháng 10–3) sẽ đẹp hơn nhiều.
- Cần chuẩn bị giày chống trơn tốt vì đoạn cuối leo đá khá dốc và trơn.

→ *Plan: Tích lũy sức khỏe đèo dốc cuối tuần ở các đồi quanh Hà Nội, rồi đặt lịch trek Phan Xi Păng vào đầu mùa khô năm sau.*


## DevOps

- Học được cách cấu hình **GitHub Actions** để tự động chạy build, test và deploy mỗi khi có commit mới.  
- Áp dụng **Terraform** để quản lý hạ tầng AWS theo mô hình Infrastructure‑as‑Code, giúp môi trường đồng bộ hơn.  
- Thử nghiệm **Argo CD** cho continuous deployment lên cluster Kubernetes, rút ngắn thời gian rollout.  

Mình sẽ thử tích hợp GitHub Actions vào pipeline hiện tại trong tuần tới để giảm thời gian build.
