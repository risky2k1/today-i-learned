# 2026-09-10

## Automation
Hôm nay tìm ra cách tối ưu GitHub Actions bằng `cache` cho node_modules và `matrix` chạy song song trên ubuntu/windows/mac. Thay vì chạy tuần tự, dùng `strategy.matrix` giúp giảm thời gian CI từ 12 phút xuống còn 4 phút. Cần nhớ khai báo `key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}` để tránh cache lỗi giữa các OS.

*Plan: Áp dụng cho repo cá nhân trước, rồi mở rộng sang project công ty.*


## Travel Tech  

- Hôm nay tôi tìm hiểu về cách sử dụng API của Google Maps để tính toán tuyến đường tối ưu cho các tour du lịch nội địa, bao gồm cả chỉ số tránh cường độ giao thông cao.  
- Với thư viện **googlemaps** trong Python, tôi có thể lấy được thời gian di chuyển thực tế và gợi ý các điểm dừng chân atractif dựa trên đánh giá của người dùng.  
- Kế hoạch áp dụng: tích hợp tính năng này vào ứng dụng quản lý hành trình cá nhân để tự động đề xuất các con đường trekking đẹp mà ít người biết đến ở miền Trung Việt Nam.


## Automation

- Hôm nay tôi tìm hiểu cách sử dụng GitHub Actions để tự động push ứng dụng Python đã build vào Docker Hub mỗi khi có push mới.
- Tôi nhận ra rằng việc viết pipeline dưới dạng file YAML giúp tôi dễ dàng chỉnh sửa, version control và replicate trên nhiều repository.
- Tôi dự định thêm giai đoạn kiểm tra unit test vào workflow để đảm bảo chỉ những thay đổi đạt yêu cầu mới được triển khai.


## DevOps

Hôm nay học lại về **SLO/SLI**: SLO nên gắn với trải nghiệm người dùng thực tế, ví dụ tỷ lệ request thành công hoặc thời gian phản hồi, thay vì chỉ đo “server có sống không”.  
Mình thấy cách dùng **error budget** khá hữu ích để cân bằng giữa tốc độ release và độ ổn định: nếu budget sắp hết thì ưu tiên sửa lỗi hơn là thêm tính năng mới.  
Có thể áp dụng ngay cho các service quan trọng bằng cách dashboard đơn giản + alert khi burn rate tăng nhanh.


## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)

Hôm nay mình khám phá ra **Vườn Quốc gia Núi Chúa (Ninh Thuận)** đáng sợ hơn nhiều so với những gì mình tưởng — địa hình chủ yếu là đồi sa mạc hoang sơ, không có trail rõ ràng nên phải đi theo vết thú hoặc dùng GPS offline. Mình đã tải bản đồ offline của Maps.me trước khi đi và lưu các điểm mốc water refill, vì ở đây rất ít chỗ bán nước.

> 📌 *Plan áp dụng:* Lần sau sẽ chuẩn bị sẵn file GPX path offline và kiểm tra kỹ dung lượng pin power bank trước khi chinh phục các cung đường không có sóng điện thoại như thế này.


## Travel Tech

Hôm nay mình tình cờ phát hiện ra cách tải trước bản đồ offline trên Google Maps cho các vùng quê Việt Nam, đặc biệt hữu ích khi đi qua những cung đường miền núi mà mạng yếu hoặc không có sóng. Chỉ cần search tên tỉnh/thành phố, chọn "Tải xuống" là Google Maps sẽ lưu cả vùng khá chính xác, giúp tiết kiệm dữ liệu khi di chuyển bằng xe máy. Mình định áp dụng ngay cho chuyến đi Đà Lạt cuối tuần này để test xem độ chính xác của bản đồ offline ở vùng nông thôn có đủ tin cậy hay không.


## Automation

Hôm nay mình tình cờ biết thư viện `schedule` trong Python rất nhẹ cho việc chạy cron job đơn giản, không cần phụ thuộc hệ thống hay crontab. Chỉ cần vài dòng: `schedule.every().day.at("07:00").do(job)` + loop `run_pending()` là có background task chạy ổn. Mình đang tính áp dụng luôn để tự động check giá phòng khách sạn cho đợt đi Đà Lạt tuần sau — tiện hơn ngồi check tay mỗi sáng.
