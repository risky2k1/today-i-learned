# 2026-09-23

## Trekking - Hiking - Camping
Mình vừa thử lên lịch leo Cù Huốc, nhận ra việc chia ra 3 ngày nhẹ nhàng giúp kiểm soát tốc độ phục hồi tốt hơn nhiều. Dự định lần tới sẽ chuẩn bị thêm sổ ghi lại độ dốc, thời tiết và cảm giác mỗi đoạn để tối ưu lộ trình sau này.


## Automation

Hôm nay tôi tìm hiểu về cách sử dụng **GitHub Actions** để tự động hóa quy trình CI/CD cho một dự án Python nhỏ. Tôi đã tạo một workflow đơn giản để mỗi lần push code lên branch `main` sẽ tự động chạy các bước kiểm tra, xây dựng Docker image và triển khai lên server test chỉ với vài cú click. Tôi dự định sẽ mở rộng workflow này để bao gồm kiểm tra bảo mật và tự động cập nhật tài liệu dự án.


## Automation

- Tôi vừa khám phá ra cách sử dụng **GitHub Actions** cùng với **Docker Compose** để tự động hóa quy trình CI/CD cho một microservice nhỏ trên nền tảng Kubernetes trong tầm tay.
- Bằng cách kết hợp bộ tiền xử lý `on: push` với một workflow duy nhất, giờ đây mỗi khi tôi đẩy code lên nhánh `main`, container sẽ được xây dựng, đẩy lên registry và triển khai tự động—không cần phải chờ đợi thủ công.
- Điều này giúp tôi tiết kiệm khoảng 30 phút mỗi tuần và giảm thiểu sai sót do tác động của con người, giúp tôi có thêm thời gian để tập trung vào việc viết code sạch hơn.


## Automation

Mới biết lại rằng GitHub Actions có thể dùng `paths` và `paths-ignore` trong `on.push` để chỉ kích hoạt workflow khi có thay đổi ở các file/thư mục cụ thể, tránh chạy lãng phí khi chỉnh docs hay comment. Ví dụ: chỉ build + deploy khi thay đổi file trong `src/` mà bỏ qua `README.md`.

> Cá nhân: áp dụng luôn cho repo hiện tại để giảm số lần chạy workflow thừa, tiết kiệm minute trên GitHub Free tier.


## Automation

Hôm nay mình tìm hiểu về `matrix strategy` trong GitHub Actions và thấy nó cực kỳ tiện lợi khi cần chạy test trên nhiều phiên bản Node.js cùng lúc. Thay vì phải viết riêng 3 workflow cho mỗi phiên bản, chỉ cần một file YAML duy nhất là đã có thể cover hết rồi. Mình định áp dụng ngay cho mấy repo cá nhân để tiết kiệm thời gian bảo trì CI/CD.


## Automation

- Tôi đã tích hợp thành công một job Linux bash tự động dọn dẹp các bản dựng Docker cũ trong hệ thống CI/CD bằng cách sử dụng cron scheduler.
- Script now includes email alerts when a cleanup run completes, giúp tôi nắm được tình trạng hệ thống một cách dễ dàng.
- Kế hoạch trong tuần này là mở rộng script để giám sát các container không hoạt động và tự động xóa chúng, từ đó giảm chi phí lưu trữ.
