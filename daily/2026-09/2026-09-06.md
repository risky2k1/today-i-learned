# 2026-09-06

## Trekking - Hiking - Camping

Hôm nay biết thêm rằng tại Pu Luong, nhiều trekking dùng Maps.me offline để định vị đồi núi mà không cần pin GPS, rất tiết kiệm cho chuyến dài.

*(Plan áp dụng)*: Tôi sẽ thử dùng cách này cho chuyến tới Cát Bà vào tháng tới.


## Programming

Hôm nay mình biết được rằng trong JavaScript, `Array.prototype.flat()` có thể nhận tham số `depth` để kiểm soát mức độ "làm phẳng" mảng lồng nhau — ví dụ `[[[1]]].flat(2)` trả về `[1]`. Trước đây mình hay dùng `flat(Infinity)` một cách khá tùy tiện mà không nghĩ tới performance.

- Mặc định `depth = 1`, không phải làm phẳng toàn bộ như mình tưởng.
- Nếu chỉ cần 1-2 level, truyền số cụ thể sẽ nhanh hơn đáng kể so với `Infinity`.

Plan áp dụng: khi review code, mình sẽ để ý các chỗ dùng `flat()` để đề xuất chỉ định `depth` phù hợp thay vì để mặc định.


## Trekking - Hiking - Camping

Hôm nay học cách tải bản đồ offline và đặt waypoint an toàn trên Gaia GPS để định vị chính xác trên Fansipan khi không có mạng di động, cùng lưu vùng map theo từng xã phường để tiết kiệm pin và dữ liệu.

Cách này sẽ giúp chuyến trekking Sapa vào mùa mưa an toàn và không bị lạc tuyến. Tôi sẽ áp dụng cho chuyến đi Làng Nước Tiếng vào cuối tháng.


## Travel Tech

- Khi đi du lịch Việt Nam gần đây, tôi đã phát hiện ra một ứng dụng mã nguồn mở tên là **VietMap** cung cấp bản đồ ngoại tuyến chi tiết cho cả thành phố và vùng nông thôn, giúp loại bỏ hoàn toàn việc phụ thuộc vào dữ liệu di động.
- Ứng dụng này tích hợp sẵn thông tin về các địa điểm du lịch địa phương, quán ăn và điểm đỗ xe, đồng thời cho phép người dùng lưu các tuyến đường trước khi đi để tiết kiệm pin.
- Tôi sẽ bắt đầu sử dụng VietMap cho tất cả các chuyến đi tiếp theo và khuyến nghị cho bạn bè vì nó thực sự giúp trải nghiệm du lịch trở nên liền mạch hơn.




## Programming

Hom nay mình học được về match case trong Python 3.10, giúp code điều kiện ngắn gọn và dễ đọc hơn. Mình sẽ áp dụng ngay vào dự án hiện tại. Đây là bước nhỏ để cải thiện hiệu suất.


## Automation

Hôm nay tôi khám phá cách dùng **GitHub Actions** để tự động chạy bộ test mỗi khi có pull request mới vào nhánh develop. Thiết lập workflow đơn giản chỉ với vài dòng YAML giúp tiết kiệm thời gian và giảm lỗi do quên test thủ công. Tôi plán áp dụng này cho dự án nội bộ tiếp theo để CI/CD mượt hơn.


## Programming  
- Hôm nay mình khám phá ra lợi ích của việc dùng `async/await` trong Python để xử lý đồng thời nhiều API requests, thay vì dùng vòng lặp bất đồng bộ truyền thống.  
- Kết hợp với `aiohttp`, tốc độ gọi API tăng đáng kể (khoảng 3-4x so với `requests`), đặc biệt hữu ích khi crawl dữ liệu lớn.  
- Mình sẽ thử áp dụng vào script tự động đồng bộ hóa dữ liệu từ nhiều nguồn trong dự án hiện tại.  

Cảm thấy việc học sâu một khái niệm như async/await không chỉ giúp code chạy nhanh hơn mà còn hiểu sâu hơn về cách bộ nhớ và I/O hoạt động. Cần dành thời gian để ôn lại lý thuyết về event loop và non-blocking I/O.


## Trekking - Hiking - Camping

- Hôm nay mình thử cách dựng trại tạm tại khu vực Đà Lạt, dùng lều nhựa nhẹ và bếp campstove mini.  
- Khám phá được rằng việc lắp ráp lều trong thời tiết mưa nhẹ cần chú ý đến góc lắp và cách chênh cách gỗ để tránh ướt.  

Sẽ áp dụng kinh nghiệm này vào chuyến trekking kế tiếp ở Phong Thổ.


User Safety: safe


## Automation

Hôm nay mình tình cờ biết đến công cụ `entr` — một tiện ích line-based cho phép tự động chạy lệnh mỗi khi có file thay đổi trong thư mục. Thay vì phải viết `inotifywait` kết hợp vòng lặp `while`, mình chỉ cần một câu lệnh gọn gàng: `ls *.py | entr -c pytest`. Mình định thử áp dụng luôn cho workflow chạy test lokal mỗi lần edit code, xem có mượt hơn `watchman` không.
