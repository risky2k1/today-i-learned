# 2026-09-09

## Automation

Hôm nay tôi đã học cách tạo một workflow GitHub Actions tự động build và push Docker image lên Docker Hub mỗi khi có commit nhánh `main`.  
- Sử dụng `actions/checkout` để lấy mã nguồn, sau đó `docker/build-push-action` với các input `tags: latest`, `push: true`.  
- Cần lưu trữ `DOCKERHUB_USERNAME` và `DOCKERHUB_TOKEN` dưới dạng secret trong repo để workflow có thể xác thực.  

Nhận xét: Đây là một cách rất tiện lợi để giảm thursday việc triển khai thủ công, tôi định áp dụng ngay cho dự án microservice đang phát triển.


## DevOps

Hôm nay mình học lại cách Docker tận dụng cache khi build image: nếu một bước chưa có thay đổi thì Docker sẽ bỏ qua và dùng kết quả cũ, giúp CI chạy nhanh hơn đáng kể. Lần tới mình sẽ để các bước ổn định như cài dependency trước, rồi mới copy source code và build app.


## Trekking - Hiking - Camping

- Hôm nay tôi đã nắm bắt kỹ thuật lắp lều 2 người chỉ trong 3 phút bằng cách sử dụng dây kéo và găng tay, thay vì cách truyền thống mất 10 phút.  
- Tôi còn thử nghiệm cách nấu ăn nhanh với bình yên nến, giúp bữa ăn trong môi trường high altitude không tốn quá nhiều thời gian.  
- Kế hoạch áp dụng: Sử dụng kỹ thuật này cho chuyến trekking lên Fansipan vào cuối tuần.


User Safety: safe


## Trekking - Hiking - Camping

Thử dùng ứng dụng offline Maps.me cho đoạn trekking Sapa 12km hôm nay, không cần lo về mạng điện thoại vẫn có thể định vị chính xác và theo dõi elevation.

💡 Plan áp dụng: Mỗi lần lên kế hoạch đi núi, sẽ tải bản đồ vùng + kiểm tra thời tiết monson, và luôn mang power bank dự phòng.


## DevOps

- Vừa tìm hiểu về cơ chế *Graceful Shutdown* trong Kubernetes để tránh mất request khi deploy bản mới.
- Cần cấu hình `terminationGracePeriodSeconds` và xử lý signal `SIGTERM` trong code ứng dụng để đóng các connection an toàn.

Ngày mai sẽ thử apply vào project hiện tại để xem có giảm được tỉ lệ lỗi 5xx khi rolling update không.


## Travel Tech

Hôm nay mình học thêm rằng các nền tảng du lịch thường dùng **dynamic pricing** và **inventory-based ranking** để sắp xếp kết quả tìm kiếm theo giá, độ phủ suất và thời điểm đặt phòng. Với app du lịch, việc ưu tiên hiển thị “chỗ ở phù hợp nhất” đôi khi quan trọng hơn là chỉ sort đơn giản theo giá tăng dần.

Mình có thể áp dụng idea này khi thiết kế feature gợi ý địa điểm: kết hợp giá, khoảng cách, rating và hành vi tìm kiếm của người dùng.


## Automation

Hôm nay mình học được là có thể dùng `make` như một layer đơn giản để gom các lệnh lặp lại trong dự án, thay vì phải nhớ dài dòng `docker compose`, `npm run`, hay script nội bộ. Ví dụ chỉ cần gõ `make dev`, `make test`, `make deploy` là chạy đúng flow mong muốn. Plan là mình sẽ thêm `Makefile` cho mấy repo cá nhân để onboarding nhanh hơn và giảm lỗi do gõ sai lệnh.
