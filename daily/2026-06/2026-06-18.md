# 2026-06-18

## DevOps
- Hôm nay mình thử cấu hình CI/CD pipeline bằng GitHub Actions cho dự án Dockerfile, học được cách viết workflow YAML để tự động build và push image.
- Nhận ra lợi ích của caching layer trong Docker, giảm thời gian build khoảng 40% so với cách nhanh trước.
- Kế hoạch: triển khai pipeline cho môi trường staging vào tuần tới.


## DevOps

- Vừa tìm hiểu về cơ chế **GitOps** với ArgoCD, thấy cách nó tự động đồng bộ state giữa Git repo và Kubernetes cluster thực sự giúp quản lý deployment an toàn hơn nhiều.
- Việc tách biệt config ra khỏi code giúp rollback nhanh chỉ bằng một lệnh `git revert`.

Chắc chắn sẽ áp dụng mô hình này vào project hiện tại để bớt phải gõ lệnh `kubectl apply` thủ công.
