# 2026-06-20


## [Programming]

Hôm nay mình phát hiện `functools.lru_cache` trong Python giúp tối ưu hóa hàm bằng cách lưu trữ kết quả trả về dựa trên tham số đầu vào, rất hữu ích cho những hàm tính toán tốn thời gian hoặc xử lý dữ liệu lặp lại. Chỉ cần thêm decorator này vào hàm là mình có thể giảm thiểu việc gọi lại hàm không cần thiết, đồng thời kiểm soát kích thước cache thông qua tham số `maxsize`.  
- Áp dụng cho hàm đệ quy hoặc xử lý API giúp cải thiện hiệu năng đáng kể.  
- Cần lưu ý rằng hàm phải có tham số băm (hashable) để cache hoạt động đúng.  

*Mình sẽ thử tích hợp vào dự án hiện tại để tối ưu việc tính toán lặp lại trong module xử lý dữ liệu.*



## Automation

Hôm nay mình tìm hiểu về tính năng **"dynamic environments"** trong GitHub Actions, cho phép tự động tạo môi trường thử nghiệm (test environment) tạm thời khi có yêu cầu triển khai. Cách này giúp giảm thiểu chi phí duy trì môi trường và tăng tốc quy trình kiểm thử tích hợp (CI/CD).  

- Môi trường được tạo ra **tự động** khi workflow chạy, xoá sạch khi hoàn tất.  
- Hỗ trợ tích hợp với các dịch vụ đám mây (AWS, Azure) để triển khai nhanh.  

**Nhận xét:** Mình đang lên kế hoạch áp dụng cách này cho dự án A/B testing để giảm thời gian setup môi trường thủ công.


## DevOps- Hôm nay mình đã học cách sử dụng Terraform để tạo infrastructure‑as‑code cho AWS (VPC, Subnet, EC2) và tự động triển khai qua GitHub Actions.  
- Bằng cách này, môi trường có thể version control và tái tạo nhanh chóng, giúp teamwork hiệu quả hơn.  
- Sẽ áp dụng Terraform cho dự án mới này để rút ngắn thời gian cấu hình môi trường.


## Trekking - Hiking - Camping

- Đã thử nghiệm lều ultralight 2‑person, trọng lượng chỉ 800 g, rất thích hợp cho các chuyến trekking 2 ngày ở Đà Lạt.  
- Kết luận: Lều vừa bền, vừa thoải mái; mình sẽ mang theo trong kế hoạch lên đỉnh Fansipan vào tháng 10.


## Trekking - Hiking  
Today I Learned: Quảng cáo về khu vực nổi bật trên sông Đà Nẵng, môi trường đa dạng đáp ứng yêu cầuenzen. Đi khám phá thêm hiểu biết về địa điểm thanh vị cây viên.  

Bên ninh, tôi đặt triano khoảng 500 giờ để giải quyết miễn thiệt về định vị. 🌿✨



## Programming

Hôm nay mình mới tìm hiểu được về `useMemo` và `useCallback` trong React - hai hook thường dùng để tối ưu performance. `useMemo` giúp cache kết quả tính toán, chỉ tính lại khi dependencies thay đổi, còn `useCallback` dùng để giữ nguyên function reference giữa các lần render.

- Dùng `useMemo` cho các phép tính tốn CPU hoặc tạo object/array phức tạp  
- Dùng `useCallback` khi truyền function xuống component con dùng như dependency
- Nếu không có dependencies thay đổi, hai hook này giúp giảm số lần component con re-render không cần thiết

Ủy, áp dụng vào dự án nextjs đang làm thì component con sẽ nhanh hơn nhiều khi nhận về props function không đổi ✅.
