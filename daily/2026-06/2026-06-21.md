# 2026-06-21



##Travel Tech  
- Tรรม học cách tích hợp API của **Google Maps API** để tạo hệ thống 추천 câu lạc bộ du lịch tại Hoi An, tập trung vào địa điểm ẩn mặt bằng trong nước.  
- Tìm hiểu cách sử dụng **SDK của Firebase** để quản lý người dùng đăng ký tour tại app du lịch Việt Nam.  

Hôm nay học về việc kết hợp công nghệ theo dõi thời gian thực (real-time tracking) để hỗ trợ khách du lịch di chuyển giữa các điểm tham quan trong vùng Hạ Long.  
*Khoa học riêng: Vukkit sẽ thuân thu королевات test API này vào weekend Allí để dự án appMgmtTrekking.*


## DevOps

- Khi triển khai Docker Compose, dùng `build:` thay vì `image:` giúp tự động rebuild image mỗi lần code thay đổi, tiết kiệm thời gian khi phát triển local.
- Thêm `depends_on:` với `condition: service_healthy` (Docker Compose v3.9+) để tránh MongoDB bắt đầu trước khi database sẵn sàng – giúp tránh lỗi nhanh chóng.
- Đánh giá: Sử dụng `--quiet-pull` khi pull images để giảm output, giữ log gọn gàng hơn.

**Lời nhận xét**: Mình có dự định thử `docker compose up --build` trong pipeline GitHub Actions để tự động kiểm tra tính tương thích Build mỗi khi push.


## Automation  

- Học cách dùng **GitHub Actions** để tự động chạy kiểm thử unit test mỗi khi có Pull Request mới.  
- Sử dụng `matrix` trong workflow giúp chạy cùng lúc trên nhiều phiên bản Node.js mà không phải tạo nhiều job riêng.  

Mình thấy workflow này giảm đáng kể thời gian chờ phản hồi, sẽ áp dụng vào dự án API nội bộ tuần tới.


## Automation

Tôi mới khám phá cách dùng **GitHub Actions** kết hợp với **S3** để triển khai web UI tự động mỗi khi có push vào branch `main`.  
- Trigger: `on: push`.  
- Chạy job build với `node:18` image.  
- Upload build folder lên bucket S3, set TTL 1 ngày.  

Nhờ thế website cập nhật ngay tức thì mà không cần thủ công.  
Plan: thử mở rộng cho backend API chạy test tự động, rồi mới push lên ECS.
