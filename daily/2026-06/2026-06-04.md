# 2026-06-04

## Travel Tech

- Hôm nay tôi tìm hiểu về cách sử dụng API của Google Maps để tạo bản đồ đường bộ tùy chỉnh cho các con đường đi bộ ở miền Tây Nguyên, giúp hiển thị điểm kiểm soát và cảnh báo thời tiết real-time.
- Thử tích hợp thư viện `leaflet` với React để xây dựng giao diện tương tác nhẹ, tốc độ load nhanh trên thiết bị di động.
- Cá nhân thấy việc kết hợp dữ liệu otev khai báo (OpenStreetMap) với API thời tiết sẽ nâng cao trải nghiệm cho cáclover trekking; kế tiếp tôi sẽ suunn estimated time of arrival (ETA) dựa trên độ khuynh độ path và áp dụng vào ứng dụng bản đồ cá nhân của mình.



## Programming

- Hôm nay mình học được cách dùng `Array.some()` và `Array.every()` để kiểm tra điều kiện nhanh hơn nhiều so với vòng lặp thủ công, đặc biệt khi cần validate danh sách điều kiện.
- Ví dụ: `items.every(item => item.isValid)` sẽ dừng ngay khi gặp phần tử đầu tiên không đạt điều kiện, tiết kiệm thời gian chạy.
- Tính năng này thực sự thiết thực khi xử lý form validation hay kiểm tra tính hợp lệ của dữ liệu đầu vào.

*Plan áp dụng ngay trong dự án tuần tới để làm trọng trường hợp validate nhân viên theo từng phòng ban.*


##Automation
- Hôm nay mình viết script PowerShell để tự động backup database mỗi đêm, giảm thời gian thực hiện thủ công.
- Dùng Task Scheduler để chạy script hàng tuần, giảm lỗi do can thiệp của con người.
- Kế hoạch: Tích hợp thêm thông báo email khi backup thành công.  
*Mình sẽ áp dụng cách này cho dự án tới.*


## Automation

- Học cách sử dụng **GitHub Actions** để tự động chạy kiểm thử đơn vị mỗi khi có pull request mới.  
- Với workflow đơn giản, chỉ cần một file YAML dưới `.github/workflows/ci.yml`, cấu hình `jobs` để cài đặt môi trường, cài package và chạy lệnh `npm test`.  
- Nhờ vậy, thời gian phản hồi lỗi giảm đáng kể và mình không còn lo quên chạy test thủ công nữa.  

> *Kế hoạch*: Áp dụng workflow này cho dự án phía backend và mở rộng thêm bước linting vào pipeline.


## Trekking - Hiking - Camping (Sapa, Hà Nội)  
- negotiated a steep trail in Sapa and learned the importance of pacing itself to avoid exhaustion.  
- discovered local edible herbs (like "ph.Write (shortcut for pepper or mint)" used by guides for medicinal purposes.  
- realized the value of wearing waterproof gear when crossing streams/muddy paths.  

**L bestehenden**: Next time, I’ll research offline maps for remote areas and carry a small notebook to jot down local tips from guides. 🌿
