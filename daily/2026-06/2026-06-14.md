# 2026-06-14



## Travel Tech  Hôm qua mình học về APIbookingtotravel.vn, một giải pháp Việt Nam cho phép bookingabilidade hàng 이야ponta tiempo real với nhiều du lịch di sản như Ninh Bin, Alten. Mình thử test với giao diện giao động của họ, phát hiện hệ thống tích hợp llamarrea 크게 khi so với các nền tảng quốc tế.  

💡 **Note cá nhân**: Có plan xây dựng mô hình tương tự cho dự án du lịchpeł/UI của mình sau này, tập trung vào tính bảo mật và tốc độ giao thực.


## Travel Tech

- Hôm naymình thử cài Flutter để xây dựng app quản lý chi phí du lịch, phát hiện cách tối ưu hoá giao diện cho việc nhập chi phí nhanh chóng.  
- Kế hoạch áp dụng tính năng này vào dự án quản lý tour hiện tại vào cuối tuần tới.


## Automation
- Hôm nay tôi học được cách sử dụng GitHub Actions để tự động chạy lint và test mỗi khi push code vào nhánh develop.  
- Đồng thời tôi cũng khám phá cách cache các dependencies Node.js để giảm thời gian CI xuống khoảng 30% so với trước.  
- Cá nhân thấy điều này rất hữu ích cho team làm việc từ xa; tôi định áp dụng quy trình tương tự cho dự án side‑project của mình vào tuần tới.




## DevOps  
- Hôm nay mình học cách cấu hình Jenkins để tự động test}&amp; deploy code mỗi khi push branch.  
- Chuyển đổi.prevent được tích hợp trong pipeline giúp phát hiện lỗi sớm hơn.  
- Mình viết script đơn giản để tự độngendoza lại container Docker chạy đồng thời sau khideploy.  

Ghi chú: Plan học thêm cách optimize Jenkins pipeline bằng Jenkinsfile để Celtuôn hơn và haiêu hiệu năng.


User Safety: safe


## Travel Tech

- Hôm nay tôi tìm hiểu về cách tích hợp API của Google Maps vào ứng dụng di động để tạo tuyến đường trekking tự động dựa trên độ cao và độ khó của đường đi.
- Thử dùng thư viện `@react-native-maps` cùng với `react-native-geolocation-service` để lấy vị trí thực thời và vẽ polyline lên bản đồ, giúp người dùng thấy rõ đoù họ đang đứng trên đường thoải mái.
- Cá nhận: tính năng này sẽ rất hữu ích cho các nhóm du lịch aventura khi họ muốn thay đổi kế hoạch trên đường mà không cần tra giấy bản đồ tradicionales. Kế hoạch áp dụng vào side‑project tiếp theo của tôi là một app “Trekking Việt Nam” nhỏ gọn.


## DevOps

-Hôm nay mình đã cấu hình CI/CD pipeline với GitHub Actions cho dự án Node.js, tự động chạy unit test và triển khai deployment lên Docker container.  
- Sử dụng secrets để quản lý environment variables, giúp tăng bảo mật và linh hoạt trong quá trình triển khai.  

Sẽ thử áp dụng pipeline này cho một micro‑service mới trong dự án của mình vào tuần tới.



## Programming

- Hôm nay tìm hiểu về **Python list comprehension** với điều kiện phức hợp: `[x for x in range(100) if x % 2 == 0 and x % 3 == 0]` —> `[0, 6, 12, 18, ...]` siếp tiết gọn gàng hơn 3 dòng code so với vòng for thông thường.
- Thử nghiệm với **dictionary unpacking** để merge config: `config_final = {**default_config, **user_config}` — user_config ghi đè lên default một cách tự nhiên.
- **Plan**: Dùng pattern này trong dự án tuần tới để xử lý validation form mà không cần if/else dài dòng.

*Nhận xét: Code ngắn nhưng phải chậm rãi debug từng dòng mới là được 🤓*
