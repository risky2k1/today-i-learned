# 2026-06-11

# DevOps

- Đã học về cách tối ưu hóa các gây dồi hoàn thiện quy trình CI/CD.
- Ứng dụng ngôn ngữ 봇 để tự động kiểm tra điểm kiểm định.
- Cảm thấy học vẻ tin tưởng vào một chiến lược điều tiết chuẩn.

Ngày giờ làm việc tôi nghĩ chuyên dụng phải luôn ramp up công cụ tự động hóa, chúng sẽ giúp tôi nhanh hơn.



## Programming

- Gần đây mình phát hiện ra sự khác biệt giữa `||` và `??` khi xử lý giá trị mặc định: `||` sẽ trả về giá trị thứ hai nếu giá trị đầu là falsy (0, "", false, null, undefined), còn `??` chỉ trả về giá trị thứ hai khi gặp `null` hoặc `undefined`.
- Ví dụ: `0 || "default"` trả về `"default"`, nhưng `0 ?? "default"` trả về `0`.
- Điều này đặc biệt hữu ích khi xử lý dữ liệu từ API hoặc form input mà không muốn làm mất giá trị falsy hợp lệ như số 0 hoặc chuỗi rỗng.

Mình vừa refactor một số hàm xử lý dữ liệu cũ trong dự án, dùng `??` thay vì `||` - code ngắn gọn và đúng ngữ nghĩa hơn rất nhiều!


User Safety: safe


## DevOps

- Khi làm việc với Docker, thay vì dùng `latest` tag cho các image, mình nên chỉ định rõ version (ví dụ: `node:18-alpine`) để tránh việc build lại bị lỗi do image tự cập nhật phiên bản mới không tương thích.
- Việc sử dụng các image bản `alpine` giúp giảm đáng kể dung lượng image và thu hẹp bề mặt tấn công (attack surface) cho security.

*Note: Mai phải check lại ngay mấy cái Dockerfile trong project cũ, đổi hết sang tag cụ thể cho chắc cú.*


# Today I Learned

- Mới học được về cách tối ưu hóa công cụ thể trong DevOps.
- Tìm thấy một ví dụ cụ thể cho việc tự tạo git repository.
- Cảm ơn người đào tạo đã giúp năm ngày làm tốt hơn!

👍 Thấy mong cha mẹ học ghế lên học lại gắn liền trong tương lai.


## Travel Tech  

- Hôm nay mình mới biết về **API quản lý đặt chỗ** của các nền tảng du lịch tại Việt Nam, cách họ sử dụng **webhook** để đồng bộ trạng thái phòng và thông tin khách hàng.  
- Sẽ thử triển khai một **script tự động** để nhận webhook, cập nhật lại danh sách phòng trống và gửi thông báo cho khách qua chatbot.  

*Ý kiến cá nhân: Tự động hoá việc sync dữ liệu này sẽ giúp giảm lỗi ngườiINPUT và tiết kiệm thời gian cho team mình.*
