# 2026-07-10

## Automation

Tôi vừa khám phá cách **tự động triển khai Docker Containers lên Kubernetes** bằng GitHub Actions.  
- *Workflow* chỉ cần một step đẩy image lên registry và chạy `kubectl rollout` để deploy; không còn thao tác thủ công ngoài việc push code.
- Tự động hoá này đã giảm thiểu thời gian release xuống dưới 5 phút và loại bỏ lỗi nhầm lộn trong quy trình cấu hình.

**Kế hoạch**: sớm thêm bước quét ảnh Docker (truy cập CVE)\,  để tăng an toàn cho pipeline.


User Safety: safe


## Programming

Hôm nay mình học được rằng trong JavaScript, dùng `structuredClone()` là cách native và nhanh nhất để sao chép một object lồng nhau (deep copy) mà không cần tới thư viện như Lodash. Trước đây mình toàn dùng `JSON.parse(JSON.stringify(obj))` nên hay bị mất các kiểu dữ liệu đặc biệt như `Date` hay `Map`. 
Plan: Sẽ refactor lại mấy đoạn code cũ vào cuối sprint này để tối ưu và giảm dependency thừa.


User Safety: safe



## Programming  
TIL: Sử dụng `context manager` trong Python giúp quản lý tài nguyên tự động (file, database connection...) mà không cần ghi đầy đủ hàm `try-finally`. Ví dụ: `with open('file.txt') as f:` giúp đóng file ngay cả khi xảy ra lỗi. Áp dụng cho các trường hợp cần đảm bảo tài nguyên được giải phóng đúng cách.  

*Áp dụng ngay vào module xử lý log để tránh rò rỉ file descriptor!*


## Lập trình

- Hôm nay mình phát hiện ra rằng hàm `enumerate()` trong Python cho phép duyệt cả chỉ số và giá trị đồng thời, giúp giảm độ phức tạp của vòng lặp khi cần truy cập vị trí. Ví dụ: `for index, value in enumerate(['a', 'b', 'c']): print(index, value)` sẽ in ra 0 a, 1 b, 2 c.
- Mình sẽ áp dụng hàm này vào việc tối ưu hóa các đoạn code cũ trong dự án hiện tại để làm cho chương trình ngắn gọn và dễ đọc hơn.



## Travel Tech

- Hôm nay tìm hiểu về API của Vietnam Railways cho phép truy xuất thông tin tàu đường sắt thời gian thực, giúp tích hợp vào app hay công cụ cá nhân quản lý chuyến đi.  
- Có thể kết hợp với Google Maps để tạo route tối ưu giữa các tỉnh miền Trung - miền Nam.  
- Ít người biết đến nhưng hữu ích cho ai thích xưng đi bằng tàu hỏa thay vì máy bay.  

Mình sẽ thử xây dựng công cụ nhỏ để cảnh báo giờ tàu sắp chạy từ TP.HCM - Đà Nẵng, hỗ trợ đặt vé nhanh hơn.



## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)  
- Hôm nay mình phát hiện rằng khu vực Hoàng Liên Sơn (Sapa) yêu cầu phải xin giấy phép trước khi dã ngoại, đặc biệt là khu vực trang bị lửa trại tại Fansipan.  
- Giấy phép này phải được đăng ký qua Cục Quản lý rừng phòng hộ hoặc đơn vị lữ hành địa phương, còn thiếu tay là bị phạt tiền.  
- Mình đang lên kế hoạch trekking đến đỉnh Pu Ta (2.200m) vào tháng 11, nên cần chuẩn bị hồ sơ cũng như tìm hiểu về thời tiết đêm se lạnh.  

*Personal note: Cần liên hệ sớm với hướng dẫn viên địa phương để tránh trường hợp "đi là phải chết" vì thiếu giấy tờ!*
