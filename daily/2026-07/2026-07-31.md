# 2026-07-31

## Automation

Hôm nay mình biết thêm về `xargs` — nó giúp chuyển output của lệnh này thành argument cho lệnh khác một cách linh hoạt, đặc biệt hữu ích khi xử lý danh sách file hoặc kết hợp với `find`. Ví dụ: `find . -name "*.log" | xargs grep "ERROR"` sẽ tìm tất cả file log và grep nội dung ERROR trong đó mà không bị lỗi "argument list too long".

Mình định áp dụng `xargs` với `-P` để song song hóa các task backup log hàng ngày trên server, tiết kiệm đáng kể thời gian thay vì tuần tự từng file.


## Trekking - Hiking - Camping  
Hôm nay tôi nhận ra rằng việc sử dụng Gaia GPS cho bản đồ offline là vô cùng hữu ích khi khám phá các hang động ở Phong Nha, vì tín hiệu di động yếu. Thêm vào đó, việc mang theo bản đồ giấy là cần thiết với mỗi chuyến đi.  
Kế hoạch cho tương lai: thử nghiệm các ứng dụng bản đồ khác như AllTrails để có lựa chọn đa dạng.


## Travel Tech  

Mình vừa khám phá **TripIt API** để xây dựng một dashboard tùy chỉnh cho chuyến đi đầy nắng. Việc pull lịch bay, khách sạn qua `tripit.com/api/` giúp tôi luôn cập nhật thời gian thực mà không cần đánh tab đổi chế độ. Định làm demo cho nhóm khi họ lên kế hoạch trip năm nay, mong có thể tính toán chi phí nhanh hơn và share bản đồ trực quan.


## DevOps  
- Tìm hiểu cách tích hợp Kubernetes vào pipeline CI/CD để triển khai container tự động hóa và quản lý độ sẵn.  
- Ứng dụng herramientas như Ansible để tự động hóa cấu hình server, giảm thời gianWORK trong triển khai.  
- Tiêu chuẩn hóa quy trình improvization trong DevOps để đảm bảo tính nhất quán giữa các nhóm.  

**Mẹo cá nhân**: H înh thực thi Kubernetes tại dự án tiếp theo để thay thế việc cấu hình thủ công Etcd.


## Automation

- Tôi vừa tìm hiểu cách sử dụng GitHub Actions để tự động thực hiện việc sao lưu dữ liệu mỗi khi có commit mới vào nhánh `main`. Quy trình này khá đơn giản, chỉ cần khai báo file workflow, sử dụng `secrets` để lưu credential và thêm bước checkout cùng script tùy chỉnh.

- Bằng cách lập lịch tác vụ cron hàng ngày trong chính workflow đó, tôi có thể đồng bộ hóa các bản sao lưu cục bộ mà không cần can thiệp thủ công. Điều này giúp tiết kiệm đáng kể thời gian so với việc phải thực hiện sao lưu bằng tay như trước.


## Programming

*   Hôm nay mới ngấm được concept "Composition over Inheritance" trong thiết kế class. Thay vì cố gắng xây dựng một cây kế thừa quá sâu và phức tạp, việc kết hợp các hành vi nhỏ (behavior) lại với nhau giúp code linh hoạt và dễ test hơn hẳn.

Học cái này xong thấy mình hồi trước viết code "mùi" inheritance quá, chắc chắn phải refactor lại đống logic cũ thôi.


## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)

*   Vừa phát hiện ra cung trekking Lảo Thẩn (Y Tý) cực kỳ hợp cho người mới bắt đầu vì đường đi khá thoáng, không quá dốc và view săn mây thì "đỉnh của chóp".
*   Lưu ý quan trọng là nên đi vào tầm tháng 10 đến tháng 4 để tránh mùa mưa, nếu không thì trekking sẽ rất trơn trượt và khó săn mây.

Chắc chắn phải lên kèo với hội bạn một chuyến vào tháng sau để đổi gió sau đợt deadline căng thẳng này!
