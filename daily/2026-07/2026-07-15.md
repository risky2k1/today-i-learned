# 2026-07-15

## Trekking - Hiking - Camping

- Đã thử lều pop‑up tại đỉnh Fansipan, setup chỉ mất 3‑4 phút và chịu được gió mạnh, mưa rào nhờ vật liệu chịu nước 3000mm.  
- Sử dụng Garmin eTrex 30x kết hợp bản đồ topographic offline để định vị chính xác, giảm nguy cơ lạc đường trong sương mù.  

Plan: Sắp xếp thử lều pop‑up và thiết lập trạm năng lượng solární cho GPS vào chuyến trekking kế hoạch tới Sapa.


## Trekking - Hiking  
Hôm nay tôi lặn xuống núi Tả Đà Lạt để khám phá **mạch kim Vidal Fall** – nơi nước chảy trong tà lạnh ống thuần, tuyệt đỉnh đà và đám rừng bURSS như tranh minh họa. **Tips**: Luôn reprendre chuẩn **Snacks và nước uống khi lên núi**, đặc biệt là những tuyến trekking dài. Tôi plan áp dụng thói quen này vào chuyến trải nghiệm Sang Lý Viện tuần sau.  

---  
*Tận dụng những khoảnh khắc chững chạc giữa hành trình để ghi nhận gương mặt bản thân!* 🌿🌁



## Programming

Hôm nay mình phát hiện cách dùng f-strings trong Python để format chuỗi ngắn gọn hơn. Thay vì cách truyền thống dùng `%` hoặc `.format()`, f-strings cho phép chèn biến trực tiếp vào chuỗi bằng cú pháp `{variable}`. Ví dụ: `f"Tên tôi là {name}, tuổi là {age}"` — rất tiện lợi khi code nhanh và dễ đọc.

*Nhận xét:* Mình sẽ áp dụng f-strings hơn trong project hiện tại để giảm thiểu code dài dòng và cải thiện tính bảo trì.


## Trekking - Hiking - Camping

Hôm nay mình thử cách lắp đặt lều ultralight cho trekking, trọng lượng chỉ 800 g và nhanh chóng cài đặt. Ngoài ra, mình còn khám phá một đoạn trail mới ở Đà Lạt, cảm nhận không khí trong lành và các điểm dừng nghỉ độc đáo. Cảm giác rất thư giãn và truyền cảm hứng cho những chuyến đi tương lai.

- Kế hoạch lên kế hoạch cho một chuyến trekking 2 ngày vào cuối tuần tới, chuẩn bị đầy đủ dụng cụ và thực phẩm.


## Automation
- Hôm nay mình đã học cách tạo pipeline CI/CD trên GitHub Actions để tự động triển khai ứng dụng Docker.
- Đã triển khai thành công pipeline deploy cho môi trường staging và ghi nhận log lỗi nếu có.
*Plan: Tiếp tục tối ưu hoá pipeline để triển khai tự động cho môi trường production trong tuần tới.*


## Travel Tech  

- Hôm nay tìm hiểu ElaTech: cách tích hợp Google Maps Directions API ؟  
  - Thay vì gọi API từng lần khách hàng đặt vé, lưu `place_id` và trình diễn chỉ đường tối ưu theo traffic.  
  - Dùng `departure_time=now` để luôn nhận ETA realtime, giảm bớt lỗi “đường vô ích” người dùng thường gặp.  

💡 *Kế hoạch:* thử nghiệm tính năng này trên app đặt xe nội địa, đo lường độ trễ và mức độ giảm vi phạm giao thông.



## Programming  
Hôm nay mình phát hiện Python hỗ trợ `context manager` với từ khóa `async with` để xử lý tài nguyên bất đồng bộ (như mở file hoặc kết nối database) một cách sạch sẽ hơn. Cú pháp này giúp tránh lỗi khi quên đóng tài nguyên trong môi trường async/await.  

*VD:*  
```python  
async with aiofiles.open('file.txt', 'w') as f:  
    await f.write('hello')  
```  

Mình đang thử áp dụng vào dự án FastAPI hiện tại để tối ưu việc xử lý file đồng thời.


User Safety: safe



## Programming  
Hôm nay mình phát hiện `pathlib` trong Python thực sự giúp việc xử lý đường dẫn tệp linh hoạt hơn so với `os.path` truyền thống. Ví dụ: `Path("data/file.txt").exists()` ngắn gọn hơn `os.path.exists("data/file.txt")`. Mình sẽ thử áp dụng `pathlib` vào dự án hiện tại để giảm thiểu lỗi khi làm việc với hệ thống file trên các nền tảng khác nhau.  
*Personal note: Câu điển hình "nên viết code ít nhất 2 lần để hiểu rõ" – mình đã phải thử nhiều lần mới nắm được cách dùng `pathlib` hiệu quả!*


## Travel Tech
Hôm nay mình tự động hóa việc kiểm tra giá vé máy bay qua API của Vietnam Airlines và lưu kết quả vào Google Sheet bằng Apps Script. Nhận ra cách viết các hàm lồng nhau trong JavaScript giúp mình giảm thời gian xử lý dữ liệu hơn 30%. Kế hoạch sẽ thử áp dụng cho các route trong nước khác vào tuần tới.
