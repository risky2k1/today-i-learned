# 2026-07-18

User Safety: safe


User Safety: safe


## DevOps  
- Hôm nay tôi phát hiện ra rằng khi dùng `docker-compose up` với nhiều service, việc đặt `depends_on` không đủ để đảm bảo các service phụ thuộc đã khởi động hoàn tất. Cần thêm `healthcheck` hoặc script để kiểm tra trạng thái sẵn sàng trước khi tiếp tục chạy.  
- Thử nghiệm với `restart: on-failure` trong Docker Compose giúp tự động khởi động lại service khi gặp lỗi, giảm downtime.  

Cảm ơn mentor đã chỉ ra, mình sẽ tích hợp healthcheck vào pipeline CI/CD cho dự án microservice.


## Programming

Hôm nay mới ngấm được cách dùng `Optional` trong Java để xử lý các giá trị null một cách "sạch" hơn, thay vì cứ phải viết `if (obj!= null)` loằng ngoằng khắp nơi. Dùng cái này giúp code trông functional hơn và giảm thiểu nguy cơ bị lỗi `NullPointerException` đáng ghét.

*Plan: Áp dụng ngay vào cái module xử lý data đang viết dở để refactor lại cho gọn.*


## Automation  
- Tôi vừa thử nghiệm **GitHub Actions** để tự động chạy test đơn vị mỗi khi có `push` vào branch `main`.  
- Thêm chế độ **cache** cho thư mục `node_modules` giảm thời gian build 30%.  
- Các job này ghi lại lỗi ngay trong PR, giúp dev sớm phát hiện và sửa ngay.  

**Nhận xét**: Hẹn ngay tuần này chuyển một số script CI hiện tại sang GitHub Actions để giảm khả năng lỗi thủ công.


## Automation
- Viết script Bash tự động backup dự án Git mỗi đêm vào 02:00.  
- Thử nghiệm `crontab` và cấu hình `environment` để script nhận được PATH đúng.  

**Plan cá nhân:** Tự động gửi email thông báo nếu backup thất bại trong tuần tới.
