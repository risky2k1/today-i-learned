# 2026-07-19

## Programming

- Hôm nay mới vỡ lẽ ra sự khác biệt giữa `Map` và `forEach` trong JavaScript. Một cái dùng để biến đổi mảng sang mảng mới, còn cái kia chỉ dùng để lặp và thực thi side-effect.
- Dùng sai `Map` trong React khi render list có thể gây ra những lỗi re-render cực kỳ khó chịu.

Phải cẩn thận hơn với mấy hàm xử lý array để tránh bug logic ngầm.


## Automation

Tôi đã khám phá ra cách sử dụng **GitHub Actions** cùng với `jq` để tự động chuyển đổi kết quả API JSON thành một bảng Markdown đẹp mắt, giúp cập nhật nhanh tài liệu `README` bất cứ khi nào dữ liệu nguồn thay đổi. Cách tiếp cận này giúp loại bỏ việc chỉnh sửa bằng tay và đảm bảo các thẻ đo lường luôn hiển thị thông tin mới nhất. Kế hoạch là sẽ mở rộng quy trình này cho các bảng giá cả và thay đổi phụ thuộc trong tương lai.
