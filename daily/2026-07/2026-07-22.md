# 2026-07-22

## DevOps

Tôi vừa phát hiện ra rằng việc sử dụng các công cụ CI/CD có thể tự động triển khai không chỉ các thay đổi code mà cả các cập nhật cấu hình infrastructure—nhờ các quy trình pipeline kết hợp GitOps. Bằng cách tích hợp Helm charts với GitHub Actions và ArgoCD, chúng ta có thể duy trì một source-of-truth duy nhất trong repository và giảm thiểu các lỗi do con người gây ra. Từ bây giờ, tôi sẽ thử áp dụng phương pháp này cho các microservices hiện tại nhằm rút ngắn chu kỳ phát hành.


User Safety: safe


## Programming
- Hôm nay tôi tìm hiểu về decorator `@functools.lru_cache` trong Python và cách nó giúp memoization tự động cho các hàm đệ quy.
- Khi áp dụng vào hàm tính Fibonacci, thời gian chạy giảm từ phép chỉ số O(2^n) xuống O(n) mà không cần viết logic cache thủ công.
- Tôi định tích hợp kỹ thuật này vào microservice xử lý tỷ giá crypto hiện tại để tối ưu hoá các truy vấn lặp lại.


User Safety: safe


## DevOps  
- Hôm nay tìm hiểu thêm về tính năng `depends_on` trong Docker Compose. Mặc dù nó giúp xác định thứ tự khởi động container, nhưng nó chỉ chờ container "chạy" chứ không chắc chắn dịch vụ bên trong đã sẵn sàng.  
- Để tránh lỗi kết nối (ví dụ: MySQL chưa khởi động xong nhưng container phụ thuộc đã cố gắng kết nối), cần dùng thêm `healthcheck` hoặc script kiểm tra trạng thái dịch vụ.  
- Cá nhân mình sẽ áp dụng `healthcheck` trong dự án Docker Compose tương lai để tối ưu độ ổn định của hệ thống.


## Travel Tech

- Hôm nay mình mới biết rằng một số airline Việt Nam đang triển khai hệ thống đặt chỗ thông minh dựa trên AI để tối ưu hoá giá và tuyến.  
- Họ sử dụng dữ liệu lịch trình và xu hướng để đề xuất mức giá theo thời điểm và địa điểm phù hợp.  
- Mình đã thử tích hợp API này vào app quản lý cho công ty du lịch của mình để thử nghiệm.  
- Sẽ áp dụng tính năng này trong 2 tuần tới và theo dõi kết quả thực tế.
