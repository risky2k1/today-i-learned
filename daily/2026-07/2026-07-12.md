# 2026-07-12


## Programming  
Hôm nay mình phát hiện Python f-string mạnh hơn mình nghĩ, giúp format chuỗi nhanh hơn và hỗ trệ biểu thức chèn trực tiếp như `{name.upper()}` hay `{x + y}`. So với `.format()` hay `%` formatting, f-string còn dễ đọc và ít lỗi hơn. Mình sẽ áp dụng viết lại các hàm log cũ trong dự án để code sạch hơn.


## Automation

- Hôm nay mình khám phá cách sử dụng **GitHub Actions** để tự động chạy kiểm thử unit test mỗi khi có pull request mới.  
- Với file workflow đơn giản chỉ cần khai báo `on: [pull_request]` và dùng `actions/setup-node` để cài Node.js, sau đó chạy `npm test`.  
- Kết quả: quá trình CI/CD hoàn toàn tự động, giảm thiểu lỗi do quên chạy test thủ công.

**Nhận xét:** Rất ấn tượng với tốc độ phản hồi, mình sẽ áp dụng kiểu workflow này cho dự án React tiếp theo để luôn có báo cáo test ngay khi mã được merge.


## Programming

Hôm nay mình học được cách sử dụng `match` expression trong Python 3.10 để thay thế chuỗi các `if‑elif‑else`. Nó giúp code ngắn gọn, dễ đọc hơn, đặc biệt khi xử lý nhiều trường hợp enum.  
- Cú pháp: `match value:` → `case pattern:` → `case _:`  
- Hỗ trợ pattern matching cho list, dict và các lớp tùy chỉnh.

**Nhận xét:** Rất hứng thú với tính năng này, sẽ áp dụng vào dự án parser log để giảm boilerplate.


## [Trekking - Hiking - Camping]

- Đi trekking núi Chúa (Phú Yên) mùa khô nên mang theo **buff/kính râm** chứ đừng chỉ tin mũ nón, gió núi thổi mịn màng cát vây bay lên mặt rất "đau da" và khô mắt.
- Nước suối ở các điểm camp gần đỉnh thường có hàm lượng khoáng cao, nên mang thêm **viên nén điện giải** pha uống thay vì chỉ uống nước lọc thường để tránh chuột rút khi leo dốc dài.
- *Note cho lần sau: Sẽ test loại balaclava mỏng thay cho buff + kính, vừa che kín mặt lại đỡ bị hơi thở làm đờm kính khi nghỉ ngơi.*


## Trekking - Hiking - Camping

- Nếu đi trekking ở vùng cao phía Bắc như Hà Giang hay Lào Cai, nên ưu tiên chọn giày có độ bám (grip) cực tốt và chống nước vì độ ẩm ở đây rất cao.
- Nên mang theo túi chống nước chuyên dụng cho các thiết bị điện tử thay vì chỉ dùng túi nilon thông thường.

*Nhận xét: Đi trekking mà đồ đạc ngấm nước thì đúng là "ác mộng", lần sau phải chuẩn bị kỹ hơn.*


## Automation

Tôi vừa khám phá ra cách sử dụng `dokan-<->library` trong Python để tự động hóa quy trình đồng bộ tệp giữa các thư mục local và đám mây, giúp việc đẩy dữ liệu lên Google Drive trở nên liền mạch mà không cần viết script thủ công. Quy trình tích hợp này đã giảm đáng kể thời gian xử lý công việc xuất dữ liệu hàng ngày, và tôi cũng có thể dễ dàng theo dõi tiến trình thông qua nhật ký công việc. Tôi dự định sẽ áp dụng kỹ thuật này cho các pipeline báo cáo định kỳ khác trong dự án tiếp theo.



## Programming

Hôm nay mình phát hiện Node.js 18+ đã tích hợp sẵn `fetch API` mà không cần cài thêm package `node-fetch`. Điều này giúp giảm dependency và đồng nhất cú pháp giữa frontend và backend. Tuy nhiên, khi dùng với CommonJS phải thêm `import { fetch } from 'node:test';` vì `fetch` vẫn ở chế độ thử nghiệm.  

Mình sẽ áp dụng cách này vào dự án API client để đơn giản hoá code hơn.


## Travel Tech

Tôi vừa thử nghiệm **Google Maps Distance Matrix API** để tính toán tối ưu hành trình đa phương tiện giữa các điểm du lịch ở Việt Nam. Gặp khi cần hỗ trợ thông số `departure_time` và `traffic_model`, API trả về độ trễ giao thông dự báo và lựa chọn tuyến đường nhanh nhất. Kế hoạch: tích hợp vào ứng dụng du lịch nội địa, tự động đề xuất lịch trình cho khách theo thời gian thực và lưu cảnh báo khi giao thông đột ngột thay đổi.


## Automation

Tôi khám phá ra rằng việc kết hợp `git hook` với công cụ linting tự động có thể giúp phát hiện lỗi phong cách mã ngay lập tức khi bạn thực hiện commit, từ đó tiết kiệm khá nhiều thời gian kiểm tra thủ công. Sử dụng `pre-commit` framework giúp việc thiết lập các hook này trở nên dễ dàng hơn bao giờ hết. Từ giờ tôi sẽ thử áp dụng nó cho tất cả các dự án mới để duy trì code linting nhất quán.



## Trekking - Hiking - Camping (Các địa điểm ở Việt Nam)  
- Hôm nay tôi phát hiện rằng đỉnh Fansipan (Sapa) có thể chọn mùa thu hoạch hạt điều và cây công nghiệp như hồ tiêu, trong khi mùa mưa sẽ khiến đường đi trở nên trơn trượt và nguy hiểm.  
- Điểm đến lý tưởng khác là quảng trường Sa Pa, nơi tổ hợp nhiều công viên và khu du lịch sinh thái, phù hợp cho các chuyến trekking ngắn ngày.  
- *Cá nhân*: Mình đang lên kế hoạch đi bộ đường dài qua thung lũng Mường Hoa vào tháng 11, tránh mùa mưa để tận hưởng cảnh quan bình minh giữa làng hưng lũy ấm áp.
