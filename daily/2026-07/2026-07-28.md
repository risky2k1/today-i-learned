# 2026-07-28

## Programming

Hôm qua mình khám phá cú pháp **TypeScript `Pick`** để trích xuất các trường mong muốn từ một interface. Thay vì viết lại interface mới để loại bỏ các thuộc tính không cần, `Pick<T, K>` giúp、この cách làm code ngắn gọn và an toàn hơn.  
✅ Lưu西 một đoạn ví dụ cho dự án hiện tại, sẽ nhanh lên 15% khi refactor giao diện API.


## Automation

- Bật lên một cron job chạy `bash backup.sh` lúc 02:00 mỗi ngày để tự động sao lưu toàn bộ thư mục `~/projects` lên ổ đĩa ngoài.
- Script `backup.sh` gói thư mục vào định dạng tar.gz, kèm timestamp, sau đó đẩy lên máy chủ remote qua `rsync -avz ./ /remote/backup/`.
- Đã thêm kiểm tra đồng bộ thành công với mail thông báo qua `mailx`.

**Nhận xét**: Ghi nhớ ngày cuối cùng khôi phục thử để xác nhận tính toàn vẹn, rồi triển khai lên môi trường production.


## [Trekking - Hiking - Camping]

Hôm nay biết rằng **Đồi Chè Tà Xùa (Sơn La)** có tuyến "đỉnh nhí" phù hợp cho newbie muốn trải nghiệm mây hunt mà không cần quá nhiều kỹ năng leo dốc chuyên nghiệp, chỉ mất khoảng 1.5 - 2h leo từ chân đồi. Điểm then chốt là phải đến lúc 5h sáng để kịp bình minh, và tốt nhất thuê xe con (moto) chở từ homestay lên ngã ba chân đồi để tiết kiệm thể lực.  

> *Note: Sắp lên kế hoạch cuối tháng này, cần mua thêm găng tay leo và áo mưa loại nhẹ gọn cho bão đêm rừng.*


## [Trekking - Hiking - Camping]

- Đi trekking dốc Núi Chúa (Ninh Thuận) vào mùa khô, layer áo mưa (rain shell) vẫn cần mang theo dù trời nắng gắt vì gió đỉnh núi thay đổi thất thường và có thể mưa rào cục bộ bất chợt.
- Nước uống tính theo công thức: `(Số km * 0.5L) + 1L dự phòng` mới an toàn, đừng tin cảm giác "khát rồi mới uống" ở độ cao vì khô hanh khiến cơ thể mất nước thầm lặng.
- Tent 3 mùa (3-season) đủ dùng cho Núi Chúa, nhưng cột dây tira (guy lines) bắt buộc phải căng kín four corners vì gió vallée thổi ngang rất mạnh về đêm.

> *Note: Lần sau mang thêm buff che mặt/khi ngủ, gió thổi cát vào lỗ tai/mũi khó chịu lắm.*



## Programming
- Hôm nay mình phát hiện cách sử dụng **Docker multi-stage builds** giúp giảm đáng kể kích thước image cuối cùng bằng cách tách quá trình build và chạy ứng dụng ra hai giai đoạn riêng biệt.
- Giai đoạn đầu tiên dùng để cài đặt dependencies và biên dịch code, giai đoạn thứ hai chỉ sao chép các file cần thiết từ giai đoạn trước, loại bỏ mọi thứ không cần thiết.
- Ví dụ: Trong Dockerfile, mình dùng `FROM node:14 AS builder` cho giai đoạn build, rồi `FROM node:14-alpine AS runner` cho giai đoạn chạy, giảm image từ 1.2GB xuống còn 150MB!

*Mình sẽ áp dụng kỹ thuật này cho các dự án Node.js/React để tối ưu CI/CD pipeline.*


## Programming  

- Sử dụng thư viện `itertools` trong Python để xử lý các chuỗi dữ liệu lớn một cách hiệu quả, giảm đáng kể thời gian chạy so với vòng lặp thông thường.  
- Ví dụ: `itertools.chain()` giúp nối các iterable thành một duyệt các phần tử một cách trơn tru.  
- Mình đang học cách tối ưu hóa code bằng các công cụ này để xử lý dữ liệu big data nhanh hơn.


## DevOps

Hôm nay tôi khám phá ra rằng việc tích hợp kiểm tra kiểu tĩnh (static type checking) vào quy trình CI không chỉ giúp bắt lỗi sớm mà còn cải thiện đáng kể sự hợp tác giữa nhóm phát triển frontend và backend. Tôi đã thêm một bước kiểm tra bằng ESLint với TypeScript vào GitHub Actions và thấy rằng thời gian quay vòng phản hồi giảm đáng kể. Kế hoạch sắp tới: mở rộng tự động hóa này để bao gồm cả việc kiểm tra bảo mật trên các dependencies.
