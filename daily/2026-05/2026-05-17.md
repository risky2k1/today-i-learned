# 2026-05-17

## Automation  
- Môi trường ứng dụng tự động giúp giảm thiểu lao động chi phí.  
Personal: Đều chất lượng sản xuất tăng cao nên cần chấp nhận thế.



## Programming

Hôm nay mình học được về cách dùng `enumerate()` và `zip()` trong Python để xử lý danh sách gọn gàng hơn:

- `enumerate()` giúp lấy cả index và giá trị trong vòng lặp `for`, tiết kiệm phải dùng biến đếm tay
- `zip()` cho phép duyệt cùng nhiều danh sách, tự động cặp đối tương ứng
- Thay vì viết `for i in range(len(list))`, giờ dùng `for i, value in enumerate(list)` là ngắn và dễ đọc hơn

Mình thấy tay mình ngắn bớt khi viết vòng lặp, nhất là khi xử lý dữ liệu có nhiều danh sách song song.




## DevOps  
- Hôm nay mình đã học cách tối ưu hình thức CI/CD bằng Jenkins và Ansible, tập trung vào triển khai đa ngôn.  
- Sử dụng Nano-envelope đểCode Secure trong pipeline, giảm rủi ro attack từ거나.  
- Thử nghiệm tích hợp monitoring thời gian thực ( Prometheus + Grafana) vào CI/CD.  

**Xem xét cá nhân**: Áp dụng vào dự án tiếng Anh hiện tại để nâng Cao Suấtsecure khi triển khai cho các client Việt Nam.


## Programming

*   Hôm nay mới tìm hiểu kỹ về `useMemo` và `useCallback` trong React, hóa ra không phải cứ bọc mọi thứ vào đó là tốt vì nó tốn chi phí tính toán để so sánh dependency.
*   Chỉ nên dùng khi xử lý các phép toán nặng hoặc khi giá trị đó là dependency của một hook/component khác để tránh re-render không cần thiết.

*Note: Phải check lại performance bằng React DevTools trước khi quyết định dùng, đừng lạm dụng kẻo lại phản tác dụng.*


## Travel Tech

Em mới khám phá **Google Maps API** hỗ trợ tìm kiếm địa điểm theo loại dịch vụ (restaurants, nhà hàng, quán cà phê) trong 5 km xung quanh vị trí hiện tại. API trả về dữ liệu JSON bao gồm tên, địa chỉ, đánh giá và thậm chí đánh dấu on-map.  

- **Tham khảo**: `https://developers.google.com/maps/documentation/places/web-service/overview`  
- **Lời khuyên cá nhân**: Tạo một API key riêng cho dự án thử nghiệm và test chậm intermittent throttling với `wait` 1s giữa các request.

> **Mở rộng**: Sử dụng dữ liệu này để xây dựng một ứng dụng “định hướng du lịch” kiểu tri thức địa phương, nhắc lại quán ăn ngon ngay khi người dùng gõ địa điểm.
