# 2026-05-09

## DevOps  
- Hôm nay mình đã học cách ối lượng **Trạng thái terraform** bằng eingegn **remote backends** như S3 hoặc Azure Blob Storage để tránh xung đột khi làm đồng thời với đồng đội.  
- Một số thói quen tốt: ối lượng versioning module để dễ dàng quản lý thay đổi, sử dụng **state locking** để ngăn tràn ch Héash.  
- Mình plan áp dụng để tự động hóa triển khai môi trường staging cho dự án SaaS mới, giảm bớt thời gian sửa lỗi do xung đột state file.  

**Ghi chú cá nhân:** Sẽ mở rộng kiến thức về **Terraform Cloud** để tích hợp với CD pipeline nhé!


## Automation

- Khi dùng **GitHub Actions** để tự động triển khai Docker image lên **AWS ECR**, chỉ cần một workflow đơn giản: `docker build`, `docker tag` và `aws ecr get-login-password`.  
- Sử dụng `cache` cho layer `node_modules` giúp thời gian build giảm tới 40% so với build từ đầu mỗi lần.  

> Mình sẽ thử tích hợp bước này vào dự án hiện tại để giảm thời gian CI/CD và tránh việc phải login lại mỗi lần.


## Programming  
Hôm nay mình khám phá kỹ thuật *currying* trong JavaScript để tạo các hàm fleible.  
- Hy vọng sẽ áp dụng để hoàn thiện hệ thống tính toán phí subscription trong dự án SaaS đang phát triển.  
- Cảm ơn lắm anh Dave đã chia sẻ video học này - hiệu ứng thậm chí vượt trội hơn qúá thôi kỳ vọng của mình!


## DevOps

Tôi đã tìm hiểu cách sử dụng **GitHub Actions** để triển khai ứng dụng Flask lên AWS Elastic Beanstalk một cách toàn automatic. Nhờ vào `eb init` và script `deploy.yml`, mỗi khi có push vào branch `main` Azure sẽ pull code, build, và deploy lên môi trường staging mà không cần thao tác thủ công. Ngoài ra, tôi cũng tích hợp unit‑test style PyTest vào workflow để đảm bảo duy trì chất lượng mã.

Cảm thấy rất tiện lợi và muốn mở rộng áp dụng cùng dự án Django của mình trong tháng tới.




## DevOps  

-Hôm nay tôi biết thêm cách cấu hình Jenkins để tự động hóa deployment vào cloud (AWS).  
- Điều này giúp giảm chi phí và tối ưu hóa lente-release.  
- Tôi cũng thử integración Jenkins với GitLab để xử lý pull request tự động.  

**Ghi chú cá nhân:**  
Tôi plan áp dụng Jenkins trong dự án của công ty để thay thế quy trình manuel hiện tại. Cần tìm hiểu thêm vềadvanced features như pipeline testing!


## Automation

Nhiều ngân hàng đã triển khai **micro‑services** trên nền tảng Docker Swarm, kết hợp với GitHub Actions để tự động build, kiểm tra và triển khai lên Kubernetes mỗi khi có commit mới vào branch `develop`. Điều này giúp giảm thời gian “release cycle” xuống 30 phút và đồng thời tăng độ tin cậy khi có rollback nhanh qua `helm rollbacks`.  

*Plan*: thử mô phỏng pipeline tương tự cho dự án internal, dùng `Docker Compose` + `Docker Hub` để triển khai CI/CD ngay rành đầu tuần.


## Automation

Nhờ tạo script `Invoke-RestMethod -Uri`, tôi có thể xử lý các API web nhanh hơn và tự động.
+ Lệnh này giúp gọi API lấy dữ liệu và làm việc với số lượng lớn mà không cần coding.
+ Tôi sẽ áp dụng làm ra một dự án chính để tích lũy kinh nghiệm.
* Vừa thực hiện, nhận định cách Automate quá trình thao tác có thể giúp tiết kiệm thời gian hiệu quả hơn.




##Trekking - Hiking - Camping  
- Tôi học cách sử dụng ứng dụng offline (như Maps.me) để định vị trong όλα Lorsquен Gareth uninhabited, trong khi trekking ở treffen Phong Nha.  
- Cần chuẩn bị áo khaki phù hợp với thời tiết thay đổi ở khu vực núi vựccdnjs, đặc biệt là giỗi đặc biệt ở một đêm.  

**Nghiện cá nhân:** Chuyến đi trekking đơn giản vào^{[1]}}Container Lăng Vịnribu nhân bản sau vài tháng để험 Daniels về khám пробаблить tự nhiên Việt Nam.
