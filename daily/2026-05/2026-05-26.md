# 2026-05-26


## Travel Tech

Hôm nay mình học được cách sử dụng Google Maps Platform APIs (như Places API hay Directions API) để tích hợp bản đồ và tìm đường vào ứng dụng web/dịch vụ du lịch. API này giúp tự động gợi ý địa điểm, tính khoảng cách và tối ưu hành trình cho khách du lịch.

- Có thể kết hợp với dữ liệu địa phương (nhà hàng, khách sạn) để tạo trải nghiệm cá nhân hóa.
- Miễn phí $200/tháng, phù hợp cho dự án thử nghiệm hoặc startup nhỏ.

Mình thấy công cụ này rất hữu ích để xây dựng tính năng "tour thông minh" cho ứng dụng du lịch Việt Nam. Plan tuần sau sẽ thử nghiệm với một dự án demo nhỏ về tour thành phố Đà Lạt.


## [Automation]

- Hômnay mình học cách viết pipeline CI/CD đơn giản bằng GitHub Actions.  
- Hiểu được cách cấu hình cache cho thư viện để giảm thời gian build một cách đáng kể.  Sẽ thử áp dụng pipeline này vào dự án API của mình vào cuối tuần này.


## Programming

Tôi vừa tìm hiểu về **Pattern Matching trong Python 3.10**. Thay vì dùng chuỗi `if-elif` hay `dict.get`, pattern matching cho phép tôi kiểm tra cấu trúc dữ liệu phức tạp một cách ngắn gọn và rõ ràng. Ví dụ, một tuple `(op, x, y)` có thể được phân tích ngay trong `match` để thực hiện phép tính tương ứng.

Nhận xét: Cần thử áp dụng pattern matching vào module xử lý dữ liệu đầu vào của dự án hiện tại để tăng tính rõ ràng và bảo trì.


## Trekking  
Con đường đảo lúa và núi núi ở Việt Nam đã biến minh chứng qua quyết định cho khám phá sinh trật tự tự nhiên. Điều này khiến hàng nghìn hành trình đến vào phục vụ cách mạng được trưng bày được bạn trên National Geographic. Luồng cảnh khám phá khắp địa phương còn tạo nguồn diffusion mạnh cho dịch vụ giao họch mới.


## DevOps

- Vừa tìm hiểu xong về khái niệm "GitOps" và cách dùng ArgoCD để đồng bộ trạng thái của Kubernetes cluster với Git repo.
- Thay vì chạy `kubectl apply` thủ công, giờ cứ push code lên là hệ thống tự cập nhật, giảm bớt sai sót do thao tác tay.

Ngày mai sẽ thử setup một pipeline đơn giản cho project cá nhân xem chạy mượt không.


## Automation

- Khi sử dụng `cron` trên Linux, có thể đặt thời gian chạy với độ chính xác đến giây bằng cách thêm một trường thứ năm (seconds) nếu dùng `fcron` hoặc `systemd timers` thay vì `cron` truyền thống.  
- Để tránh lỗi “duplicate job”, luôn đặt một file lock (ví dụ `flock -n /tmp/myjob.lock`) ngay trong script, giúp chỉ một instance duy nhất chạy cùng lúc.  

> **Ghi nhớ:** Sắp tới sẽ áp dụng `systemd timer` cho việc sao lưu database hàng ngày để có độ chính xác cao hơn và dễ quản lý log hơn.
