# 2026-05-04

## Automation

- Khi sử dụng `cron` trên Linux, có thể khai báo môi trường biến `PATH` ngay trong file crontab để tránh lỗi “command not found” khi script gọi các lệnh không có trong PATH mặc định.  
- `systemd-timer` là một giải pháp thay thế hiện đại hơn `cron`, cho phép cấu hình thời gian chạy theo định dạng ISO 8601 và hỗ trợ các thuộc tính như `OnBootSec` hay `RandomizedDelaySec` một cách dễ dàng.  

**Nhận xét:** Việc chuẩn hoá môi trường cho cron giúp script chạy ổn định hơn; mình sẽ thử migrate một số job quan trọng sang systemd‑timer trong tuần tới.  


##Automation
- Hôm nay mình tự động hoá việc triển khai CI/CD bằng GitHub Actions, cấu hình pipeline để build, test và deploy ứng dụng lên Kubernetes.
- Nhận ra rằng việc quản lý secret và environment variables trong pipeline cần được bảo mật và rõ ràng.
- Kế hoạch: Áp dụng workflow này cho dự án mới và thử nghiệm reusable workflows trong tuần tới.


## Automation

- Khi sử dụng `cron` trên Linux, có thể đặt biến môi trường `MAILTO=""` để ngăn email thông báo lỗi khi lệnh thất bại.  
- Đối với các script Python, việc bật `set -e` trong bash wrapper giúp dừng pipeline ngay khi một bước gặp lỗi, tránh việc các bước tiếp theo chạy vô ích.  

> **Ghi chú:** Sắp tới tuần này mình sẽ áp dụng `set -e` cho các CI pipeline hiện tại để giảm thời gian debug.


## Travel Tech

Hômnay mình đã thử triển khai GitHub Actions để tự động hoá pipeline CI/CD cho dự án microservice, giảm thời gian build từ 15 phút xuống còn 3 phút. Kết quả hiện ra log chi tiết và các bước deploy nhanh chóng, giúp team dễ dàng theo dõi tiến độ. Cố định cách làm này để áp dụng cho tất cả repository mới.


Tuyệt vời! Đây là Today I Learned của mình:

## Programming

Hôm nay mình tìm hiểu về cách sử dụng `async/await` trong Python để viết code bất đồng bộ (asynchronous) dễ đọc và quản lý hơn. Thay vì dùng callbacks lồng nhau, `async/await` giúp code trông giống như code đồng bộ, nhưng lại tận dụng tối đa tài nguyên khi chờ đợi các tác vụ I/O.  Điều này đặc biệt hữu ích khi làm việc với các API hoặc database.

*   `async` định nghĩa một hàm là coroutine.
*   `await` dùng để tạm dừng thực thi hàm cho đến khi một coroutine khác hoàn thành.

Mình sẽ thử áp dụng `async/await` vào project xử lý dữ liệu từ nhiều API để cải thiện hiệu suất.
