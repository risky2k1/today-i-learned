# 2026-05-02

## DevOps

- Với GitHub Actions, tôi vừa thử nghiệm biên dịch Docker images trực tiếp trong job thư mục `docker` và push lên GitHub Packages bằng một chỉ một dòng `docker push ghcr.io/<user>/<repo>:${{ github.sha }}`; tạo cơ chế tự động quay vòng tag theo hash commit.
- Dùng `act` local runner giúp cô lập môi trường tích hợp case test nhanh chóng, tiết kiệm thời gian khi sửa lỗi chức năng trên runners thật.

Nhận xét: Bây giờ chuẩn bị triển khai pipeline CI/CD cho dự án API backend, bật cache `~/.m2` và `node_modules` để giảm thời gian build.


Tuyệt vời! Mình sẽ bắt đầu ghi lại Today I Learned cho bản thân.

## Programming

*   **`str.startswith()` và `str.endswith()`:** Hai hàm này cực kỳ hữu ích để kiểm tra xem một chuỗi có bắt đầu hoặc kết thúc bằng một chuỗi con cụ thể hay không. Mình đã từng dùng `if string.startswith("http")` để lọc các URL.
*   **List Comprehensions:**  Cách viết ngắn gọn và hiệu quả để tạo ra một danh sách mới dựa trên một danh sách hiện có. Ví dụ: `[x * 2 for x in range(10)]` sẽ tạo ra một danh sách các số từ 0 đến 18.
*   **`f-strings` (Formatted String Literals):**  Cách dễ dàng nhất để chèn các biến vào chuỗi. Ví dụ: `name = "Lan"; print(f"Chào {name}!")`

**Nhận xét:** Mình thấy list comprehensions rất mạnh mẽ và sẽ cố gắng sử dụng chúng nhiều hơn trong các dự án sắp tới để code ngắn gọn và dễ đọc hơn.  Mình cũng cần luyện tập thêm về việc sử dụng `startswith()` và `endswith()` để tránh viết các đoạn code phức tạp hơn cần thiết.


# DevOps

- Tôi đã học được về một công cụ điều tiết tìm kiếm trong DevOps.
- Sử dụng GitHub Actions giúp tối ưu hóa quy trình triển khai nhiều động tự động.
- Giúp nâng cao chất lượng ứng dụng và cảnh báo sớm nhiều khiến lôi kéo giảm.

💡 Cảm ơn người chia sẻ cho tôi!  
Sẽ áp dụng trên một trải nghiệm thực tế ngay sau này.

**Kết nối:** Nỗ lực điều chỉnh dễ dàng hơn nữa nhé!


## Trekking  
Tôi môi giới hắn về giá trị giới thiệu hành trình bình ổn khi đi bộ dã nguồn. Tiếng gió còn chuyển từ chấn lửa nằm lặng hơn vắng trống. ✨


## Travel Tech- Hôm nay mình đã thử chế độ offline của Google Maps và tải bản bản đồ Sapa để dùng mà không cần internet.
- Kiểm tra và kích hoạt eSIM của Viettel để duy trì kết nối trên đường mòn miền núi.
- Kế hoạch mua eSIM cho chuyến Đà Mau cuối tuần tới và ghi lại cảm nhận.


## Automation
- Hôm nay mình tìm hiểu về cách dùng Ansible để tự động deploy ứng dụng lên server. Mình đã viết playbook đầu tiên với roles và handlers để quản lý dịch vụ Nginx.  
- Mình sẽ áp dụng ngay vào dự án cá nhân để thay thế việc deploy thủ công, tiết kiệm khá nhiều thời gian.  

*Comment:* Thấy thú vị với cách Infrastructure as Code này, sau này sẽ tìm hiểu thêm về modules quản lý database và bảo mật.


## Travel Tech- Hôm nay mình thử app AR để “điêu” các điểm tham quan ẩn القتل trong phố Hội An, cảm thấy rất mới lạ và tiện lợi.  
- Nhận ra rằng việc dùng map offline và ghi chú nhanh giúp mình luôn biết mình đang ở đâu mà không lo mất mạng.  
- Kế hoạch sẽ thử đặt homestay qua một app địa phương vào cuối tuần tới để trải nghiệm homestay truyền thống.

*Plan:* sẽ ghi lại cảm nhận sau khi ở lại và chia sẻ review cho đồng nghiệp developer.


Tuyệt vời! Đây là Today I Learned của mình:

## Programming

Hôm nay mình tìm hiểu về cách sử dụng `async/await` trong Python để viết code bất đồng bộ (asynchronous) dễ đọc và dễ bảo trì hơn. Thay vì dùng callbacks lồng nhau, `async/await` giúp code trông giống như code đồng bộ, nhưng lại tận dụng tối đa tài nguyên khi chờ đợi các tác vụ I/O.  Việc này đặc biệt hữu ích khi làm việc với các API hoặc database.

*   `async` định nghĩa một hàm là coroutine.
*   `await` dùng để tạm dừng thực thi hàm cho đến khi một coroutine khác hoàn thành.

Mình sẽ thử áp dụng `async/await` vào project xử lý dữ liệu từ nhiều API để cải thiện hiệu suất.


## Trekking- Hiking - Camping

- Đã thử thách mình lên đỉnh Fansipan vào cuối tuần, cảm nhận không khí trong lành và cảm giác tự hào khi đứng trên đỉnh núi cao nhất Việt Nam.  
- Học được kỹ năng lắp trại nhanh chóng và cách xây dựng trại tạm bằng cách dùng trekking pole và tấm nhựa.

*Plan:* Sắp xếp cuối tuần tới để lên núi Fansipan lại, chuẩn bị kỹ hơn gear và chuẩn bị sẵn sẵn cho trời mưa.
