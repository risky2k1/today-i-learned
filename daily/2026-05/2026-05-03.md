# 2026-05-03

## Trekking - Hiking - Camping (Địa điểm: Đà Lạt)

- Đã khám phá đường mòn **Langbiang** vào sáng sớm, nơi có tầm nhìn toàn cảnh thành phố và dãy núi xanh mướt.  
- Thấy rằng việc mang theo **bình nước cách nhiệt** giúp giữ nước lạnh suốt cả ngày, tránh phải mua nước đóng chai.  
- Khi dừng nghỉ, thử **đốt lửa bằng đá lò xo** (firesteel) thay vì bật lò phát điện, cảm giác tự lập hơn rất nhiều.

> Cảm nhận: Hành trình nhẹ nhàng nhưng đầy thử thách, mình sẽ lên kế hoạch kéo dài 2 ngày ở Đà Lạt để thử thêm các đường mòn khác như Trại Vân Trì.


## Programming

- Hôm nay mới nắm được cách dùng hook `use()` mới trong React 19, cho phép đọc trực tiếp giá trị của Promise hoặc Context mà không cần bọc trong `useEffect` hay gọi `useContext` như trước.
- Áp dụng thử vào đoạn code lấy thông tin user từ API hiện tại thì thấy giảm được gần một nửa số dòng xử lý loading, error so với cách cũ.

Sắp tới refactor module xác thực của dự án công ty sẽ dùng hook này để code gọn hơn.


## Programming

* Hôm nay mới biết cách dùng `Array.prototype.reduce()` trong JavaScript để transform một mảng các object thành một object duy nhất dựa trên một key cụ thể, thay vì phải dùng vòng lặp `forEach` rồi tạo biến tạm bên ngoài.
* Cách này giúp code nhìn "functional" hơn và tránh được việc mutate (thay đổi) biến bên ngoài scope.

Thấy hay phết, mai sẽ refactor lại mấy cái hàm xử lý data cũ trong project đang làm xem có gọn hơn không.


## Automation

- Hôm nay tôi học cách sử dụng **GitHub Actions** để tự động chạy linter và test mỗi khi push code lên nhánh `develop`.  
- Thiết lập workflow đơn giản với `actions/setup-node` và `run: npm test` giúp phát hiện sớm lỗi trước khi merge.  
- Cá nhân thấy đây là bước quan trọng để giảm tải công việc thủ công và tăng độ tin cậy của CI/CD; tuần tới tôi dự định mở rộng workflow để tự động deploy bản build lên môi trường staging khi có tag release.


## Travel Tech  
- Tìm hiểu về hệ thống động giá phòng khách sạn, dùng AI để phân tích nhu cầu khách hàng và điều chỉnh giá theo thời gian thực.  
- Ứng dụng công nghệ blockchain trong du lịch Việt Nam để tăng tính minh bạch trong giao dịch vé tham quan và phòng lưu trú.  

*Nhận xét:* Có thể thử áp dụng công nghệ này cho dự án nhỏ về hệ thống đặt phòng homestay tại Đà Lạt.
